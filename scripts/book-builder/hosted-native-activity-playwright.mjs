import { exerciseAuthoredSizeSaveReload, exerciseExternalActivityNavigation, exerciseIntegerHotspotCoordinates, openStudentsUnitOnePage } from "./native-authoring-regressions.mjs";
import { nativeDocumentPair } from "./hosted-native-activity-document-fixtures.mjs";
import { componentActivityOrderEntries, projectComponentActivityOrder, reorderComponentActivity } from "../../src/data/native-activities/nativeActivityOrder.js";

import { exerciseMarkWordsAuthoring, exerciseMarkWordsHostedViewer } from "./hosted-native-activity-mark-words.mjs";
import assert from "node:assert/strict"; import { exerciseHostedComposition } from "./hosted-native-composition.mjs";
import { createHash } from "node:crypto";
import { createReadStream } from "node:fs";
import { mkdir, readFile, stat } from "node:fs/promises";
import { createServer } from "node:http";
import path from "node:path";
import { chromium, expect } from "@playwright/test";
import sharp from "sharp";
import { inspectManagedPdf } from "../../lib/book-assets/pdf-inspection.js";
import { addDisposableCanonicalPageNative, exerciseDisposableCanonicalPageNativeDeletion, createAndSavePageHotspot, drawPageHotspot, studentsBookCurrentPageEnvelope } from "./students-book-current-browser-fixtures.mjs";
import { VIEWER_EXIT_FULLSCREEN_MESSAGE } from "../../src/shared/viewerPresentationProtocol.js";
import { createEmptyHostedTeacherUiDocument } from "../../src/data/ultimate-b2/hostedTeacherUiDocument.js";
import { roundStageValue } from "../../src/components/builder-studio/stageGeometry.js";
import { createEmptyUltimateB2UnitExtras } from "../../src/data/ultimate-b2/unitExtras.js";
import { createNativeOpenResponseQuestion } from "../../src/data/native-activities/nativeOpenResponse.js";
import { normalizeNativeRuntimePublicDocument } from "../../src/data/native-activities/nativeActivityRuntimeValidation.js";
import { createUltimateB2HostedOpenResponseSeed } from "../../src/data/ultimate-b2/hostedOpenResponseDraft.js";
import { findStudentsBookImplementation } from "../../src/data/ultimate-b2/studentsBookCatalog.js";

import { localPlaywrightLaunchOptions } from "../android-teacher/playwright-launch-options.mjs";
import { assertAhemRendering, createVideoCompanionUploader, logicalFontSize, uploadReadableText, verifyReadableTextStartsOffAndBlocksIncompleteSave, waitForHostedViewerFrame } from "./hosted-native-activity-authoring-helpers.mjs";
import { exerciseCompleteSentencesAuthoring, exerciseCompleteSentencesBulkHotspotImport, handleCompleteSentencesFontRequest } from "./hosted-native-activity-complete-sentences.mjs";
import { exerciseDragDropPanelTransitionGuard, exerciseDragDropProxy, exerciseDragDropResetGuard, exerciseValidatedDragDrop, measureDragDrop } from "./hosted-native-activity-drag-drop.mjs";
import { exerciseDragDropExtensions } from "./hosted-native-activity-drag-drop-extensions.mjs";
import { measureEditorDock } from "./hosted-native-activity-geometry.mjs";
import { halfHeightChoicePng, hotspotMp3, listeningSrt, landscapeChoicePng, oldschoolSrt, onePixelPng, portraitChoicePng, replacementTallReadablePng, replacementWorksheetPdf, secondPixelPng, smallFourThreeChoicePng, tallReadablePng, worksheetPdf } from "./hosted-native-activity-media-fixtures.mjs";
import { assertUnitExtrasModalLayout } from "./hosted-native-activity-modal-layout.mjs";
import { exerciseOldschoolQuestionVisibility, exerciseOldschoolMultipleChoicePanel, exerciseOldschoolPersistentTranscript, importOldschoolExactTranscriptMapping } from "./hosted-native-activity-oldschool-listening.mjs";
import { createVisualHotspotCreator, exerciseBulkHotspotImport, exerciseVisualHotspotPointerMove, setVisualHotspotGeometry, sourceRectanglesOverlap } from "./hosted-native-activity-single-choice.mjs";
import { exerciseSupplementalAudio } from "./hosted-native-activity-supplemental-audio.mjs";
import { exerciseOuterFocusAspectRatio, exercisePersistedVisualRefinements } from "./hosted-native-activity-visual-refinement.mjs";
import { createViewerFrameNavigationLifecycle, viewerFrameNavigationIdentity } from "./viewer-frame-navigation-lifecycle.mjs";
const builderRoot = path.resolve("dist-netlify/ultimate-b2-builder");
const viewerRoot = path.resolve("dist-netlify/ultimate-b2-interactive");
const contentRoot = "/builder/api/content/books/ultimate-b2/components/ultimate-b2-students-book";
const nativeRoot = "/builder/api/native-activities/books/ultimate-b2/components/ultimate-b2-students-book";
const unitExtraRoot = "/builder/api/unit-extras/books/ultimate-b2/components/ultimate-b2-students-book";
const previewToken = `v2.${Buffer.from('{"fixture":true}').toString("base64url")}.${"a".repeat(43)}`;
let previewAuthorizationSequence = 0;
const nextPreviewToken = () => { previewAuthorizationSequence += 1; return `v2.${Buffer.from(JSON.stringify({ fixture: true, nonce: previewAuthorizationSequence })).toString("base64url")}.${String(previewAuthorizationSequence % 10).repeat(43)}`; };
const publicationRoot = "/builder/api/publication/books/ultimate-b2";
const builderPagesRoot = "/builder/api/pages/books/ultimate-b2/components/ultimate-b2-students-book";
const previewPagesRoot = "/preview/pages/books/ultimate-b2/components/ultimate-b2-students-book";
const mime = { ".css": "text/css", ".html": "text/html", ".js": "text/javascript", ".json": "application/json", ".png": "image/png", ".svg": "image/svg+xml", ".mp3": "audio/mpeg", ".mp4": "video/mp4", ".pdf": "application/pdf", ".ttf": "font/ttf" };
const listeningMp3 = await readFile(path.resolve("src/assets/books/ultimate-b2/teacher-offline-media/unit-1-television-dialogue.mp3"));
const videoMp4 = await readFile(path.resolve("src/assets/books/ultimate-b2/teacher-offline-media/ultimate-b2-startup-intro.mp4"));
const videoSrt = "1\r\n00:00:00,000 --> 00:00:02,000\r\nWelcome to the companion video.\r\n\r\n2\r\n00:00:02.200 --> 00:00:05.000\r\nContinue with the activity questions.";
const uploadVideoCompanion = createVideoCompanionUploader(videoMp4, videoSrt);
const replacementVideoSrt = "1\n00:00:00,000 --> 00:00:02,100\nReplacement opening subtitle.\n\n2\n00:00:02,300 --> 00:00:05,100\nReplacement closing subtitle.";
const nativeDocuments = new Map(); const nativeAssets = new Map(); const nativeUploads = new Map(); const nativeAssetByContent = new Map(); const nativeFonts = new Map(); const nativeFontUploads = new Map(); const deleteMutations = new Map(); const lifecycleMutations = new Map(); const requestedPaths = []; const viewerRequests = []; const authorizationIntents = []; const viewerExchangeIntents = [];
let indexRevision = 0; let nativeIndex = { schemaVersion: "1.0", activities: [] }; let origin = "";
const orderPages = () => projectComponentActivityOrder(componentActivityOrderEntries([], nativeIndex, activityLifecycle));
let catalogDiagnostics = [{ activityId: "ultimate-b2-sb-u1-p1-o88", kind: "open-response", pageId: "ub2-sb-unit-1-part-1", code: "pair_missing", stage: "pair-load", loadable: false, ready: false }];
let hotspotRevision = 0; let hotspotManifest = { schemaVersion: "1.0", packageSlug: "ultimate-b2", componentSlug: "students-book", pages: {} };
let lifecycleRevision = 0; let activityLifecycle = { schemaVersion: "1.0", activities: {} };
let uploadSequence = 10; let nextNativeOrdinal = 90;
let unitExtraRevision = 0; let unitExtraDocument = createEmptyUltimateB2UnitExtras(); let unitExtraUploadSequence = 1;
const unitExtraUploads = new Map(); const unitExtraAssets = new Map();
const legacyActivityId = "ultimate-b2-sb-u1-p1-o89";
function json(response, statusCode, value) { const bytes = Buffer.from(JSON.stringify(value)); response.writeHead(statusCode, { "Cache-Control": "no-store", "Content-Length": bytes.length, "Content-Type": "application/json" }); response.end(bytes); }
async function requestBytes(request) { const chunks = []; for await (const chunk of request) chunks.push(chunk); return Buffer.concat(chunks); }
async function requestJson(request) { return JSON.parse((await requestBytes(request)).toString("utf8")); }
function envelope(resource, documentKey, revision, document) { return { bookSlug: "ultimate-b2", componentSlug: "ultimate-b2-students-book", resource, documentKey, schemaVersion: "1.0", revision, source: revision ? "database" : "repository", document }; }
const pagesEnvelope = () => studentsBookCurrentPageEnvelope(hotspotRevision);
async function assertContentTabActive(page, editorSelector) { assert.equal(await page.locator(editorSelector).getByRole("tab", { name: "Content" }).getAttribute("aria-selected"), "true"); }
const legacyPair = nativeDocumentPair(legacyActivityId, "open-response", "ub2-sb-unit-1-part-1", "Legacy checksum fixture");
legacyPair.publicDocument.metadata.visibleInstructionText = "Deprecated native instruction must stay hidden.";
const legacyQuestionId = `q-${"8".repeat(32)}`;
legacyPair.publicDocument.parts[0].interaction.questions = [{ ...createNativeOpenResponseQuestion(legacyQuestionId, 0), prompt: "Legacy compatibility prompt" }];
legacyPair.teacherDocument.parts[0].solution.modelAnswers = [{ questionId: legacyQuestionId, text: "Legacy private answer" }];
const legacyTeacherBeforeCleanup = structuredClone(legacyPair.teacherDocument);
const legacyAssetId = "10000000-0000-4000-8000-000000000009";
legacyPair.publicDocument.assets = [{ assetId: legacyAssetId, checksumSha256: "9".repeat(64), role: "activity_artwork", slot: "legacy-background" }];
legacyPair.publicDocument.parts[0].interaction.artwork = [{ id: `art-${"9".repeat(32)}`, assetSlot: "legacy-background", area: { x: 0, y: 0, width: 1024, height: 582 }, order: 0, altText: "Legacy background", decorative: false, fit: "cover" }];
nativeDocuments.set(legacyActivityId, legacyPair);
nativeAssets.set(legacyAssetId, { activityId: legacyActivityId, slot: "legacy-background", type: "image/png", bytes: onePixelPng });
indexRevision = 1;
nativeIndex = { schemaVersion: "1.0", activities: [{ activityId: legacyActivityId, kind: "open-response", placement: { pageId: "ub2-sb-unit-1-part-1" }, sortOrder: 1 }] };
async function staticResponse(pathname, response) {
  const relative = pathname === "/" ? "index.html" : decodeURIComponent(pathname).replace(/^\/+/, ""); let file = path.resolve(builderRoot, relative);
  let details = file.startsWith(`${builderRoot}${path.sep}`) ? await stat(file).catch(() => null) : null;
  if (!details?.isFile()) { file = path.join(builderRoot, "index.html"); details = await stat(file); }
  response.writeHead(200, { "Content-Length": details.size, "Content-Type": mime[path.extname(file).toLowerCase()] || "application/octet-stream" }); createReadStream(file).pipe(response);
}

const server = createServer(async (request, response) => {
  const url = new URL(request.url, "http://127.0.0.1"); requestedPaths.push(url.pathname);
  if (url.pathname === "/builder/api/auth" && url.searchParams.get("action") === "me") return json(response, 200, { authenticated: true, builderUser: { id: "phase-3-browser", full_name: "Phase 3 Browser", role: "developer", status: "active" } });
  if (url.pathname === "/builder/api/preview-authorization" && request.method === "POST") { authorizationIntents.push(await requestJson(request)); return json(response, 200, { token: nextPreviewToken(), expiresAt: "2099-01-01T00:00:00.000Z" }); }
  if (url.pathname === `${contentRoot}/hotspots` && request.method === "GET") return json(response, 200, envelope("hotspots", "default", hotspotRevision, hotspotManifest));
  if (url.pathname === `${contentRoot}/hotspots` && request.method === "PUT") { const body = await requestJson(request); hotspotRevision += 1; hotspotManifest = structuredClone(body.document); return json(response, 200, { ...envelope("hotspots", "default", hotspotRevision, hotspotManifest), currentRevision: hotspotRevision, idempotent: false }); }
  if (url.pathname === `${contentRoot}/unit-extras` && request.method === "GET") return json(response, 200, envelope("unit-extras", "default", unitExtraRevision, unitExtraDocument));
  if (url.pathname === `${unitExtraRoot}/save` && request.method === "POST") { const body = await requestJson(request); if (body.expectedRevision !== unitExtraRevision) return json(response, 409, { error: "revision_conflict", currentRevision: unitExtraRevision }); unitExtraRevision += 1; unitExtraDocument = structuredClone(body.document); return json(response, 200, { ...envelope("unit-extras", "default", unitExtraRevision, unitExtraDocument), currentRevision: unitExtraRevision, idempotent: false }); }
  const unitExtraAssetMatch = url.pathname.match(new RegExp(`^${unitExtraRoot}/units/(unit-[0-9]+)/(videos|audios)/((?:video|audio)-[a-f0-9]+)/assets/(prepare|finalize)$`));
  if (unitExtraAssetMatch && request.method === "POST") {
    const body = await requestJson(request); if (body.expectedRevision !== unitExtraRevision) return json(response, 409, { error: "revision_conflict", currentRevision: unitExtraRevision });
    const mediaKind = unitExtraAssetMatch[2]; const itemId = unitExtraAssetMatch[3]; const mimeType = mediaKind === "audios" ? "audio/mpeg" : "video/mp4"; const role = mediaKind === "audios" ? "unit_extra_audio" : "unit_extra_video";
    if (unitExtraAssetMatch[4] === "prepare") { const uploadId = `20000000-0000-4000-8000-${String(unitExtraUploadSequence++).padStart(12, "0")}`; unitExtraUploads.set(uploadId, { unitId: unitExtraAssetMatch[1], mediaKind, itemId, mimeType, file: body.file, bytes: null }); return json(response, 200, { outcome: "prepared", uploadId, authorization: { url: `${origin}/test/unit-extra-uploads/${uploadId}`, headers: { "Content-Type": mimeType } }, idempotent: false }); }
    const upload = unitExtraUploads.get(body.uploadId); if (!upload?.bytes || upload.itemId !== itemId || upload.mediaKind !== mediaKind) return json(response, 400, { error: "upload_incomplete" });
    const checksumSha256 = createHash("sha256").update(upload.bytes).digest("hex"); const assetId = `30000000-0000-4000-8000-${String(unitExtraUploadSequence++).padStart(12, "0")}`; unitExtraAssets.set(assetId, { ...upload, checksumSha256 });
    return json(response, 200, { outcome: "completed", reference: { assetId, checksumSha256, role, slot: upload.itemId }, previewUrl: `${unitExtraRoot}/units/${upload.unitId}/${mediaKind}/${upload.itemId}/assets/${assetId}/preview`, metadata: { mimeType, byteSize: upload.bytes.length, durationMs: mediaKind === "videos" ? 5_840 : null }, idempotent: false });
  }
  const unitExtraUploadMatch = url.pathname.match(/^\/test\/unit-extra-uploads\/([0-9a-f-]+)$/);
  if (unitExtraUploadMatch && request.method === "PUT") { const upload = unitExtraUploads.get(unitExtraUploadMatch[1]); if (!upload) return json(response, 404, {}); upload.bytes = await requestBytes(request); response.writeHead(204); return response.end(); }
  const unitExtraPreviewMatch = url.pathname.match(new RegExp(`^${unitExtraRoot}/units/(unit-[0-9]+)/(videos|audios)/((?:video|audio)-[a-f0-9]+)/assets/([0-9a-f-]+)/preview$`));
  if (unitExtraPreviewMatch && request.method === "GET") { const asset = unitExtraAssets.get(unitExtraPreviewMatch[4]); if (!asset) return json(response, 404, {}); response.writeHead(200, { "Content-Type": asset.mimeType, "Content-Length": asset.bytes.length }); return response.end(asset.bytes); }
  if (url.pathname === `${contentRoot}/ui-controller` && request.method === "GET") return json(response, 200, envelope("ui-controller", "default", 0, createEmptyHostedTeacherUiDocument()));
  if (url.pathname === publicationRoot && request.method === "GET") return json(response, 200, {
    bookSlug: "ultimate-b2", compilerId: "ultimate-b2-product-v1", releaseSchemaVersion: "1.0", headRevision: 0, published: null, releases: [],
    components: [
      { componentSlug: "ultimate-b2-students-book", compilerId: "ultimate-b2-students-book-v2", releaseSchemaVersion: "2.0", currentSourceSha256: "1".repeat(64) },
      { componentSlug: "ultimate-b2-workbook", compilerId: "ultimate-b2-workbook-v1", releaseSchemaVersion: "1.0", currentSourceSha256: "2".repeat(64) },
      { componentSlug: "ultimate-b2-grammar-book", compilerId: "ultimate-b2-grammar-book-v1", releaseSchemaVersion: "1.0", currentSourceSha256: "3".repeat(64) },
    ],
  });
  if (url.pathname === `${contentRoot}/native-activity-index` && request.method === "GET") return json(response, 200, envelope("native-activity-index", "default", indexRevision, nativeIndex));
  if (url.pathname === `${nativeRoot}/lifecycle` && request.method === "GET") return json(response, 200, envelope("activity-lifecycle", "default", lifecycleRevision, activityLifecycle));
  if (url.pathname === `${nativeRoot}/order` && request.method === "GET") return json(response, 200, { pages: orderPages(), indexRevision, lifecycleRevision });
  if (url.pathname === `${nativeRoot}/reorder` && request.method === "POST") {
    const input = await requestJson(request);
    if (input.expectedIndexRevision !== indexRevision || input.expectedLifecycleRevision !== lifecycleRevision) return json(response, 409, { error: "revision_conflict" });
    const next = reorderComponentActivity({ canonical: [], index: nativeIndex, lifecycle: activityLifecycle, ...input });
    nativeIndex = next.index; activityLifecycle = next.lifecycle; indexRevision++; lifecycleRevision++;
    return json(response, 200, { outcome: "saved" });
  }
  if (url.pathname === `${nativeRoot}/catalog` && request.method === "GET") return json(response, 200, { schemaVersion: "1.0", bookSlug: "ultimate-b2", componentSlug: "ultimate-b2-students-book", activities: nativeIndex.activities.map((entry) => ({ ...entry, title: nativeDocuments.get(entry.activityId)?.publicDocument.metadata.title || entry.activityId, ready: true, issues: [] })), ...(catalogDiagnostics.length ? { invalidActivities: catalogDiagnostics } : {}) });
  if (await handleCompleteSentencesFontRequest({
    request, response, url, nativeRoot, origin, nativeFonts, nativeFontUploads, nativeAssets, nextSequence: () => uploadSequence++, json, requestBytes, requestJson,
  })) return;
  if (url.pathname === `${nativeRoot}/create` && request.method === "POST") {
    const body = await requestJson(request); const activityId = `ultimate-b2-sb-u1-p1-o${nextNativeOrdinal++}`; const title = body.title.trim() || `New ${body.kind === "image" ? "Image" : body.kind === "single-choice" ? "Multiple Choice" : body.kind === "complete-sentences" ? "Complete the Sentences" : body.kind === "listening" ? "Listening" : body.kind === "oldschool-listening" ? "Oldschool Listening" : body.kind === "drag-drop" ? "Drag & Drop" : "Open Response"}`;
    nativeDocuments.set(activityId, nativeDocumentPair(activityId, body.kind, body.pageId, title)); indexRevision += 1;
    nativeIndex = { schemaVersion: "1.0", activities: [...nativeIndex.activities, { activityId, kind: body.kind, placement: { pageId: body.pageId }, sortOrder: indexRevision }] };
    return json(response, 200, { outcome: "created", activityId, indexRevision, publicRevision: 1, teacherRevision: 1, kind: body.kind, placement: { pageId: body.pageId }, idempotent: false });
  }
  const lifecycleMatch = url.pathname.match(new RegExp(`^${nativeRoot}/activities/([a-z0-9-]+)/(retire|move)$`));
  if (lifecycleMatch && request.method === "POST") {
    const body = await requestJson(request); const activityId = lifecycleMatch[1]; const operation = lifecycleMatch[2]; const replay = lifecycleMutations.get(body.clientMutationId);
    if (replay) return replay.activityId === activityId && replay.operation === operation ? json(response, 200, { ...replay.result, outcome: "idempotent", idempotent: true }) : json(response, 409, { error: "mutation_id_conflict" });
    const nativeEntry = nativeIndex.activities.find((entry) => entry.activityId === activityId);
    const currentLifecycle = activityLifecycle.activities[activityId];
    const canonicalActive = !nativeEntry && currentLifecycle?.status !== "retired";
    const currentPageId = nativeEntry?.placement.pageId || currentLifecycle?.pageId || body.sourcePageId;
    if ((!nativeEntry && !canonicalActive) || currentPageId !== body.sourcePageId) return json(response, operation === "move" ? 409 : 404, { error: operation === "move" ? "location_conflict" : "activity_not_found" });
    if (operation === "move" && nativeEntry) {
      nativeEntry.placement = { pageId: body.destinationPageId };
      nativeDocuments.get(activityId).publicDocument.placement = { pageId: body.destinationPageId };
      indexRevision += 1; nativeDocuments.get(activityId).publicRevision += 1;
    } else {
      activityLifecycle = { ...activityLifecycle, activities: { ...activityLifecycle.activities, [activityId]: { status: operation === "retire" ? "retired" : "active", pageId: operation === "move" ? body.destinationPageId : currentPageId } } };
      lifecycleRevision += 1;
    }
    let removedHotspotCount = 0; const pages = {};
    for (const [pageId, hotspots] of Object.entries(hotspotManifest.pages)) { const retained = hotspots.filter((hotspot) => { const remove = hotspot.activityKey === activityId; if (remove) removedHotspotCount += 1; return !remove; }); if (retained.length) pages[pageId] = retained; }
    if (removedHotspotCount) { hotspotManifest = { ...hotspotManifest, pages }; hotspotRevision += 1; }
    const result = { outcome: operation === "move" ? "moved" : "retired", activityId, lifecycleRevision, indexRevision, publicRevision: nativeDocuments.get(activityId)?.publicRevision || 0, hotspotRevision, removedHotspotCount, destinationPageId: operation === "move" ? body.destinationPageId : null, destinationHotspotRequired: operation === "move", idempotent: false };
    lifecycleMutations.set(body.clientMutationId, { activityId, operation, result }); return json(response, 200, result);
  }
  const deleteMatch = url.pathname.match(new RegExp(`^${nativeRoot}/activities/([a-z0-9-]+)/delete$`));
  if (deleteMatch && request.method === "POST") {
    const body = await requestJson(request); const activityId = deleteMatch[1]; const replay = deleteMutations.get(body.clientMutationId);
    if (replay) return replay.activityId === activityId ? json(response, 200, { ...replay.result, outcome: "idempotent", idempotent: true }) : json(response, 409, { error: "mutation_id_conflict" });
    if (!nativeIndex.activities.some((entry) => entry.activityId === activityId)) return json(response, 404, { error: "native_activity_not_found" });
    nativeIndex = { ...nativeIndex, activities: nativeIndex.activities.filter((entry) => entry.activityId !== activityId) }; indexRevision += 1;
    let removedHotspotCount = 0; const pages = {};
    for (const [pageId, hotspots] of Object.entries(hotspotManifest.pages)) { const retained = hotspots.filter((hotspot) => { const remove = hotspot.activityKey === activityId; if (remove) removedHotspotCount += 1; return !remove; }); if (retained.length) pages[pageId] = retained; }
    if (removedHotspotCount) { hotspotManifest = { ...hotspotManifest, pages }; hotspotRevision += 1; }
    const result = { outcome: "deleted", activityId, indexRevision, hotspotRevision, removedHotspotCount, idempotent: false };
    deleteMutations.set(body.clientMutationId, { activityId, result }); return json(response, 200, result);
  }
  const nativeMatch = url.pathname.match(new RegExp(`^${contentRoot}/native-activity-(public|teacher)/([a-z0-9-]+)$`));
  if (nativeMatch) {
    const state = nativeDocuments.get(nativeMatch[2]); if (!state) return json(response, 404, { error: "builder_resource_not_found" });
    if (request.method === "GET") return json(response, 200, envelope(`native-activity-${nativeMatch[1]}`, nativeMatch[2], state[`${nativeMatch[1]}Revision`], state[`${nativeMatch[1]}Document`]));
    if (request.method === "PUT" && nativeMatch[1] === "public" && state.publicDocument.kind === "image") { const body = await requestJson(request); if (body.expectedRevision !== state.publicRevision) return json(response, 409, { error: "revision_conflict", currentRevision: state.publicRevision }); state.publicRevision += 1; state.publicDocument = structuredClone(body.document); return json(response, 200, { ...envelope("native-activity-public", nativeMatch[2], state.publicRevision, state.publicDocument), currentRevision: state.publicRevision, idempotent: false }); }
    return json(response, 405, { error: "method_not_allowed" });
  }
  const pairMatch = url.pathname.match(new RegExp(`^${nativeRoot}/activities/([a-z0-9-]+)/save$`));
  if (pairMatch && request.method === "POST") {
    if (!nativeIndex.activities.some((entry) => entry.activityId === pairMatch[1])) return json(response, 404, { error: "native_activity_not_found" });
    const state = nativeDocuments.get(pairMatch[1]); const body = await requestJson(request);
    if (body.expectedPublicRevision !== state.publicRevision || body.expectedTeacherRevision !== state.teacherRevision) return json(response, 409, { error: "revision_conflict", currentPublicRevision: state.publicRevision, currentTeacherRevision: state.teacherRevision });
    state.publicRevision += 1; state.teacherRevision += 1; state.publicDocument = structuredClone(body.publicDocument); state.teacherDocument = structuredClone(body.teacherDocument);
    return json(response, 200, { activityId: pairMatch[1], publicRevision: state.publicRevision, teacherRevision: state.teacherRevision, publicDocument: state.publicDocument, teacherDocument: state.teacherDocument, idempotent: false });
  }
  const prepareMatch = url.pathname.match(new RegExp(`^${nativeRoot}/activities/([a-z0-9-]+)/assets/prepare$`));
  if (prepareMatch && request.method === "POST") { if (!nativeIndex.activities.some((entry) => entry.activityId === prepareMatch[1])) return json(response, 404, { error: "native_activity_not_found" }); const body = await requestJson(request); const uploadId = `10000000-0000-4000-8000-${String(uploadSequence++).padStart(12, "0")}`; nativeUploads.set(uploadId, { activityId: prepareMatch[1], slot: body.assetSlot, name: body.name, type: body.type, purpose: body.purpose, bytes: null }); return json(response, 200, { uploadId, expiresIn: 900, authorization: { url: `${origin}/fixture-upload/${uploadId}`, headers: { "Content-Type": body.type }, expiresIn: 900 }, idempotent: false }); }
  if (url.pathname.startsWith("/fixture-upload/") && request.method === "PUT") { const upload = nativeUploads.get(url.pathname.split("/").at(-1)); upload.bytes = await requestBytes(request); response.writeHead(200); return response.end(); }
  const finalizeMatch = url.pathname.match(new RegExp(`^${nativeRoot}/activities/([a-z0-9-]+)/assets/finalize$`));
  if (finalizeMatch && request.method === "POST") {
    if (!nativeIndex.activities.some((entry) => entry.activityId === finalizeMatch[1])) return json(response, 404, { error: "native_activity_not_found" });
    const body = await requestJson(request); const upload = nativeUploads.get(body.uploadId); const checksumSha256 = createHash("sha256").update(upload.bytes).digest("hex"); const contentIdentity = `${upload.activityId}:${upload.purpose || "native-asset"}:${checksumSha256}`;
    let assetId = nativeAssetByContent.get(contentIdentity); let asset = assetId ? nativeAssets.get(assetId) : null;
    if (!asset) { assetId = `10000000-0000-4000-8000-${String(uploadSequence++).padStart(12, "0")}`; asset = { ...upload, checksumSha256 }; nativeAssets.set(assetId, asset); nativeAssetByContent.set(contentIdentity, assetId); }
    const dimensions = upload.type === "audio/mpeg" || upload.type === "video/mp4" || upload.type === "application/pdf"
      ? { width: null, height: null }
      : /^(?:choice-|words-)/.test(upload.name)
        ? await sharp(upload.bytes).metadata().then(({ width, height }) => ({ width, height }))
        : upload.name.startsWith("readable-")
          ? { width: 1000, height: 1800 }
          : { width: 1200, height: 800 };
    return json(response, 200, { reference: { assetId, checksumSha256, role: upload.purpose === "teacher-answer" ? "native_teacher_answer" : "activity_artwork", slot: asset.slot }, previewUrl: `${nativeRoot}/activities/${finalizeMatch[1]}/assets/${assetId}/preview`, metadata: { mimeType: upload.type, byteSize: upload.bytes.length, ...dimensions, ...(upload.type === "video/mp4" ? { durationMs: 5_840 } : {}) }, idempotent: false });
  }
  const assetPreviewMatch = url.pathname.match(new RegExp(`^${nativeRoot}/activities/([a-z0-9-]+)/assets/([0-9a-f-]+)/preview$`));
  if (assetPreviewMatch && request.method === "GET") { const asset = nativeAssets.get(assetPreviewMatch[2]); const bytes = asset?.bytes || Buffer.alloc(0); response.writeHead(200, { "Content-Type": asset?.type || "application/octet-stream", "Content-Length": bytes.length }); return response.end(bytes); }
  const canonicalMatch = url.pathname.match(new RegExp(`^${contentRoot}/open-response/([a-z0-9-]+)$`));
  if (canonicalMatch && request.method === "GET") { const activity = findStudentsBookImplementation(canonicalMatch[1]); if (!activity) return json(response, 404, { error: "builder_resource_not_found" }); return json(response, 200, envelope("open-response", canonicalMatch[1], 0, createUltimateB2HostedOpenResponseSeed(activity))); }
  if (url.pathname === builderPagesRoot && request.method === "GET") return json(response, 200, pagesEnvelope());
  if (url.pathname.startsWith("/builder/api/open-response-import/status/") && request.method === "GET") return json(response, 200, { activityId: url.pathname.split("/").at(-1), revision: 0, fingerprint: null, updatedAt: null });
  return staticResponse(url.pathname, response);
});

await new Promise((resolve) => server.listen(0, "127.0.0.1", resolve)); origin = `http://127.0.0.1:${server.address().port}`;
let browser;
try {
  browser = await chromium.launch(localPlaywrightLaunchOptions()); const context = await browser.newContext({ viewport: { width: 1440, height: 900 } });
  await context.addInitScript(() => {
    let fullscreenElement = null; let requestCount = 0; let exitCount = 0; let lastRequestedElement = null; let rejectNextRequest = false;
    let mediaPlayCount = 0; let mediaPauseCount = 0; let lastMediaSource = ""; const playingMedia = new WeakSet();
    const signal = () => document.dispatchEvent(new Event("fullscreenchange"));
    Object.defineProperty(document, "fullscreenElement", { configurable: true, get: () => fullscreenElement });
    Object.defineProperty(document, "fullscreenEnabled", { configurable: true, get: () => true });
    Object.defineProperty(HTMLElement.prototype, "requestFullscreen", { configurable: true, value: async function requestFullscreen() { requestCount += 1; lastRequestedElement = this; if (rejectNextRequest) { rejectNextRequest = false; throw new DOMException("Fullscreen request rejected", "NotAllowedError"); } fullscreenElement = this; signal(); } });
    Object.defineProperty(document, "exitFullscreen", { configurable: true, value: async () => { exitCount += 1; fullscreenElement = null; signal(); } });
    Object.defineProperty(HTMLMediaElement.prototype, "paused", { configurable: true, get() { return !playingMedia.has(this); } });
    Object.defineProperty(HTMLMediaElement.prototype, "play", { configurable: true, value() { mediaPlayCount += 1; lastMediaSource = this.currentSrc || this.src; playingMedia.add(this); this.dispatchEvent(new Event("play")); return Promise.resolve(); } });
    Object.defineProperty(HTMLMediaElement.prototype, "pause", { configurable: true, value() { mediaPauseCount += 1; playingMedia.delete(this); this.dispatchEvent(new Event("pause")); } });
    Object.defineProperty(globalThis, "__hhplmsFullscreenTest", { configurable: true, value: Object.freeze({
      externalExit() { fullscreenElement = null; signal(); },
      rejectNextRequest() { rejectNextRequest = true; },
      snapshot() { return { requestCount, exitCount, requestedTagName: lastRequestedElement?.tagName || null }; },
    }) });
    Object.defineProperty(globalThis, "__hhplmsMediaTest", { configurable: true, value: Object.freeze({ snapshot() { return { playCount: mediaPlayCount, pauseCount: mediaPauseCount, lastSource: lastMediaSource }; } }) });
  });
  await context.route("https://hhplms-viewer.netlify.app/**", async (route) => {
    const viewerRequest = route.request(); const url = new URL(viewerRequest.url());
    viewerRequests.push({ pathname: url.pathname, search: url.search, headers: viewerRequest.headers(), method: viewerRequest.method() });
    if (url.pathname === "/preview/native-activities/books/ultimate-b2/components/ultimate-b2-students-book/order") return route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify({ bookSlug: "ultimate-b2", componentSlug: "ultimate-b2-students-book", pages: orderPages() }) });
    if (url.pathname === "/preview/authorization/exchange" && viewerRequest.method() === "POST") {
      viewerExchangeIntents.push(viewerRequest.postDataJSON());
      return route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify({ token: previewToken, expiresAt: "2099-01-01T00:00:00.000Z" }) });
    }
    if (url.pathname === "/preview/content/books/ultimate-b2/components/ultimate-b2-students-book/hotspots") return route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify({ bookSlug: "ultimate-b2", componentSlug: "ultimate-b2-students-book", resource: "hotspots", schemaVersion: "1.0", revision: hotspotRevision, source: hotspotRevision ? "database" : "repository", document: hotspotManifest }) });
    if (url.pathname === "/preview/content/books/ultimate-b2/components/ultimate-b2-students-book/ui-controller") return route.fulfill({ status: 404, contentType: "application/json", body: "{}" });
    if (url.pathname === previewPagesRoot) return route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify(pagesEnvelope()) });
    const nativePreview = url.pathname.match(/^\/preview\/native-activities\/books\/ultimate-b2\/components\/ultimate-b2-students-book\/activities\/([a-z0-9-]+)\/(public|teacher)$/);
    if (nativePreview) {
      const state = nativeDocuments.get(nativePreview[1]); const audience = nativePreview[2];
      if (!state || !nativeIndex.activities.some((entry) => entry.activityId === nativePreview[1]) || (audience === "teacher" && !["multi-part", "image", "open-response", "single-choice", "complete-sentences", "listening", "oldschool-listening", "drag-drop", "mark-the-words"].includes(state.publicDocument.kind))) return route.fulfill({ status: 404, contentType: "application/json", body: "{}" });
      const document = state[`${audience}Document`]; const revision = state[`${audience}Revision`];
      return route.fulfill({ status: 200, contentType: "application/json", body: JSON.stringify({ bookSlug: "ultimate-b2", componentSlug: "ultimate-b2-students-book", activityId: nativePreview[1], kind: state.publicDocument.kind, audience, schemaVersion: "1.0", revision, document }) });
    }
    const nativeAsset = url.pathname.match(/^\/preview\/native-activities\/books\/ultimate-b2\/components\/ultimate-b2-students-book\/activities\/([a-z0-9-]+)\/assets\/([0-9a-f-]+)$/);
    if (nativeAsset) { const asset = nativeAssets.get(nativeAsset[2]); if (!asset || (asset.role !== "activity_font" && asset.activityId !== nativeAsset[1])) return route.fulfill({ status: 404, contentType: "application/json", body: "{}" }); return route.fulfill({ status: 200, contentType: asset.type || "application/octet-stream", body: asset.bytes }); }
    if (url.pathname.startsWith("/preview/")) return route.fulfill({ status: 404, contentType: "application/json", body: "{}" });
    const relative = url.pathname === "/" ? "index.html" : decodeURIComponent(url.pathname).replace(/^\/+/, "");
    let file = path.resolve(viewerRoot, relative); let details = file.startsWith(`${viewerRoot}${path.sep}`) ? await stat(file).catch(() => null) : null;
    if (!details?.isFile() && viewerRequest.resourceType() === "document") { file = path.join(viewerRoot, "index.html"); details = await stat(file).catch(() => null); }
    if (!details?.isFile()) return route.fulfill({ status: 404, body: "" });
    return route.fulfill({ status: 200, contentType: mime[path.extname(file).toLowerCase()] || "application/octet-stream", body: await readFile(file) });
  });
  const page = await context.newPage(); page.setDefaultTimeout(45_000);
  let autoOpenDraftPages = false;
  const draftFrameNavigations = createViewerFrameNavigationLifecycle({
    run: (frame, navigation) => openStudentsUnitOnePage(frame, navigation),
    onError: (error) => {
      if (/Frame was detached|Target page, context or browser has been closed/.test(error.message)) return;
      process.nextTick(() => { throw error; });
    },
  });
  page.on("framenavigated", (frame) => {
    if (!autoOpenDraftPages || frame === page.mainFrame()) return;
    const navigationUrl = frame.url();
    if (navigationUrl === "about:blank") { draftFrameNavigations.supersede(frame, navigationUrl); return; }
    const frameUrl = new URL(navigationUrl);
    if (frameUrl.origin !== "https://hhplms-viewer.netlify.app" || frameUrl.searchParams.get("view") !== "library") { draftFrameNavigations.supersede(frame, navigationUrl); return; }
    draftFrameNavigations.begin(frame, viewerFrameNavigationIdentity(navigationUrl));
  });
  await page.goto(`${origin}/#/books/ultimate-b2/components/ultimate-b2-students-book/activities`, { waitUntil: "domcontentloaded" });
  await page.getByRole("button", { name: "Add Activity" }).waitFor(); await page.locator(".native-or-editor").waitFor();
  const catalogWarning = page.getByRole("alert").filter({ hasText: "1 Students Book activity could not be loaded and needs repair." });
  await catalogWarning.waitFor(); await catalogWarning.getByText("ultimate-b2-sb-u1-p1-o88", { exact: true }).waitFor(); await catalogWarning.getByText(/Open Response · pair_missing/).waitFor();
  assert.equal(await page.getByRole("button", { name: /ultimate-b2-sb-u1-p1-o88/ }).count(), 0, "a quarantined activity is not selectable");
  assert.equal(await page.getByRole("button", { name: new RegExp(legacyActivityId) }).count(), 1, "valid activities remain selectable");
  const catalogRequestsBeforeRetry = requestedPaths.filter((pathname) => pathname === `${nativeRoot}/catalog`).length; catalogDiagnostics = [];
  await catalogWarning.getByRole("button", { name: "Retry" }).click(); await catalogWarning.waitFor({ state: "detached" });
  assert.ok(requestedPaths.filter((pathname) => pathname === `${nativeRoot}/catalog`).length > catalogRequestsBeforeRetry);
  assert.equal(await page.locator(".activity-builder-header .b2-hosted-review-banner").count(), 0); assert.equal(await page.locator(".activity-builder-header-actions").getByRole("button", { name: "Add Activity" }).count(), 1); assert.equal(await page.locator(".native-or-editor").getByLabel("Student instruction").count(), 0);
  const headerGeometry = await page.locator(".activity-builder-header").evaluate((header) => { const copy = header.firstElementChild.getBoundingClientRect(); const actions = header.querySelector(".activity-builder-header-actions").getBoundingClientRect(); const button = header.querySelector(".hosted-builder-action").getBoundingClientRect(); return { copyBottom: copy.bottom, actionsTop: actions.top, actionsWidth: actions.width, buttonWidth: button.width }; }); assert.ok(headerGeometry.actionsTop <= headerGeometry.copyBottom, JSON.stringify(headerGeometry)); assert.ok(headerGeometry.actionsWidth <= headerGeometry.buttonWidth + 1, JSON.stringify(headerGeometry));
  const search = page.getByPlaceholder("Search title, type, or ID"); await search.fill("does-not-exist"); await page.getByText("The selected activity is hidden by the current filters.", { exact: false }).waitFor(); assert.equal(await page.locator(".native-or-editor").count(), 1); await search.fill("");
  await page.getByLabel("Access").selectOption("native"); await page.locator(".activity-navigation-tree > section").waitFor(); assert.equal(await page.locator(".activity-navigation-tree > section").count(), 1); await page.getByLabel("Access").selectOption("all");
  const bodyOverflowBeforeExtras = await page.evaluate(() => document.body.style.overflow); await page.getByLabel("Add extras to Unit 1", { exact: true }).click(); await page.getByRole("button", { name: "Videos", exact: true }).click();
  let extrasDialog = page.getByRole("dialog", { name: "Unit 1 Extras" }); await extrasDialog.waitFor(); await extrasDialog.getByText("No Extra Videos in this Unit.", { exact: true }).waitFor(); await assertUnitExtrasModalLayout(extrasDialog, { expectInnerScroll: false });
  await extrasDialog.getByRole("button", { name: "Add Video" }).click(); await extrasDialog.getByRole("button", { name: "Add Video" }).click();
  let extraCards = extrasDialog.locator(".unit-extra-video-card"); assert.equal(await extraCards.count(), 2);
  for (const viewport of [{ width: 1180, height: 650 }, { width: 768, height: 700 }]) { await page.setViewportSize(viewport); await assertUnitExtrasModalLayout(extrasDialog, { expectInnerScroll: true }); } await page.setViewportSize({ width: 1440, height: 900 });
  await extraCards.nth(0).locator('input:not([type="file"])').fill("Optional subtitle video"); await extraCards.nth(1).locator('input:not([type="file"])').fill("Captioned managed video");
  await extraCards.nth(0).locator('input[type="file"][accept*="video/mp4"]').setInputFiles({ name: "optional-subtitle.mp4", mimeType: "video/mp4", buffer: videoMp4 }); await extrasDialog.getByText("MP4 validated. Save Unit Extras to attach it.", { exact: true }).waitFor();
  extraCards = extrasDialog.locator(".unit-extra-video-card"); await extraCards.nth(1).locator('input[type="file"][accept*="video/mp4"]').setInputFiles({ name: "captioned-managed.mp4", mimeType: "video/mp4", buffer: videoMp4 }); await extrasDialog.getByText("MP4 validated. Save Unit Extras to attach it.", { exact: true }).waitFor();
  extraCards = extrasDialog.locator(".unit-extra-video-card"); await extraCards.nth(1).locator('input[type="file"][accept*=".srt"]').setInputFiles({ name: "unit-extra.srt", mimeType: "application/x-subrip", buffer: Buffer.from(videoSrt) }); await extrasDialog.getByText("2 subtitle cues imported.", { exact: true }).waitFor();
  await extrasDialog.locator(".unit-extra-page-visibility input[type=checkbox]").first().check(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor();
  assert.deepEqual(unitExtraDocument.units[0].categories.videos.map((video) => [video.title, video.cues.length]), [["Optional subtitle video", 0], ["Captioned managed video", 2]]); assert.equal(unitExtraDocument.pages[0].extrasVisibility.videos, true);
  await extrasDialog.getByRole("button", { name: "Move Captioned managed video up" }).click(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor(); assert.deepEqual(unitExtraDocument.units[0].categories.videos.map((video) => video.title), ["Captioned managed video", "Optional subtitle video"]);
  extraCards = extrasDialog.locator(".unit-extra-video-card"); const replacedAssetId = unitExtraDocument.units[0].categories.videos[0].asset.assetId; await extraCards.nth(0).locator('input[type="file"][accept*="video/mp4"]').setInputFiles({ name: "captioned-managed-replacement.mp4", mimeType: "video/mp4", buffer: videoMp4 }); await extrasDialog.getByText("MP4 validated. Save Unit Extras to attach it.", { exact: true }).waitFor(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor(); assert.notEqual(unitExtraDocument.units[0].categories.videos[0].asset.assetId, replacedAssetId); assert.equal(unitExtraDocument.units[0].categories.videos[0].fileName, "captioned-managed-replacement.mp4");
  await extrasDialog.locator(".unit-extra-video-card").nth(1).getByRole("button", { name: "Delete Video" }).click(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor(); assert.deepEqual(unitExtraDocument.units[0].categories.videos.map((video) => video.title), ["Captioned managed video"]);
  await extrasDialog.getByRole("button", { name: "Close", exact: true }).click(); await extrasDialog.waitFor({ state: "detached" }); assert.equal(await page.evaluate(() => document.body.style.overflow), bodyOverflowBeforeExtras); await page.getByLabel("Add extras to Unit 1", { exact: true }).click(); await page.getByRole("button", { name: "Videos", exact: true }).click(); extrasDialog = page.getByRole("dialog", { name: "Unit 1 Extras" }); await extrasDialog.waitFor(); await extrasDialog.locator(".unit-extra-video-card").waitFor(); assert.equal(await extrasDialog.locator(".unit-extra-video-card").count(), 1); assert.equal(await extrasDialog.locator('.unit-extra-video-card input:not([type="file"])').inputValue(), "Captioned managed video"); await assertUnitExtrasModalLayout(extrasDialog, {}); await extrasDialog.getByRole("button", { name: "Close", exact: true }).click(); await extrasDialog.waitFor({ state: "detached" });
  await page.getByLabel("Add extras to Unit 1", { exact: true }).click(); await page.getByRole("button", { name: "Audio", exact: true }).click(); extrasDialog = page.getByRole("dialog", { name: "Unit 1 Extras" }); await extrasDialog.waitFor(); await extrasDialog.getByText("No Extra Audio in this Unit.", { exact: true }).waitFor(); await assertUnitExtrasModalLayout(extrasDialog, { expectInnerScroll: false });
  await extrasDialog.getByRole("button", { name: "Add Audio" }).click(); let audioCard = extrasDialog.locator(".unit-extra-audio-card"); assert.equal(await audioCard.count(), 1); await audioCard.locator('input:not([type="file"])').fill("Standalone unit audio"); await audioCard.locator('input[type="file"][accept*="audio/mpeg"]').setInputFiles({ name: "standalone-unit-audio.mp3", mimeType: "audio/mpeg", buffer: listeningMp3 }); await extrasDialog.getByText("MP3 validated. Save Unit Extras to attach it.", { exact: true }).waitFor(); await extrasDialog.locator(".unit-extra-page-visibility input[type=checkbox]").first().check(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor(); assert.equal(unitExtraDocument.units[0].categories.audios[0].title, "Standalone unit audio"); assert.equal(unitExtraDocument.units[0].categories.audios[0].asset.role, "unit_extra_audio"); assert.equal(unitExtraDocument.pages[0].extrasVisibility.audios, true);
  await extrasDialog.getByRole("button", { name: "Close", exact: true }).click(); await extrasDialog.waitFor({ state: "detached" }); await page.getByLabel("Add extras to Unit 1", { exact: true }).click(); await page.getByRole("button", { name: "Audio", exact: true }).click(); extrasDialog = page.getByRole("dialog", { name: "Unit 1 Extras" }); await extrasDialog.waitFor(); audioCard = extrasDialog.locator(".unit-extra-audio-card"); await audioCard.waitFor(); assert.equal(await audioCard.count(), 1); assert.equal(await audioCard.locator('input:not([type="file"])').inputValue(), "Standalone unit audio"); const originalAudioAssetId = unitExtraDocument.units[0].categories.audios[0].asset.assetId;
  await audioCard.locator('input[type="file"][accept*="audio/mpeg"]').setInputFiles({ name: "standalone-unit-audio-replacement.mp3", mimeType: "audio/mpeg", buffer: hotspotMp3(8) }); await extrasDialog.getByText("MP3 validated. Save Unit Extras to attach it.", { exact: true }).waitFor(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor(); assert.notEqual(unitExtraDocument.units[0].categories.audios[0].asset.assetId, originalAudioAssetId); assert.equal(unitExtraDocument.units[0].categories.audios[0].fileName, "standalone-unit-audio-replacement.mp3");
  await audioCard.getByRole("button", { name: "Remove MP3 assignment" }).click(); await audioCard.getByText("Required", { exact: true }).waitFor(); await audioCard.getByRole("button", { name: "Delete Audio" }).click(); await extrasDialog.getByRole("button", { name: "Save Unit Extras" }).click(); await extrasDialog.getByText("Unit Extras saved.", { exact: true }).waitFor(); assert.deepEqual(unitExtraDocument.units[0].categories.audios, []); await extrasDialog.getByRole("button", { name: "Close", exact: true }).click(); await extrasDialog.waitFor({ state: "detached" }); await page.getByLabel("Add extras to Unit 1", { exact: true }).click(); await page.getByRole("button", { name: "Audio", exact: true }).click(); extrasDialog = page.getByRole("dialog", { name: "Unit 1 Extras" }); await extrasDialog.waitFor(); await extrasDialog.getByText("No Extra Audio in this Unit.", { exact: true }).waitFor(); await extrasDialog.getByRole("button", { name: "Close", exact: true }).click();
  const firstUnitToggle = page.locator(".activity-navigation-tree .activity-tree-toggle").first(); assert.equal(await firstUnitToggle.getAttribute("aria-expanded"), "true"); await firstUnitToggle.click(); assert.equal(await firstUnitToggle.getAttribute("aria-expanded"), "false"); assert.equal(await page.locator(".native-or-editor").count(), 1); await firstUnitToggle.click();
  const activityLayout = page.locator(".b2-hosted-activity-layout"); const activitySidebar = page.locator(".activity-builder-sidebar"); const activityWorkspace = page.locator(".b2-hosted-activity-preview"); const navigationToggle = page.getByRole("button", { name: "Collapse activity navigation" });
  assert.equal(await activityLayout.getAttribute("data-navigation-expanded"), "true"); await page.waitForTimeout(250); const expandedWorkspaceWidth = (await activityWorkspace.boundingBox()).width; await activitySidebar.evaluate((element) => { element.scrollTop = Math.min(80, element.scrollHeight - element.clientHeight); }); const sidebarScrollTop = await activitySidebar.evaluate((element) => element.scrollTop); const expandedUnitState = await firstUnitToggle.getAttribute("aria-expanded");
  await activityWorkspace.focus(); await activitySidebar.hover(); await activityWorkspace.hover(); await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "false"); await page.waitForTimeout(250); const collapsedWorkspaceWidth = (await activityWorkspace.boundingBox()).width; assert.ok(collapsedWorkspaceWidth >= expandedWorkspaceWidth + 240, JSON.stringify({ expandedWorkspaceWidth, collapsedWorkspaceWidth })); assert.equal(await firstUnitToggle.getAttribute("aria-expanded"), expandedUnitState); assert.equal(await activitySidebar.evaluate((element) => element.scrollTop), sidebarScrollTop);
  const revealToggle = page.getByRole("button", { name: "Show activity navigation" }); await revealToggle.hover(); await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "true"); await page.waitForTimeout(250); assert.ok((await activityWorkspace.boundingBox()).width <= expandedWorkspaceWidth + 2);
  await navigationToggle.click(); assert.equal(await activityLayout.getAttribute("data-navigation-expanded"), "false"); assert.equal(await activityWorkspace.evaluate((element) => element === document.activeElement), true); await page.getByRole("button", { name: "Show activity navigation" }).focus(); assert.equal(await activityLayout.getAttribute("data-navigation-expanded"), "true"); assert.equal(await page.getByRole("button", { name: "Collapse activity navigation" }).evaluate((element) => element === document.activeElement), true); await activityWorkspace.hover(); await page.waitForTimeout(350); assert.equal(await activityLayout.getAttribute("data-navigation-expanded"), "true"); await activityWorkspace.focus(); await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "false");
  assert.equal(await page.getByRole("button", { name: "Review", exact: true }).count(), 1); assert.equal(await page.locator(".unified-builder-review-dialog iframe").count(), 1);
  const firstPageToggle = page.locator(".activity-tree-page .activity-tree-toggle").first(); if (await firstPageToggle.getAttribute("aria-expanded") === "false") await firstPageToggle.click();
  await page.getByRole("button", { name: new RegExp(legacyActivityId) }).click(); await page.getByRole("heading", { name: "Legacy checksum fixture" }).waitFor(); assert.equal(await page.getByText("builder_content_failed", { exact: true }).count(), 0); assert.equal(await page.getByLabel("Visible instruction").count(), 0); assert.equal(await page.getByRole("button", { name: "Save Draft" }).isDisabled(), true); await page.getByRole("tab", { name: "Local Preview" }).click(); assert.equal(await page.getByText("Deprecated native instruction must stay hidden.", { exact: true }).count(), 0); await page.getByRole("tab", { name: "Content" }).click(); await page.getByLabel("Activity title").fill("Legacy checksum fixture updated"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(legacyActivityId).publicDocument.metadata.visibleInstructionText, ""); assert.deepEqual(nativeDocuments.get(legacyActivityId).teacherDocument, legacyTeacherBeforeCleanup); await page.getByRole("tab", { name: "Layout" }).click(); await page.locator(".native-or-layers button").filter({ hasText: "Legacy background" }).click(); assert.equal(await page.getByLabel("Lock position and size").isChecked(), false);
  await page.getByRole("button", { name: "Add Activity" }).click(); const addDialog = page.getByRole("dialog", { name: "Add activity" }); await addDialog.waitFor(); assert.equal(await addDialog.evaluate((element) => element.contains(document.activeElement)), true); await page.keyboard.press("Escape"); await addDialog.waitFor({ state: "detached" }); assert.equal(await page.getByRole("button", { name: "Add Activity" }).evaluate((element) => element === document.activeElement), true);
  const screenshotRoot = path.resolve("test-results/hosted-builder-ui"); await mkdir(screenshotRoot, { recursive: true }); await page.waitForTimeout(250); await page.screenshot({ path: path.join(screenshotRoot, "activity-builder-1440.png"), fullPage: true });
  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /Open Response/ }).check(); await page.getByLabel(/Initial title/).fill("Browser native response"); await page.getByRole("button", { name: "Create activity" }).click();
  const openResponseId = "ultimate-b2-sb-u1-p1-o90"; await page.getByRole("heading", { name: "Browser native response" }).waitFor(); await page.getByText(openResponseId, { exact: true }).first().waitFor(); await page.getByText("Content needs attention", { exact: true }).first().waitFor(); await verifyReadableTextStartsOffAndBlocksIncompleteSave(page);
  assert.equal(await page.getByLabel("Visible instruction").count(), 0); await page.getByLabel("Activity title").fill("Persisted browser response");
  assert.equal(await page.getByRole("tab", { name: "Content" }).getAttribute("aria-selected"), "true"); await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("tabpanel", { name: "Local Preview" }).getByRole("heading", { name: "Persisted browser response", exact: true }).waitFor(); await page.getByRole("tab", { name: "Content" }).click(); assert.equal(await page.getByLabel("Activity title").inputValue(), "Persisted browser response"); await page.getByText("Unsaved changes", { exact: true }).waitFor();
  for (const viewport of [{ width: 1440, height: 900 }, { width: 1024, height: 768 }, { width: 640, height: 900 }]) await measureEditorDock(page, { context: `open-response-dirty-${viewport.width}`, viewport, screenshotPath: path.join(screenshotRoot, `activity-editor-dirty-${viewport.width}.png`) }); await page.setViewportSize({ width: 1440, height: 900 });
  await activitySidebar.hover(); await activityWorkspace.hover(); await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "false"); assert.equal(await page.getByLabel("Activity title").inputValue(), "Persisted browser response"); await page.getByRole("button", { name: "Show activity navigation" }).hover(); await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "true");
  await page.getByRole("button", { name: new RegExp(legacyActivityId) }).click(); await page.getByRole("dialog", { name: "Discard unsaved changes?" }).waitFor(); await page.getByRole("button", { name: "Keep editing" }).click(); assert.equal(await page.getByLabel("Activity title").inputValue(), "Persisted browser response");
  await page.getByRole("button", { name: "Review", exact: true }).click(); await page.getByRole("heading", { name: "Review · Saved Draft", exact: true }).waitFor(); await page.locator(".unified-builder-review-dialog iframe").waitFor(); assert.equal(await page.locator(".unified-builder-review-dialog iframe").count(), 1); assert.equal(await page.getByRole("button", { name: "Fullscreen", exact: true }).count(), 0); await page.getByText("Unsaved changes are not included in Review. Save them first.", { exact: true }).waitFor(); const unsavedViewer = page.frameLocator(".unified-builder-review-dialog iframe"); await unsavedViewer.locator(".teacher-offline-library").waitFor(); assert.equal(await unsavedViewer.getByText("Browser native response", { exact: true }).count(), 0); assert.equal(await unsavedViewer.getByText("Persisted browser response", { exact: true }).count(), 0); const firstReviewAuthorizationCount = authorizationIntents.length;
  const activityAuthorizationCount = authorizationIntents.length;
  const [activityPlayer] = await Promise.all([
    context.waitForEvent("page"),
    page.getByRole("link", { name: "Open Player", exact: true }).click(),
  ]);
  await activityPlayer.getByRole("heading", { name: "Player Review", exact: true }).waitFor();
  const activityPlayerUrl = new URL(activityPlayer.url());
  assert.equal(activityPlayerUrl.origin, origin);
  assert.equal(activityPlayerUrl.pathname, "/");
  assert.equal(activityPlayerUrl.search, "");
  assert.equal(activityPlayerUrl.hash, "#/books/ultimate-b2/components/ultimate-b2-students-book/review?view=library");
  assert.doesNotMatch(activityPlayer.url(), /previewAuthorization|token|secret/i);
  await waitForHostedViewerFrame(activityPlayer.locator(".hosted-builder-review-page iframe"));
  const activityPlayerFrameUrl = new URL(await activityPlayer.locator(".hosted-builder-review-page iframe").getAttribute("src"));
  assert.equal(activityPlayerFrameUrl.origin, "https://hhplms-viewer.netlify.app");
  assert.equal(activityPlayerFrameUrl.searchParams.get("view"), "library");
  assert.equal(activityPlayerFrameUrl.searchParams.has("activityId"), false);
  assert.equal(await activityPlayer.locator(".hosted-builder-review-page iframe").getAttribute("referrerpolicy"), "no-referrer");
  assert.equal(await activityPlayer.locator(".hosted-viewer-preview iframe").evaluate((iframe) => iframe.parentElement?.classList.contains("hosted-viewer-preview")), true);
  assert.ok(authorizationIntents.length > activityAuthorizationCount);
  const activityViewer = activityPlayer.frameLocator(".hosted-builder-review-page iframe");
  await activityViewer.locator(".teacher-offline-library").waitFor();
  assert.equal(await activityViewer.getByRole("button", { name: "Show Text", exact: true }).count(), 0);
  assert.equal(await activityViewer.getByText("Browser native response", { exact: true }).count(), 0);
  await activityViewer.getByRole("button", { name: "Minimize application", exact: true }).waitFor();
  await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).click();
  await activityPlayer.getByRole("button", { name: "Exit Fullscreen", exact: true }).waitFor();
  assert.equal(await activityPlayer.getByRole("button", { name: "Exit Fullscreen", exact: true }).getAttribute("aria-pressed"), "true");
  assert.deepEqual(await activityPlayer.evaluate(() => ({
    activeIframe: document.fullscreenElement === document.querySelector(".hosted-viewer-preview iframe"),
    activeWrapper: document.fullscreenElement === document.querySelector(".hosted-viewer-preview"),
    toolbarOutsideFullscreen: !document.fullscreenElement?.contains(document.querySelector(".hosted-viewer-preview > header")),
    statusOutsideFullscreen: !document.fullscreenElement?.contains(document.querySelector(".hosted-viewer-preview-state")),
    ...globalThis.__hhplmsFullscreenTest.snapshot(),
  })), { activeIframe: true, activeWrapper: false, toolbarOutsideFullscreen: true, statusOutsideFullscreen: true, requestCount: 1, exitCount: 0, requestedTagName: "IFRAME" });

  await activityPlayer.evaluate((message) => window.postMessage(message, window.origin), VIEWER_EXIT_FULLSCREEN_MESSAGE);
  await activityPlayer.waitForTimeout(50);
  assert.equal((await activityPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.snapshot())).exitCount, 0, "top-window sender must be ignored");
  await activityViewer.locator("body").evaluate(() => window.parent.postMessage("HHPLMS_VIEWER_EXIT_FULLSCREEN_WRONG", "*"));
  await activityPlayer.waitForTimeout(50);
  assert.equal((await activityPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.snapshot())).exitCount, 0, "wrong Viewer message must be ignored");

  await activityViewer.getByRole("button", { name: "Minimize application", exact: true }).click();
  await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).waitFor();
  assert.equal((await activityPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.snapshot())).exitCount, 1);
  assert.equal(await activityPlayer.evaluate(() => document.fullscreenElement), null);
  assert.equal(await activityPlayer.getByRole("heading", { name: "Player Review", exact: true }).count(), 1);

  await activityViewer.getByRole("button", { name: "Minimize application", exact: true }).click();
  await activityPlayer.waitForTimeout(50);
  assert.equal((await activityPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.snapshot())).exitCount, 1, "valid message outside iframe fullscreen must be ignored");

  await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).click();
  await activityPlayer.getByRole("button", { name: "Exit Fullscreen", exact: true }).waitFor();
  await activityPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.externalExit());
  await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).waitFor();
  assert.equal(await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).getAttribute("aria-pressed"), "false");
  assert.equal(await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).evaluate((element) => element === document.activeElement), true);
  await activityPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.rejectNextRequest());
  await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).click();
  await activityPlayer.getByText("Fullscreen could not be changed.", { exact: true }).waitFor();
  assert.equal(await activityPlayer.getByRole("button", { name: "Fullscreen", exact: true }).getAttribute("aria-pressed"), "false");
  assert.equal(await activityPlayer.locator(".hosted-viewer-preview iframe").count(), 1);
  await activityPlayer.close();
  await page.keyboard.press("Escape"); await page.locator(".unified-builder-review-dialog").waitFor({ state: "hidden" }); assert.equal(await page.locator(".unified-builder-review-dialog iframe").count(), 1); assert.equal(await page.getByRole("button", { name: "Review", exact: true }).evaluate((element) => element === document.activeElement), true); autoOpenDraftPages = true;
  await page.getByRole("button", { name: "Add Question", exact: true }).click(); const firstQuestionId = await page.locator(".native-or-question-workspace aside button").nth(1).locator("code").textContent(); await page.getByLabel("Prompt").fill("First prompt"); await page.getByLabel(/Private model answer/).fill("First private model answer");
  await page.getByRole("button", { name: "Add Question", exact: true }).click(); const secondQuestionId = await page.locator(".native-or-question-workspace aside button").nth(2).locator("code").textContent(); assert.notEqual(firstQuestionId, secondQuestionId); await page.getByLabel("Prompt").fill("Second prompt"); await page.getByLabel(/Private model answer/).fill("Second private model answer"); await page.getByRole("button", { name: "Move Up", exact: true }).click();
  await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await uploadReadableText(page, tallReadablePng, "readable-open-response.png", "Open Response readable passage"); assert.equal(await page.getByRole("button", { name: "Save Draft" }).isDisabled(), false); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const firstReadableAssetId = nativeDocuments.get(openResponseId).publicDocument.readableText.assetSlot; await uploadReadableText(page, replacementTallReadablePng, "readable-open-response-replacement.png", "Replacement readable passage"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.notEqual(nativeDocuments.get(openResponseId).publicDocument.readableText.assetSlot, firstReadableAssetId); await page.getByRole("button", { name: "Remove / Disable Readable Text" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(Object.hasOwn(nativeDocuments.get(openResponseId).publicDocument, "readableText"), false);
  for (const viewport of [{ width: 1440, height: 900 }, { width: 1024, height: 768 }, { width: 640, height: 900 }]) await measureEditorDock(page, { context: `open-response-clean-${viewport.width}`, viewport, screenshotPath: viewport.width === 1440 ? path.join(screenshotRoot, "activity-editor-clean-1440.png") : undefined }); await page.setViewportSize({ width: 1440, height: 900 });
  await page.getByRole("tab", { name: "Content" }).click(); assert.equal(await page.getByLabel("Activity title").inputValue(), "Persisted browser response"); assert.equal(await page.getByLabel("Visible instruction").count(), 0); assert.equal(nativeDocuments.get(openResponseId).publicDocument.metadata.visibleInstructionText, ""); assert.equal(await page.locator(".native-or-question-workspace aside button").nth(1).locator("code").textContent(), secondQuestionId);
  await page.getByRole("button", { name: /^Question 1/ }).click(); assert.equal(await page.getByLabel(/Private model answer/).inputValue(), "Second private model answer");
  await page.getByRole("tab", { name: "Layout" }).click();
  await page.getByLabel("Add Image").setInputFiles({ name: "fixture.png", mimeType: "image/png", buffer: onePixelPng });
  const artworkQuickControls = page.getByRole("group", { name: "Artwork quick controls" }); await artworkQuickControls.waitFor();
  const initialArtworkArea = await artworkQuickControls.locator('input[type="number"]').evaluateAll((inputs) => Object.fromEntries(inputs.slice(0, 4).map((input) => [input.getAttribute("aria-label").replace("Quick ", "").toLowerCase(), Number(input.value)])));
  assert.equal(Math.round(initialArtworkArea.width / initialArtworkArea.height * 100) / 100, 1.5); assert.equal(initialArtworkArea.x, Math.round((1024 - initialArtworkArea.width) / 2)); assert.equal(initialArtworkArea.y, Math.round((582 - initialArtworkArea.height) / 2));
  assert.equal(await page.locator(".native-or-artwork img").first().evaluate((image) => getComputedStyle(image).objectFit), "contain"); await artworkQuickControls.getByLabel("Quick Width").fill(String(initialArtworkArea.width + 40)); const widthOnlyArtworkArea = { width: Number(await artworkQuickControls.getByLabel("Quick Width").inputValue()), height: Number(await artworkQuickControls.getByLabel("Quick Height").inputValue()) }; assert.equal(widthOnlyArtworkArea.width, initialArtworkArea.width + 40); assert.equal(widthOnlyArtworkArea.height, initialArtworkArea.height); await artworkQuickControls.getByLabel("Quick Height").fill(String(initialArtworkArea.height + 30)); const heightOnlyArtworkArea = { width: Number(await artworkQuickControls.getByLabel("Quick Width").inputValue()), height: Number(await artworkQuickControls.getByLabel("Quick Height").inputValue()) }; assert.equal(heightOnlyArtworkArea.width, widthOnlyArtworkArea.width); assert.equal(heightOnlyArtworkArea.height, initialArtworkArea.height + 30); assert.equal(await page.getByRole("button", { name: /Resize Artwork from/ }).count(), 4); await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "false"); await page.waitForTimeout(250); const artworkCornerHandle = page.getByRole("button", { name: "Resize Artwork from bottom right" }); await artworkCornerHandle.scrollIntoViewIfNeeded(); await artworkCornerHandle.hover(); const artworkCornerBox = await artworkCornerHandle.boundingBox(); assert.ok(artworkCornerBox); const artworkCornerHit = await page.evaluate(({ x, y }) => { const target = document.elementFromPoint(x, y); return { tag: target?.tagName, className: target?.className, label: target?.getAttribute?.("aria-label") }; }, { x: artworkCornerBox.x + artworkCornerBox.width / 2, y: artworkCornerBox.y + artworkCornerBox.height / 2 }); console.log(`OPEN_RESPONSE_ARTWORK_POINTER_TARGET ${JSON.stringify({ artworkCornerBox, artworkCornerHit })}`); await page.mouse.down(); await page.mouse.move(artworkCornerBox.x + artworkCornerBox.width / 2 + 36, artworkCornerBox.y + artworkCornerBox.height / 2 + 24, { steps: 8 }); await page.mouse.up(); const cornerArtworkArea = { width: Number(await artworkQuickControls.getByLabel("Quick Width").inputValue()), height: Number(await artworkQuickControls.getByLabel("Quick Height").inputValue()) }; console.log(`OPEN_RESPONSE_ARTWORK_GEOMETRY ${JSON.stringify({ initialArtworkArea, widthOnlyArtworkArea, heightOnlyArtworkArea, cornerArtworkArea })}`); assert.ok(cornerArtworkArea.width > heightOnlyArtworkArea.width, JSON.stringify({ heightOnlyArtworkArea, cornerArtworkArea })); assert.ok(cornerArtworkArea.height > heightOnlyArtworkArea.height, JSON.stringify({ heightOnlyArtworkArea, cornerArtworkArea })); await artworkQuickControls.getByLabel("Quick Fit").selectOption("cover"); assert.equal(await page.locator(".native-or-artwork img").first().evaluate((image) => getComputedStyle(image).objectFit), "cover"); await artworkQuickControls.getByLabel("Quick Fit").selectOption("contain");
  await artworkQuickControls.getByLabel("Alt text").fill("A deterministic test diagram"); await artworkQuickControls.getByLabel("Quick X").fill("210"); await artworkQuickControls.getByLabel("Quick Y").fill("130"); await page.getByRole("button", { name: "Duplicate graphic" }).click(); await page.locator(".native-or-layers button").nth(1).waitFor();
  await page.getByLabel("Alt text").fill("Background diagram"); await page.getByLabel("Quick X").fill("0"); await page.getByLabel("Quick Y").fill("0"); await page.getByLabel("Quick Width").fill("1024"); await page.getByLabel("Quick Height").fill("582"); await page.getByRole("button", { name: "Send to Back" }).click(); await page.getByLabel("Lock position and size").check(); assert.equal(await page.getByLabel("Quick X").isDisabled(), true); await page.getByRole("button", { name: "First prompt" }).click(); await page.getByRole("group", { name: "Prompt quick controls" }).waitFor(); await page.locator(".native-or-layers button").filter({ hasText: "Background diagram" }).click(); assert.equal(await page.getByLabel("Alt text").inputValue(), "Background diagram"); await page.waitForTimeout(150); await page.screenshot({ path: path.join(screenshotRoot, "native-open-response-layout-1440.png"), fullPage: true }); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const canonicalOpenResponseReference = structuredClone(nativeDocuments.get(openResponseId).publicDocument.assets[0]); const savedOpenResponsePanels = nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.presentation.panels; assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.length, 1); assert.equal(savedOpenResponsePanels[0].images.length, 2); assert.equal(new Set(savedOpenResponsePanels[0].images.map((item) => item.assetSlot)).size, 1); assert.equal(savedOpenResponsePanels[0].images.find((item) => item.altText === "Background diagram").locked, true); assert.equal(savedOpenResponsePanels[0].images.find((item) => item.altText === "A deterministic test diagram").locked, false);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: "Response for question 1" }).click(); const responseQuickControls = page.getByRole("group", { name: "Response region quick controls" }); await responseQuickControls.waitFor(); assert.equal(await page.locator(".native-or-properties.studio-inspector").count(), 0); assert.equal(await responseQuickControls.getByLabel("Accessibility label").inputValue(), "Response for question 1"); await responseQuickControls.getByLabel("Quick Line count").fill("4"); await responseQuickControls.getByLabel("Quick Auto-fit minimum").fill("16"); const requestedSize = responseQuickControls.getByLabel("Requested answer size"); await requestedSize.fill(""); await requestedSize.blur(); await responseQuickControls.getByText(/previous value is still saved/).waitFor(); assert.equal(nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.questions[0].responseRegion.presentation.answerFontSizeMax, 22); await requestedSize.fill("28.5"); await requestedSize.press("Enter"); await responseQuickControls.getByText(/whole number/).waitFor(); await responseQuickControls.getByLabel("Answer sizing", { exact: true }).selectOption("authored"); await requestedSize.fill("100"); await requestedSize.press("Enter"); await responseQuickControls.getByText("Requested size committed at 100px.", { exact: true }).waitFor(); await responseQuickControls.getByLabel("Quick Answer align").selectOption("center"); await responseQuickControls.getByText(/Requested 100px .* rendered 100px|Requested and rendered at 100px/).waitFor();
  await exerciseAuthoredSizeSaveReload(page, () => nativeDocuments.get(openResponseId).publicDocument, orderPages);
  await responseQuickControls.getByLabel("Quick Padding X").fill("0"); await responseQuickControls.getByLabel("Quick Width").fill("400"); assert.equal(await responseQuickControls.getByLabel("Quick Line width").inputValue(), "400");
  await responseQuickControls.getByLabel("Quick Width").fill("500"); assert.equal(await responseQuickControls.getByLabel("Quick Line width").inputValue(), "500"); await responseQuickControls.getByLabel("Quick Line width").fill("320"); assert.equal(await responseQuickControls.getByLabel("Quick Width").inputValue(), "500"); await responseQuickControls.getByLabel("Quick Width").fill("600"); await page.waitForFunction(() => document.querySelector('[aria-label="Quick Width"]')?.value === "600" && document.querySelector('[aria-label="Quick Line width"]')?.value === "320");
  await responseQuickControls.getByLabel("Quick Width").fill("620"); assert.equal(await responseQuickControls.getByLabel("Quick Line width").inputValue(), "320"); const lineWidthControl = responseQuickControls.getByLabel("Quick Line width"); await lineWidthControl.fill("620"); await lineWidthControl.blur(); await page.waitForFunction(() => { const width = document.querySelector('[aria-label="Quick Width"]'); const lineWidth = document.querySelector('[aria-label="Quick Line width"]'); const frame = document.querySelector('[aria-label="Response region selected"]'); const stage = frame?.closest("[data-studio-stage]"); const renderedWidth = Number.parseFloat(frame?.style.width) * Number(stage?.dataset.surfaceWidth) / 100; return width?.value === "620" && lineWidth?.value === "620" && Math.abs(renderedWidth - 620) < .001; }); const linkedBeforeResize = { width: Number(await responseQuickControls.getByLabel("Quick Width").inputValue()), lineWidth: Number(await lineWidthControl.inputValue()) }; assert.deepEqual(linkedBeforeResize, { width: 620, lineWidth: 620 });
  const responseResizeHandle = page.getByRole("button", { name: "Resize Response region from bottom right" }); await page.evaluate(() => { document.addEventListener("pointerdown", (event) => { globalThis.__hhplmsAcceptancePointerDown = { target: event.target?.getAttribute?.("aria-label") || event.target?.className || event.target?.tagName || "unknown", x: event.clientX, y: event.clientY }; }, { once: true, capture: true }); }); await responseResizeHandle.hover(); await page.mouse.down(); const receivedResizePointer = await page.evaluate(() => { const value = globalThis.__hhplmsAcceptancePointerDown; delete globalThis.__hhplmsAcceptancePointerDown; return value; }); assert.equal(receivedResizePointer?.target, "Resize Response region from bottom right"); await page.mouse.move(receivedResizePointer.x + 32, receivedResizePointer.y + 8, { steps: 6 }); await page.mouse.up(); await page.waitForFunction(() => { const width = document.querySelector('[aria-label="Quick Width"]'); const lineWidth = document.querySelector('[aria-label="Quick Line width"]'); return width && lineWidth && width.value !== "620" && lineWidth.value === width.value; }); const linkedAfterResize = { width: Number(await responseQuickControls.getByLabel("Quick Width").inputValue()), lineWidth: Number(await lineWidthControl.inputValue()), paddingX: Number(await responseQuickControls.getByLabel("Quick Padding X").inputValue()) }; assert.notEqual(linkedAfterResize.width, linkedBeforeResize.width); assert.notEqual(linkedAfterResize.lineWidth, linkedBeforeResize.lineWidth); assert.equal(linkedAfterResize.lineWidth, linkedAfterResize.width - (2 * linkedAfterResize.paddingX));
  await page.waitForFunction(() => document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded === "false"); for (let index = 0; index < 10; index += 1) await page.getByRole("button", { name: "Zoom in" }).click(); const canvasViewport = page.locator(".studio-canvas-viewport"); await canvasViewport.scrollIntoViewIfNeeded(); await page.waitForFunction(() => { const viewport = document.querySelector(".studio-canvas-viewport"); const wrap = viewport?.querySelector(".studio-artboard-wrap"); const stage = viewport?.querySelector("[data-studio-stage]"); if (!viewport || !wrap || !stage || document.querySelector(".b2-hosted-activity-layout")?.dataset.navigationExpanded !== "false") return false; const viewportBox = viewport.getBoundingClientRect(); const stageBox = stage.getBoundingClientRect(); const signature = [viewportBox.left, viewportBox.top, viewportBox.width, viewportBox.height, stageBox.left, stageBox.top, stageBox.width, stageBox.height, viewport.scrollWidth, viewport.scrollHeight].map((value) => Math.round(value * 100) / 100).join(":"); const stable = globalThis.__openResponsePanGeometry === signature; globalThis.__openResponsePanGeometry = signature; if (stable) delete globalThis.__openResponsePanGeometry; return stable && viewportBox.top >= 0 && viewportBox.bottom <= innerHeight && viewportBox.left >= 0 && viewportBox.right <= innerWidth + 1 && wrap.style.width === "200%" && Number(stage.dataset.surfaceWidth) > 0 && Number(stage.dataset.surfaceHeight) > 0 && viewport.scrollWidth > viewport.clientWidth + 1 && viewport.scrollHeight > viewport.clientHeight + 1; }); const zoomedOverflow = await canvasViewport.evaluate((element) => ({ scrollWidth: element.scrollWidth, scrollHeight: element.scrollHeight, clientWidth: element.clientWidth, clientHeight: element.clientHeight })); assert.ok(zoomedOverflow.scrollWidth > zoomedOverflow.clientWidth + 1 && zoomedOverflow.scrollHeight > zoomedOverflow.clientHeight + 1, JSON.stringify(zoomedOverflow)); assert.equal(await page.getByRole("group", { name: "Response region selected" }).count(), 1); await canvasViewport.evaluate((element) => { element.scrollLeft = 120; element.scrollTop = 0; }); const geometryBeforePan = await responseQuickControls.locator("input").evaluateAll((inputs) => inputs.map((input) => input.value)); const scrollBeforePan = await canvasViewport.evaluate((element) => ({ left: element.scrollLeft, top: element.scrollTop })); const panProbe = await canvasViewport.evaluate((element) => { const box = element.getBoundingClientRect(); const stageBox = element.querySelector("[data-studio-stage]").getBoundingClientRect(); const visibleStage = { left: Math.max(box.left, stageBox.left, 0), top: Math.max(box.top, stageBox.top, 0), right: Math.min(box.right, stageBox.right, innerWidth), bottom: Math.min(box.bottom, stageBox.bottom, innerHeight) }; const point = { x: visibleStage.left + Math.min(8, (visibleStage.right - visibleStage.left) / 2), y: (visibleStage.top + visibleStage.bottom) / 2 }; const stack = document.elementsFromPoint(point.x, point.y); const target = stack[0]; const blocker = target?.closest("button,input,textarea,select,a,summary,[contenteditable='true']"); const describe = (node) => node ? { tag: node.tagName.toLowerCase(), className: typeof node.className === "string" ? node.className : "", ariaLabel: node.getAttribute("aria-label") } : null; return { start: target && element.contains(target) && !blocker && visibleStage.right > visibleStage.left && visibleStage.bottom > visibleStage.top ? point : null, viewport: { left: box.left, top: box.top, right: box.right, bottom: box.bottom }, stage: { left: stageBox.left, top: stageBox.top, right: stageBox.right, bottom: stageBox.bottom }, visibleStage, target: describe(target), blocker: describe(blocker), stack: stack.slice(0, 6).map(describe) }; }); assert.ok(panProbe.start, JSON.stringify(panProbe)); const panStart = panProbe.start; await canvasViewport.evaluate((element) => { element.addEventListener("pointerdown", () => { element.dataset.acceptanceMiddlePan = "received"; }, { once: true }); }); await page.mouse.move(panStart.x, panStart.y); await page.mouse.down({ button: "middle" }); await page.mouse.move(panStart.x - 100, panStart.y - 80); await page.mouse.up({ button: "middle" }); assert.equal(await canvasViewport.getAttribute("data-acceptance-middle-pan"), "received"); await canvasViewport.evaluate((element) => delete element.dataset.acceptanceMiddlePan); const scrollAfterPan = await canvasViewport.evaluate((element) => ({ left: element.scrollLeft, top: element.scrollTop })); assert.ok(scrollAfterPan.left > scrollBeforePan.left && scrollAfterPan.top > scrollBeforePan.top, JSON.stringify({ scrollBeforePan, scrollAfterPan })); console.log(`OPEN_RESPONSE_ZOOM_PAN ${JSON.stringify({ zoomedOverflow, panStart, scrollBeforePan, scrollAfterPan })}`); assert.deepEqual(await responseQuickControls.locator("input").evaluateAll((inputs) => inputs.map((input) => input.value)), geometryBeforePan); assert.equal(await page.getByRole("group", { name: "Response region selected" }).count(), 1); await page.mouse.move(panStart.x + 120, panStart.y + 120); assert.deepEqual(await canvasViewport.evaluate((element) => ({ left: element.scrollLeft, top: element.scrollTop })), scrollAfterPan);
  const assertOpenResponseLayoutGeometry = async (width, height) => {
    await page.setViewportSize({ width, height }); if (width !== 1440) await page.waitForTimeout(250); await page.getByRole("button", { name: "Fit", exact: true }).click();
    const geometry = await page.evaluate(() => {
      const viewport = document.querySelector(".studio-open-response .studio-canvas-viewport"); const wrapper = document.querySelector(".studio-open-response .studio-artboard-wrap"); const stage = document.querySelector(".studio-open-response .studio-artboard"); const toolbar = document.querySelector(".studio-open-response .studio-canvas-toolbar");
      const numeric = (value) => Number.parseFloat(value) || 0;
      const rect = (element) => { const box = element.getBoundingClientRect(); return Object.fromEntries(["left", "top", "right", "bottom", "width", "height"].map((key) => [key, Math.round(box[key] * 1000) / 1000])); };
      const metrics = (element, properties) => { const style = getComputedStyle(element); return { rect: rect(element), clientWidth: element.clientWidth, clientHeight: element.clientHeight, scrollWidth: element.scrollWidth, scrollHeight: element.scrollHeight, offsetWidth: element.offsetWidth, offsetHeight: element.offsetHeight, style: Object.fromEntries(properties.map((property) => [property, style.getPropertyValue(property)])) }; };
      const viewportBox = viewport.getBoundingClientRect(); const stageBox = stage.getBoundingClientRect(); const toolbarBox = toolbar.getBoundingClientRect(); const viewportStyle = getComputedStyle(viewport);
      const viewportProperties = ["box-sizing", "padding-left", "padding-right", "padding-top", "padding-bottom", "border-left-width", "border-right-width", "border-top-width", "border-bottom-width", "overflow-x", "overflow-y", "scrollbar-width"];
      const wrapperProperties = ["box-sizing", "width", "min-width", "max-width", "padding-left", "padding-right", "padding-top", "padding-bottom", "margin-left", "margin-right", "margin-top", "margin-bottom", "transition-property", "transition-duration", "transition-timing-function"];
      const usableBounds = { left: viewportBox.left + numeric(viewportStyle.borderLeftWidth) + numeric(viewportStyle.paddingLeft), right: viewportBox.right - numeric(viewportStyle.borderRightWidth) - numeric(viewportStyle.paddingRight), top: viewportBox.top + numeric(viewportStyle.borderTopWidth) + numeric(viewportStyle.paddingTop), bottom: viewportBox.bottom - numeric(viewportStyle.borderBottomWidth) - numeric(viewportStyle.paddingBottom) };
      const descendantExtents = [...viewport.querySelectorAll("*")].map((element) => { const box = element.getBoundingClientRect(); return { element: `${element.tagName.toLowerCase()}.${[...element.classList].join(".")}`, rect: rect(element), rightExtent: Math.round((box.right - usableBounds.right) * 1000) / 1000, bottomExtent: Math.round((box.bottom - usableBounds.bottom) * 1000) / 1000 }; }).filter((entry) => entry.rect.width || entry.rect.height);
      return { bodyWidth: document.body.scrollWidth, rootWidth: document.documentElement.scrollWidth, innerWidth, inspectorCount: document.querySelectorAll(".studio-open-response .native-or-properties.studio-inspector").length, scrollLeft: viewport.scrollLeft, scrollTop: viewport.scrollTop, scrollWidth: viewport.scrollWidth, scrollHeight: viewport.scrollHeight, clientWidth: viewport.clientWidth, clientHeight: viewport.clientHeight, stageInside: stageBox.left >= usableBounds.left - 1 && stageBox.right <= usableBounds.right + 1 && stageBox.top >= usableBounds.top - 1 && stageBox.bottom <= usableBounds.bottom + 1, toolbarInside: toolbarBox.left >= 0 && toolbarBox.right <= innerWidth + 1, scrollbarWidth: getComputedStyle(viewport, "::-webkit-scrollbar").width, viewport: metrics(viewport, viewportProperties), wrapper: metrics(wrapper, wrapperProperties), stage: { ...metrics(stage, ["box-sizing", "width", "height", "aspect-ratio", "border-left-width", "border-right-width", "border-top-width", "border-bottom-width"]), actualAspectRatio: Math.round(stageBox.width / stageBox.height * 1_000_000) / 1_000_000 }, usableBounds: Object.fromEntries(Object.entries(usableBounds).map(([key, value]) => [key, Math.round(value * 1000) / 1000])), fitMeasurement: { observedClientWidth: viewport.clientWidth, measuredContentWidth: viewport.clientWidth - numeric(viewportStyle.paddingLeft) - numeric(viewportStyle.paddingRight), assignedHeight: viewport.style.height, zoom: Number.parseFloat(wrapper.style.width) / 100 }, rightExtentLeaders: descendantExtents.sort((left, right) => right.rightExtent - left.rightExtent).slice(0, 6), bottomExtentLeaders: descendantExtents.sort((left, right) => right.bottomExtent - left.bottomExtent).slice(0, 6) };
    });
    console.log(`OPEN_RESPONSE_FIT_GEOMETRY ${width}x${height} ${JSON.stringify(geometry)}`);
    assert.equal(geometry.bodyWidth, width, JSON.stringify(geometry)); assert.equal(geometry.rootWidth, width, JSON.stringify(geometry)); assert.equal(geometry.inspectorCount, 0); assert.equal(geometry.scrollLeft, 0); assert.equal(geometry.scrollTop, 0); assert.ok(geometry.scrollWidth <= geometry.clientWidth + 1, JSON.stringify(geometry)); assert.ok(geometry.scrollHeight <= geometry.clientHeight + 1, JSON.stringify(geometry)); assert.ok(Math.abs(geometry.wrapper.rect.width - geometry.fitMeasurement.measuredContentWidth) <= 1, JSON.stringify(geometry)); assert.ok(Math.abs(geometry.stage.actualAspectRatio - (1024 / 582)) <= .001, JSON.stringify(geometry)); assert.equal(geometry.stageInside, true, JSON.stringify(geometry)); assert.equal(geometry.toolbarInside, true, JSON.stringify(geometry)); assert.equal(geometry.scrollbarWidth, "0px");
    await page.screenshot({ path: path.join(screenshotRoot, `native-open-response-toolbar-${width}.png`), fullPage: true });
  };
  for (const size of [[1440, 900], [1024, 768], [760, 700]]) await assertOpenResponseLayoutGeometry(...size); await page.setViewportSize({ width: 1440, height: 900 });
  const openResponseStage = page.locator(".studio-artboard.native-or-surface"); const stageBox = await openResponseStage.boundingBox(); assert.ok(stageBox); const clickEmptyStage = async () => { await canvasViewport.scrollIntoViewIfNeeded(); const stage = await openResponseStage.boundingBox(); const viewport = await canvasViewport.boundingBox(); assert.ok(stage && viewport); const point = { x: stage.x + 8, y: (Math.max(stage.y, viewport.y) + Math.min(stage.y + stage.height, viewport.y + viewport.height)) / 2 }; assert.equal(await page.evaluate(({ x, y }) => document.elementFromPoint(x, y)?.classList.contains("native-or-surface"), point), true, JSON.stringify(point)); await page.mouse.click(point.x, point.y); }; await clickEmptyStage(); assert.equal(await page.getByRole("group", { name: "Response region selected" }).count(), 0); assert.equal(await page.getByText("Select an object for quick controls", { exact: true }).count(), 1); await page.locator(".native-or-layers button").filter({ hasText: "A deterministic test diagram" }).click(); await page.getByRole("group", { name: "Artwork quick controls" }).waitFor(); await clickEmptyStage(); assert.equal(await page.getByRole("group", { name: "Artwork selected" }).count(), 0); await page.getByRole("button", { name: "First prompt" }).click(); await page.getByRole("group", { name: "Prompt quick controls" }).waitFor(); await clickEmptyStage(); assert.equal(await page.getByRole("group", { name: "Prompt selected" }).count(), 0); await page.getByRole("button", { name: "Response for question 1" }).click(); await page.getByRole("group", { name: "Response region quick controls" }).waitFor();
  const authoringResponseRegion = page.locator('.studio-open-response .native-or-surface[data-native-or-presentation="authoring"] .native-or-response.is-selected'); await authoringResponseRegion.waitFor(); const authoringResponseStyle = await authoringResponseRegion.evaluate((element) => { const style = getComputedStyle(element); const box = element.getBoundingClientRect(); return { width: box.width, height: box.height, backgroundColor: style.backgroundColor, borderTopWidth: style.borderTopWidth, borderTopColor: style.borderTopColor, boxShadow: style.boxShadow, outlineStyle: style.outlineStyle }; }); assert.ok(authoringResponseStyle.width > 0 && authoringResponseStyle.height > 0, JSON.stringify(authoringResponseStyle)); assert.notEqual(authoringResponseStyle.borderTopWidth, "0px"); assert.notEqual(authoringResponseStyle.borderTopColor, "rgba(0, 0, 0, 0)"); assert.ok(authoringResponseStyle.boxShadow !== "none" || authoringResponseStyle.outlineStyle !== "none", JSON.stringify(authoringResponseStyle));
  await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const savedResponseWidth = nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.questions.find((question) => question.id === firstQuestionId).responseRegion.area.width; const savedLineWidth = nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.questions.find((question) => question.id === firstQuestionId).responseRegion.presentation.lineWidth; assert.equal(savedResponseWidth, savedLineWidth);
  await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: "Add Panel" }).click();
  await page.getByLabel("Add question prompt").selectOption(firstQuestionId); await page.getByLabel("Add answer box").selectOption(firstQuestionId); await page.getByLabel("Add answer box").selectOption(secondQuestionId); await page.getByLabel("Add Background").setInputFiles({ name: "panel-two.png", mimeType: "image/png", buffer: secondPixelPng });
  await page.getByRole("button", { name: /^Panel 1 / }).click(); await page.getByRole("button", { name: "Remove Question 1 from answer boxes", exact: true }).click(); assert.equal(await page.getByRole("button", { name: "Remove Question 1 from prompts", exact: true }).count(), 1); assert.equal(await page.getByRole("button", { name: "Remove Question 1 from answer boxes", exact: true }).count(), 0);
  await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor();
  let panelized = nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.presentation.panels; assert.equal(panelized.length, 2); assert.deepEqual(panelized.map((entry) => [entry.promptQuestionIds, entry.responseQuestionIds]), [[[secondQuestionId, firstQuestionId], [firstQuestionId]], [[firstQuestionId], [secondQuestionId, firstQuestionId]]]); assert.deepEqual(panelized.map((entry) => entry.images.length), [2, 1]); assert.deepEqual(nativeDocuments.get(openResponseId).teacherDocument.parts[0].solution.modelAnswers.map((entry) => entry.questionId), [secondQuestionId, firstQuestionId]); assert.doesNotMatch(JSON.stringify(nativeDocuments.get(openResponseId).publicDocument), /private model answer/i);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); const sharedPanelOneResponse = page.getByLabel("Response for question 1"); await sharedPanelOneResponse.fill("One canonical response"); assert.equal(await page.getByLabel("Response for question 2").count(), 0); assert.equal(await page.getByText("Second prompt", { exact: true }).count(), 1); await page.getByRole("button", { name: "Next", exact: true }).click(); const sharedPanelTwoResponse = page.getByLabel("Response for question 1"); assert.equal(await sharedPanelTwoResponse.inputValue(), "One canonical response"); const panelTwoOnlyResponse = page.getByLabel("Response for question 2"); await panelTwoOnlyResponse.fill("Box-only response"); assert.equal(await page.getByText("Second prompt", { exact: true }).count(), 0); await page.getByRole("button", { name: "Previous", exact: true }).click(); assert.equal(await sharedPanelOneResponse.inputValue(), "One canonical response");
  await page.getByRole("button", { name: "Teacher Preview" }).click(); const revealPanelOne = page.getByRole("button", { name: /Reveal model answer for Response for question 1/ }); await revealPanelOne.evaluate((button) => button.click()); await page.getByText("First private model answer", { exact: true }).waitFor(); await page.getByRole("button", { name: "Next", exact: true }).click(); const revealPanelTwo = page.getByRole("button", { name: /Reveal model answer for Response for question 2/ }); await revealPanelTwo.evaluate((button) => button.click()); await page.getByText("Second private model answer", { exact: true }).waitFor();
  await page.getByRole("tab", { name: "Content" }).click(); await page.locator(".native-or-question-workspace aside button").filter({ hasText: "First prompt" }).click(); assert.equal(await page.getByLabel(/Private model answer/).inputValue(), "First private model answer"); await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: /^Panel 1 / }).click(); await page.getByLabel("Add answer box").selectOption(secondQuestionId); await page.getByRole("button", { name: /^Panel 2 / }).click(); page.once("dialog", (dialog) => dialog.accept()); await page.getByRole("button", { name: "Delete Panel" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); panelized = nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.presentation.panels; assert.equal(panelized.length, 1); assert.deepEqual(panelized[0].promptQuestionIds, [secondQuestionId, firstQuestionId]); assert.deepEqual(panelized[0].responseQuestionIds, [secondQuestionId, firstQuestionId]); assert.equal(nativeDocuments.get(openResponseId).teacherDocument.parts[0].solution.modelAnswers.find((entry) => entry.questionId === firstQuestionId).text, "First private model answer");
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: "Response for question 1" }).click(); assert.equal(Number(await page.getByLabel("Quick Width").inputValue()), savedResponseWidth); assert.equal(Number(await page.getByLabel("Quick Line width").inputValue()), savedLineWidth); await page.locator(".native-or-layers button").filter({ hasText: "Background diagram" }).click(); assert.equal(await page.getByLabel("Lock position and size").isChecked(), true); await page.getByLabel("Lock position and size").uncheck(); await page.getByLabel("Quick Width").fill("800"); await page.getByLabel("Quick X").fill("220"); assert.equal(await page.getByLabel("Quick X").inputValue(), "220"); await page.getByLabel("Lock position and size").check(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor();
  await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); const secondResponse = page.getByLabel("Response for question 2"); const firstResponse = page.getByLabel("Response for question 1"); await secondResponse.fill("Student line one\nStudent line two"); await firstResponse.fill("Another local response"); assert.equal(await page.getByText("Draft saved.", { exact: true }).count(), 1); assert.equal(await page.getByText("Second private model answer", { exact: true }).count(), 0); const studentStaticSurface = page.locator(".native-or-preview .native-or-surface:visible"); assert.equal(await studentStaticSurface.locator("button.native-or-artwork,button.native-or-prompt").count(), 0); await page.getByRole("button", { name: "Teacher Preview" }).click(); assert.equal(await page.getByText("Second private model answer", { exact: true }).count(), 0); const teacherStaticSurface = page.locator(".native-or-preview .native-or-surface:visible"); const staticArtwork = teacherStaticSurface.locator(".native-or-artwork").first(); const staticPrompt = teacherStaticSurface.locator(".native-or-prompt").first(); assert.deepEqual(await staticArtwork.evaluate((element) => ({ tag: element.tagName, disabled: element.hasAttribute("disabled"), opacity: getComputedStyle(element).opacity, cursor: getComputedStyle(element).cursor, pointerEvents: getComputedStyle(element).pointerEvents })), { tag: "DIV", disabled: false, opacity: "1", cursor: "default", pointerEvents: "none" }); assert.deepEqual(await staticPrompt.evaluate((element) => ({ tag: element.tagName, disabled: element.hasAttribute("disabled"), opacity: getComputedStyle(element).opacity, cursor: getComputedStyle(element).cursor, pointerEvents: getComputedStyle(element).pointerEvents })), { tag: "DIV", disabled: false, opacity: "1", cursor: "default", pointerEvents: "none" }); const revealSecond = page.getByRole("button", { name: /Reveal model answer for Response for question 2/ }); assert.equal(await revealSecond.evaluate((button) => getComputedStyle(button).cursor), "pointer"); await revealSecond.evaluate((button) => button.click()); assert.equal(await page.getByRole("button", { name: /Hide model answer for Response for question 2/ }).getAttribute("aria-pressed"), "true"); assert.equal(await page.getByText("Second private model answer", { exact: true }).count(), 1); assert.equal(await page.getByText("First private model answer", { exact: true }).count(), 0); await page.getByRole("button", { name: "Student Preview" }).click(); assert.equal(await secondResponse.inputValue(), "Student line one\nStudent line two"); assert.equal(await page.getByText("Second private model answer", { exact: true }).count(), 0);
  const runtimeOpenResponseSurface = page.locator('.native-or-preview .native-or-surface[data-native-or-presentation="runtime"]:visible'); await runtimeOpenResponseSurface.waitFor(); const runtimeResponseRegion = runtimeOpenResponseSurface.locator(".native-or-response").first(); const runtimeResponseStyle = await runtimeResponseRegion.evaluate((element) => { const style = getComputedStyle(element); const box = element.getBoundingClientRect(); const line = element.querySelector(".native-or-line"); const lineStyle = getComputedStyle(line); const lineBox = line.getBoundingClientRect(); return { width: box.width, height: box.height, backgroundColor: style.backgroundColor, borderTopWidth: style.borderTopWidth, borderTopColor: style.borderTopColor, borderRadius: style.borderRadius, lineWidth: lineBox.width, lineBorderTopWidth: lineStyle.borderTopWidth, lineBorderTopColor: lineStyle.borderTopColor }; }); assert.ok(runtimeResponseStyle.width > 0 && runtimeResponseStyle.height > 0 && runtimeResponseStyle.lineWidth > 0, JSON.stringify(runtimeResponseStyle)); assert.equal(runtimeResponseStyle.backgroundColor, "rgba(0, 0, 0, 0)"); assert.equal(runtimeResponseStyle.borderTopColor, "rgba(0, 0, 0, 0)"); assert.equal(runtimeResponseStyle.borderRadius, "0px"); assert.notEqual(runtimeResponseStyle.lineBorderTopWidth, "0px"); assert.notEqual(runtimeResponseStyle.lineBorderTopColor, "rgba(0, 0, 0, 0)");
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Local Preview" }).click(); assert.equal(await page.getByLabel("Response for question 2").inputValue(), ""); await page.getByRole("tab", { name: "Content" }).click(); await page.getByRole("button", { name: /^Question 1/ }).click(); page.once("dialog", (dialog) => dialog.accept()); await page.getByRole("button", { name: "Delete Question" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(openResponseId).teacherDocument.parts[0].solution.modelAnswers.some((item) => item.questionId === secondQuestionId), false);
  await page.getByRole("tab", { name: "Layout" }).click(); await page.locator(".native-or-layers button").filter({ hasText: "Background diagram" }).click(); page.once("dialog", (dialog) => dialog.accept()); await page.getByRole("button", { name: "Remove graphic" }).click(); assert.equal(await page.locator(".native-or-artwork img").count(), 1); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.length, 1);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); await page.locator(".native-or-layers button").filter({ hasText: "A deterministic test diagram" }).click(); page.once("dialog", (dialog) => dialog.accept()); await page.getByRole("button", { name: "Remove graphic" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.length, 0);
  await page.getByLabel("Add Image").setInputFiles({ name: "fixture.png", mimeType: "image/png", buffer: onePixelPng }); await page.getByRole("heading", { name: "Artwork" }).waitFor(); await page.getByLabel("Alt text").fill("Reintroduced diagram"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.deepEqual(nativeDocuments.get(openResponseId).publicDocument.assets[0], canonicalOpenResponseReference);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); await page.locator(".native-or-artwork img").waitFor(); await page.getByLabel("Add Image").setInputFiles({ name: "fixture.png", mimeType: "image/png", buffer: onePixelPng }); await page.locator(".native-or-layers button").nth(1).waitFor(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const reloadedPanel = nativeDocuments.get(openResponseId).publicDocument.parts[0].interaction.presentation.panels[0]; assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.length, 1); assert.equal(reloadedPanel.images.length, 2); assert.equal(new Set(reloadedPanel.images.map((item) => item.assetSlot)).size, 1);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); assert.equal(await page.locator(".native-or-artwork img").count(), 2); await uploadReadableText(page, tallReadablePng, "readable-open-response.png", "Open Response readable passage"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor();
  await uploadVideoCompanion(page); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor();
  await page.locator('.native-video-editor input[type="file"][accept*="application/pdf"]').setInputFiles({ name: "classroom-worksheet.pdf", mimeType: "application/pdf", buffer: worksheetPdf }); await page.getByText(/classroom-worksheet\.pdf/).waitFor(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.worksheet.fileName, "classroom-worksheet.pdf"); assert.equal([...nativeUploads.values()].some((upload) => upload.type === "application/pdf" && upload.purpose === "video-worksheet"), true);
  assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.fileName, "classroom-companion.mp4"); assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.cues.length, 2); assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.durationMs, 5_840); assert.doesNotMatch(JSON.stringify(nativeDocuments.get(openResponseId).publicDocument.video), /https?:\/\/|teacher|answer/i);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Video", exact: true }).click(); await page.getByText("Video companion complete", { exact: true }).waitFor(); assert.equal(await page.getByRole("switch", { name: "Video" }).getAttribute("aria-checked"), "true"); await page.screenshot({ path: path.join(screenshotRoot, "native-open-response-video-1440.png"), fullPage: true });
  const videoSlotBeforeWorksheetChanges = nativeDocuments.get(openResponseId).publicDocument.video.assetSlot; const cuesBeforeWorksheetChanges = structuredClone(nativeDocuments.get(openResponseId).publicDocument.video.cues); const firstWorksheetSlot = nativeDocuments.get(openResponseId).publicDocument.video.worksheet.assetSlot; const firstWorksheetAssetId = nativeDocuments.get(openResponseId).publicDocument.assets.find((entry) => entry.slot === firstWorksheetSlot).assetId;
  await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); const worksheetAction = page.getByRole("button", { name: "Video Worksheet" }); await worksheetAction.waitFor(); assert.equal(await worksheetAction.count(), 1, "worksheet action is visible once before opening Video"); const downloadPromise = page.waitForEvent("download"); await worksheetAction.click(); const worksheetDownload = await downloadPromise; assert.equal(worksheetDownload.suggestedFilename(), "classroom-worksheet.pdf"); const worksheetHref = worksheetDownload.url(); assert.match(worksheetHref, new RegExp(firstWorksheetAssetId)); await page.getByRole("button", { name: "Video", exact: true }).click(); await page.screenshot({ path: path.join(screenshotRoot, "native-open-response-local-preview-1440.png"), fullPage: true }); assert.equal(await page.locator(".native-video-player-shell").getByRole("button", { name: "Video Worksheet" }).count(), 0, "worksheet action is absent from the player"); assert.equal(await page.getByRole("button", { name: "Video Worksheet" }).count(), 1, "worksheet action remains singular in navigation"); const subtitlesControl = page.getByRole("button", { name: "Turn subtitles off" }); assert.equal(await subtitlesControl.getAttribute("aria-pressed"), "true"); assert.equal(await subtitlesControl.locator("svg").count(), 1); await subtitlesControl.focus(); await subtitlesControl.press("Enter"); assert.equal(await page.getByRole("button", { name: "Turn subtitles on" }).getAttribute("aria-pressed"), "false"); const worksheetResponse = await page.request.get(new URL(worksheetHref, origin).href); assert.equal(worksheetResponse.status(), 200); assert.equal(worksheetResponse.headers()["content-type"], "application/pdf"); assert.deepEqual(Buffer.from(await worksheetResponse.body()), worksheetPdf);
  await page.getByRole("tab", { name: "Video", exact: true }).click(); await page.locator('.native-video-editor input[type="file"][accept*="image/png"]').setInputFiles({ name: "replacement-worksheet.png", mimeType: "image/png", buffer: landscapeChoicePng }); await page.getByText(/replacement-worksheet\.pdf/).waitFor(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const convertedWorksheetUpload = [...nativeUploads.values()].find((upload) => upload.name === "replacement-worksheet.pdf" && upload.purpose === "video-worksheet"); assert.ok(convertedWorksheetUpload); assert.equal(convertedWorksheetUpload.type, "application/pdf"); assert.doesNotThrow(() => inspectManagedPdf(convertedWorksheetUpload.bytes)); assert.match(convertedWorksheetUpload.bytes.toString("latin1"), /\/MediaBox \[0 0 841\.89 595\.28\]/); const replacementWorksheetSlot = nativeDocuments.get(openResponseId).publicDocument.video.worksheet.assetSlot; assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.worksheet.fileName, "replacement-worksheet.pdf"); assert.notEqual(replacementWorksheetSlot, firstWorksheetSlot); assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.some((entry) => entry.slot === firstWorksheetSlot), false); assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.assetSlot, videoSlotBeforeWorksheetChanges); assert.deepEqual(nativeDocuments.get(openResponseId).publicDocument.video.cues, cuesBeforeWorksheetChanges);
  await page.getByRole("button", { name: "Remove PDF" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(Object.hasOwn(nativeDocuments.get(openResponseId).publicDocument.video, "worksheet"), false); assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.some((entry) => entry.slot === replacementWorksheetSlot), false); assert.equal(await page.getByRole("button", { name: "Video Worksheet" }).count(), 0);
  await page.locator('.native-video-editor input[type="file"][accept*="application/pdf"]').setInputFiles({ name: "classroom-worksheet.pdf", mimeType: "application/pdf", buffer: worksheetPdf }); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor();
  await page.locator('.native-video-editor input[type="file"][accept*=".srt"]').setInputFiles({ name: "replacement.srt", mimeType: "application/x-subrip", buffer: Buffer.from(replacementVideoSrt) }); await page.getByText("Video companion complete", { exact: true }).waitFor(); await page.locator('.native-video-editor input[type="file"][accept*="video/mp4"]').setInputFiles({ name: "classroom-companion-replacement.mp4", mimeType: "video/mp4", buffer: videoMp4 }); await page.getByText(/classroom-companion-replacement\.mp4/).waitFor(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.cues[0].text, "Replacement opening subtitle."); assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.fileName, "classroom-companion-replacement.mp4");
  assert.equal(nativeDocuments.get(openResponseId).publicDocument.video.worksheet.fileName, "classroom-worksheet.pdf"); const worksheetSlotBeforeVideoRemoval = nativeDocuments.get(openResponseId).publicDocument.video.worksheet.assetSlot; const replacedVideoSlot = nativeDocuments.get(openResponseId).publicDocument.video.assetSlot; await page.getByRole("button", { name: "Remove / Disable Video" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(Object.hasOwn(nativeDocuments.get(openResponseId).publicDocument, "video"), false); assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.some((asset) => asset.slot === replacedVideoSlot), false); assert.equal(nativeDocuments.get(openResponseId).publicDocument.assets.some((asset) => asset.slot === worksheetSlotBeforeVideoRemoval), false); await uploadVideoCompanion(page); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); await exerciseSupplementalAudio({ page, nativeDocuments, activityId: openResponseId, audioBytes: listeningMp3, replacementAudioBytes: Buffer.concat([listeningMp3, Buffer.from([0])]), referenceBytes: tallReadablePng, replacementReferenceBytes: replacementTallReadablePng });

  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /^Image/ }).check(); await page.getByRole("button", { name: "Create activity" }).click(); await assertContentTabActive(page, ".native-image-editor"); const imageContentText = "The library closes at 4:30 p.m.\nReturn borrowed books before closing.";
  const imageId = "ultimate-b2-sb-u1-p1-o91"; await page.getByText(imageId, { exact: true }).first().waitFor(); await page.getByText("Content incomplete", { exact: true }).first().waitFor(); assert.equal(await page.getByLabel("Visible instruction").count(), 0); await page.getByText("This learner-facing text is retained for LMS reading support. It is not displayed inside the Interactive image stage.", { exact: true }).waitFor(); await verifyReadableTextStartsOffAndBlocksIncompleteSave(page); await page.getByLabel("Activity title").fill("Production image draft"); await page.getByRole("textbox", { name: /^Content/ }).fill(imageContentText); await page.getByRole("tab", { name: "Local Preview" }).click(); const emptyImagePreview = page.getByRole("tabpanel", { name: "Local Preview" }); await emptyImagePreview.getByRole("heading", { name: "Production image draft", exact: true }).waitFor(); assert.equal(await emptyImagePreview.getByText(imageContentText, { exact: true }).count(), 0); assert.equal(await emptyImagePreview.locator(".native-image-learner-content").count(), 0); await page.getByRole("tab", { name: "Content" }).click(); assert.equal(await page.getByRole("textbox", { name: /^Content/ }).inputValue(), imageContentText); await page.getByRole("tab", { name: "Layout" }).click();
  await page.locator(".native-or-upload input[type=file]").setInputFiles({ name: "image.png", mimeType: "image/png", buffer: onePixelPng }); await page.locator(".native-image-surface img").waitFor(); assert.equal(await page.locator(".native-image-surface img").first().evaluate((img) => img.parentElement?.tagName), "BUTTON"); await page.getByLabel("Alt text").fill("A useful diagram"); await page.getByLabel("Fit").selectOption("cover"); await page.getByRole("spinbutton", { name: "X", exact: true }).fill("210"); await page.getByRole("spinbutton", { name: "Y", exact: true }).fill("130"); const imageWidthInput = page.getByRole("spinbutton", { name: "Width", exact: true }); const imageHeightInput = page.getByRole("spinbutton", { name: "Height", exact: true }); const imageHeightBeforeWidthEdit = await imageHeightInput.inputValue(); await imageWidthInput.fill(String(Number(await imageWidthInput.inputValue()) + 73)); assert.equal(await imageHeightInput.inputValue(), imageHeightBeforeWidthEdit, "Image width edits must not change height"); assert.equal(await page.getByRole("button", { name: /Resize A useful diagram from/ }).count(), 4);
  await page.locator(".native-or-upload input[type=file]").setInputFiles({ name: "second-image.png", mimeType: "image/png", buffer: secondPixelPng }); await page.locator(".native-or-layers button").nth(1).waitFor(); await page.getByLabel("Alt text").fill("Foreground diagram");
  await page.locator(".native-or-layers button").filter({ hasText: "A useful diagram" }).click(); await page.getByRole("button", { name: "Duplicate image" }).click(); await page.locator(".native-or-layers button").nth(2).waitFor(); await page.getByLabel("Alt text").fill("Background diagram"); await page.getByRole("button", { name: "Send to Back" }).click(); await page.getByLabel("Lock position and size").check(); assert.equal(await page.getByRole("spinbutton", { name: "X", exact: true }).isDisabled(), true);
  await page.locator(".native-or-upload input[type=file]").setInputFiles({ name: "same-image.png", mimeType: "image/png", buffer: onePixelPng }); await page.locator(".native-or-layers button").nth(3).waitFor(); await page.getByLabel("Alt text").fill("Reuploaded diagram"); assert.equal(await page.locator(".native-image-surface img").count(), 4); await uploadReadableText(page, tallReadablePng, "readable-image.png", "Image activity readable passage"); await page.getByRole("tab", { name: "Layout" }).click(); await page.waitForTimeout(150); await page.screenshot({ path: path.join(screenshotRoot, "native-image-layout-1440.png"), fullPage: true }); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(imageId).publicDocument.readableText.sourceHeight, 1800);
  const firstImageReference = structuredClone(nativeDocuments.get(imageId).publicDocument.assets.find((item) => item.checksumSha256 === createHash("sha256").update(onePixelPng).digest("hex"))); assert.equal(nativeDocuments.get(imageId).publicDocument.assets.length, 3); assert.equal(nativeDocuments.get(imageId).publicDocument.parts[0].interaction.images.length, 4); assert.equal(new Set(nativeDocuments.get(imageId).publicDocument.parts[0].interaction.images.map((item) => item.assetSlot)).size, 2);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(imageId) }).click(); assert.equal(await page.getByLabel("Activity title").inputValue(), "Production image draft"); assert.equal(await page.getByRole("textbox", { name: /^Content/ }).inputValue(), imageContentText); assert.equal(await page.getByLabel("Visible instruction").count(), 0); assert.equal(nativeDocuments.get(imageId).publicDocument.parts[0].interaction.contentText, imageContentText); assert.equal("contentText" in nativeDocuments.get(imageId).teacherDocument.parts[0].solution, false); await page.getByRole("tab", { name: "Local Preview" }).click(); const authoredImagePreview = page.getByRole("tabpanel", { name: "Local Preview" }); await authoredImagePreview.locator(".native-image-surface img").first().waitFor(); assert.equal(await authoredImagePreview.getByText(imageContentText, { exact: true }).count(), 0); assert.equal(await authoredImagePreview.locator(".native-image-learner-content").count(), 0); assert.equal(await authoredImagePreview.locator(".native-image-presentation").count(), 1); await page.getByRole("tab", { name: "Layout" }).click(); assert.equal(await page.locator(".native-image-surface img").count(), 4); await page.locator(".native-or-layers button").filter({ hasText: "Background diagram" }).click(); assert.equal(await page.getByLabel("Lock position and size").isChecked(), true);
  await page.locator(".native-or-layers button").filter({ hasText: "Reuploaded diagram" }).click(); await page.getByRole("button", { name: "Remove image" }).click(); await page.getByRole("button", { name: "Confirm remove" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(imageId).publicDocument.assets.length, 3);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(imageId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); assert.equal(await page.locator(".native-image-surface img").count(), 3);
  await page.locator(".native-or-layers button").filter({ hasText: "Background diagram" }).click(); await page.getByRole("button", { name: "Remove image" }).click(); await page.getByRole("button", { name: "Confirm remove" }).click(); await page.locator(".native-or-layers button").filter({ hasText: "A useful diagram" }).click(); await page.getByRole("button", { name: "Remove image" }).click(); await page.getByRole("button", { name: "Confirm remove" }).click(); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(imageId).publicDocument.assets.length, 2); assert.equal(nativeDocuments.get(imageId).publicDocument.parts[0].interaction.images.length, 1);
  await page.locator(".native-or-upload input[type=file]").setInputFiles({ name: "image.png", mimeType: "image/png", buffer: onePixelPng }); await page.getByLabel("Alt text").fill("Restored diagram"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.deepEqual(nativeDocuments.get(imageId).publicDocument.assets.find((item) => item.slot === firstImageReference.slot), firstImageReference); assert.equal(nativeDocuments.get(imageId).publicDocument.assets.length, 3); assert.equal(nativeDocuments.get(imageId).teacherDocument.parts[0].solution.kind, "image");
  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /Multiple Choice/ }).check(); await page.getByLabel(/Initial title/).fill("Browser native multiple choice"); await page.getByRole("button", { name: "Create activity" }).click(); await assertContentTabActive(page, ".native-single-choice-editor");
  const singleChoiceId = "ultimate-b2-sb-u1-p1-o92"; await page.getByText(singleChoiceId, { exact: true }).first().waitFor(); await page.getByRole("heading", { name: "Browser native multiple choice" }).first().waitFor(); assert.equal(await page.getByLabel("Visible instruction").count(), 0); await verifyReadableTextStartsOffAndBlocksIncompleteSave(page); await page.getByLabel("Activity title").fill("Persisted browser multiple choice");
  await page.getByRole("button", { name: "Add Question" }).click(); await page.getByText("Needs answer", { exact: true }).first().waitFor(); assert.equal(await page.getByRole("button", { name: "Save Draft" }).isDisabled(), true); await page.getByLabel("Prompt").fill("Which release is graded?"); let optionInputs = page.locator('.native-single-choice-editor fieldset textarea[aria-label^="Option "]'); await optionInputs.nth(0).fill("The latest head"); await optionInputs.nth(1).fill("The pinned release"); assert.equal(nativeDocuments.get(singleChoiceId).teacherDocument.parts[0].solution.correctAnswers.length, 0); await page.getByRole("tab", { name: "Answer Key" }).click(); const secondAnswerChoice = page.getByRole("checkbox", { name: "Option 2: The pinned release" }); await secondAnswerChoice.focus(); await secondAnswerChoice.press("Space"); assert.equal(await secondAnswerChoice.isChecked(), true); assert.equal(await page.getByRole("button", { name: "Save Draft" }).isDisabled(), false); await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByText("The pinned release", { exact: true }).waitFor(); await page.getByText("Unsaved changes", { exact: true }).waitFor(); await page.getByRole("tab", { name: "Content" }).click(); assert.equal(await page.getByLabel("Prompt").inputValue(), "Which release is graded?");
  await page.getByRole("button", { name: "Add Question" }).click(); await page.getByLabel("Prompt").fill("Who calculates the score?"); optionInputs = page.locator('.native-single-choice-editor fieldset textarea[aria-label^="Option "]'); await optionInputs.nth(0).fill("The browser"); await optionInputs.nth(1).fill("The server"); await page.getByRole("tab", { name: "Answer Key" }).click(); await page.getByRole("checkbox", { name: "Option 2: The server" }).click(); await page.getByRole("tab", { name: "Content" }).click(); await page.getByRole("button", { name: "Add Option" }).click(); optionInputs = page.locator('.native-single-choice-editor fieldset textarea[aria-label^="Option "]'); await optionInputs.nth(2).fill("A teacher"); await optionInputs.nth(2).locator("xpath=..").getByRole("button").first().click(); optionInputs = page.locator('.native-single-choice-editor fieldset textarea[aria-label^="Option "]'); await optionInputs.nth(0).locator("xpath=..").getByRole("button", { name: "Delete", exact: true }).click(); await page.getByRole("button", { name: "Add Question" }).click(); await page.getByLabel("Prompt").fill("Temporary reorder question"); await page.getByRole("button", { name: "Move Up", exact: true }).click(); page.once("dialog", (dialog) => dialog.accept()); await page.getByRole("button", { name: "Delete Question" }).click(); await uploadReadableText(page, tallReadablePng, "readable-multiple-choice.png", "Multiple Choice readable passage"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(singleChoiceId).publicDocument.readableText.sourceHeight, 1800);
  const singleState = nativeDocuments.get(singleChoiceId); const savedChoiceIds = singleState.publicDocument.parts[0].interaction.questions.map((question) => ({ questionId: question.id, optionIds: question.options.map((option) => option.id) })); assert.equal(singleState.publicDocument.parts[0].interaction.questions.length, 2); assert.doesNotMatch(JSON.stringify(singleState.publicDocument), /correctOptionId|correctAnswers/); assert.equal(singleState.teacherDocument.parts[0].solution.correctAnswers.length, 2);
  await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); await page.locator(".native-or-preview").getByText("The pinned release", { exact: true }).click(); assert.equal(await page.locator(".native-or-preview").getByText("Correct answer", { exact: false }).count(), 0); await page.getByRole("button", { name: "Teacher Preview" }).click(); const localTeacherChoice = page.locator('.native-or-preview .native-single-choice-teacher[data-native-single-choice-presentation="text"]'); await localTeacherChoice.waitFor(); const localCorrectChoice = localTeacherChoice.getByText("The pinned release", { exact: true }).locator(".."); await localCorrectChoice.click(); assert.equal(await localCorrectChoice.getAttribute("data-answer-state"), "correct"); await localTeacherChoice.getByText("Correct.", { exact: true }).waitFor(); assert.equal(await localTeacherChoice.getByText("Correct answer", { exact: false }).count(), 0); assert.ok(await localTeacherChoice.locator('input[type="radio"]').count() >= 2); await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(singleChoiceId) }).click(); assert.equal(await page.getByLabel("Activity title").inputValue(), "Persisted browser multiple choice"); assert.deepEqual(nativeDocuments.get(singleChoiceId).publicDocument.parts[0].interaction.questions.map((question) => ({ questionId: question.id, optionIds: question.options.map((option) => option.id) })), savedChoiceIds); await page.getByRole("tab", { name: "Answer Key" }).click(); assert.equal(await page.getByRole("checkbox", { name: "Option 2: The pinned release" }).isChecked(), true); assert.equal(nativeDocuments.get(singleChoiceId).teacherDocument.parts[0].solution.correctAnswers[0].correctOptionIds[0], savedChoiceIds[0].optionIds[1]); await page.getByRole("tab", { name: "Content" }).click();
  await page.getByRole("tab", { name: "Visual" }).click(); await page.getByRole("button", { name: "Enable visual mode" }).click(); assert.equal(await page.getByRole("button", { name: "Save Draft" }).isDisabled(), true);
  await page.locator(".native-single-choice-visual-authoring input[type=file]").setInputFiles({ name: "choice-panel-1.png", mimeType: "image/png", buffer: halfHeightChoicePng }); await page.locator(".native-single-choice-hotspot-canvas img").waitFor();
  const createVisualHotspot = createVisualHotspotCreator(page);
  const initialHotspotGeometry = await createVisualHotspot(1); const selectedHotspotFrame = page.getByRole("group", { name: "Hotspot selected" }); const firstHotspotX = Number(await page.getByRole("spinbutton", { name: "X", exact: true }).inputValue()); await selectedHotspotFrame.press("ArrowRight"); assert.equal(Number(await page.getByRole("spinbutton", { name: "X", exact: true }).inputValue()), firstHotspotX + 1); const firstHotspotY = Number(await page.getByRole("spinbutton", { name: "Y", exact: true }).inputValue()); await selectedHotspotFrame.press("Shift+ArrowDown"); const keyboardGeometry = await Promise.all(["X", "Y", "Width", "Height"].map((name) => page.getByRole("spinbutton", { name, exact: true }).inputValue())).then((values) => values.map(Number)); assert.equal(keyboardGeometry[1], Math.min(firstHotspotY + 10, initialHotspotGeometry.sourceHeight - initialHotspotGeometry.height));
  const authoringCanvas = page.locator(".native-single-choice-hotspot-canvas");
  const authoringCanvasBox = await exerciseVisualHotspotPointerMove(page, initialHotspotGeometry);
  for (const [name, value] of [["X", 200], ["Y", 80], ["Width", 200], ["Height", 100]]) await page.getByRole("spinbutton", { name, exact: true }).fill(String(value));
  for (const [corner, dx, dy] of [["top left", -8, -8], ["top right", 8, -8], ["bottom right", 8, 8], ["bottom left", -8, 8]]) {
    if (!await selectedHotspotFrame.count()) await page.locator(".native-single-choice-authoring-hotspot").first().click();
    const before = await Promise.all(["X", "Y", "Width", "Height"].map((name) => page.getByRole("spinbutton", { name, exact: true }).inputValue())).then((values) => values.map(Number));
    const handle = page.getByRole("button", { name: `Resize Hotspot from ${corner}` }); const box = await handle.boundingBox(); assert.ok(box);
    await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2); await page.mouse.down(); await page.mouse.move(box.x + box.width / 2 + dx, box.y + box.height / 2 + dy); await page.mouse.up();
    if (!await selectedHotspotFrame.count()) await page.locator(".native-single-choice-authoring-hotspot").first().click();
    const after = await Promise.all(["X", "Y", "Width", "Height"].map((name) => page.getByRole("spinbutton", { name, exact: true }).inputValue())).then((values) => values.map(Number));
    assert.notDeepEqual(after, before, `${corner} resize handle must change source geometry`); assert.ok(after[0] >= 0 && after[1] >= 0 && after[0] + after[2] <= initialHotspotGeometry.sourceWidth && after[1] + after[3] <= initialHotspotGeometry.sourceHeight);
  }
  await setVisualHotspotGeometry(page, { x: 48, y: 190, width: 96, height: 60 });
  await authoringCanvas.click({ position: { x: authoringCanvasBox.width * .9, y: authoringCanvasBox.height * .9 } }); await page.getByRole("heading", { name: "Panel properties" }).waitFor(); assert.equal(await page.getByRole("group", { name: "Hotspot selected" }).count(), 0); assert.equal(await page.getByLabel("Option to map").inputValue(), ""); assert.equal(await page.getByRole("button", { name: "New hotspot" }).isDisabled(), false); await page.locator(".native-single-choice-authoring-hotspot").first().click(); await page.getByRole("heading", { name: "Hotspot properties" }).waitFor(); assert.equal(await page.getByRole("group", { name: "Hotspot selected" }).count(), 1); await selectedHotspotFrame.press("Escape");
  await createVisualHotspot(null); await page.getByRole("button", { name: "Delete Hotspot" }).click(); assert.equal(await page.getByRole("button", { name: "New hotspot" }).isDisabled(), false);
  await createVisualHotspot(2); await page.getByRole("button", { name: "Delete Hotspot" }).click(); await createVisualHotspot(2, { x: 320, y: 190, width: 96, height: 60 }); await page.getByRole("group", { name: "Hotspot selected" }).press("Escape");
  await page.getByRole("button", { name: "Add Panel" }).click(); await page.locator(".native-single-choice-visual-authoring input[type=file]").setInputFiles({ name: "choice-panel-2.png", mimeType: "image/png", buffer: portraitChoicePng }); await page.getByRole("button", { name: "Move Up", exact: true }).last().click(); await page.getByRole("button", { name: "Move Down", exact: true }).last().click();
  await createVisualHotspot(3, { x: 60, y: 100, width: 80, height: 60 }); await page.getByRole("group", { name: "Hotspot selected" }).press("Escape"); await createVisualHotspot(4, { x: 220, y: 100, width: 80, height: 60 }); await page.getByRole("group", { name: "Hotspot selected" }).press("Escape"); assert.equal(await page.getByRole("button", { name: "New hotspot" }).isDisabled(), true); assert.equal(await page.getByRole("button", { name: "New hotspot" }).getAttribute("title"), "Every option already has a hotspot");
  await page.getByRole("button", { name: "Add Panel" }).click(); page.once("dialog", (dialog) => dialog.accept()); await page.getByRole("button", { name: "Delete Panel" }).click(); await page.screenshot({ path: path.join(screenshotRoot, "native-single-choice-visual-1440.png"), fullPage: true });
  await page.getByRole("tab", { name: "Readable Text" }).click();
  const audioEditor = page.locator(".native-audio-hotspot-editor"); const readablePercentGeometry = (locator) => locator.evaluate((element) => Object.fromEntries(["left", "top", "width", "height"].map((key) => [key, Number.parseFloat(element.style[key])])));
  const drawReadableRegion = async ({ region, start, end, exerciseTransactionGuards = false }) => { const outer = region === "focus"; const redraw = audioEditor.getByRole("button", { name: outer ? "Redraw outer focus" : "Redraw inner highlight" }); const canvas = audioEditor.getByRole("group", { name: "Readable text focus and highlight regions" }); const selection = audioEditor.getByRole("group", { name: outer ? "Outer readable text focus selected" : "Inner colored highlight selected", exact: true });
    await redraw.click(); const redrawHandle = await redraw.elementHandle(); assert.ok(redrawHandle); await page.waitForFunction((button) => button.getAttribute("aria-pressed") === "true", redrawHandle); await audioEditor.locator(".native-audio-hotspot-focus-editor.is-drawing").waitFor(); await canvas.scrollIntoViewIfNeeded(); const box = await canvas.boundingBox(); assert.ok(box); const point = ([x, y]) => ({ x: box.x + box.width * x, y: box.y + box.height * y }); const from = point(start); const to = point(end);
    if (exerciseTransactionGuards) { const committed = canvas.locator(".native-audio-hotspot-focus-box"); const beforeCancel = await readablePercentGeometry(committed); await canvas.evaluate((element) => element.addEventListener("pointerdown", (event) => { element.dataset.observedPointerId = String(event.pointerId); }, { once: true })); await page.mouse.move(from.x, from.y); await page.mouse.down(); const observedPointerId = await canvas.getAttribute("data-observed-pointer-id"); assert.notEqual(observedPointerId, null); const pointerId = Number(observedPointerId); assert.ok(Number.isInteger(pointerId)); await canvas.evaluate((element) => { delete element.dataset.observedPointerId; }); const unrelated = { pointerId: pointerId === 987654321 ? 987654320 : 987654321, pointerType: "mouse", isPrimary: false, button: 0, buttons: 0, clientX: to.x, clientY: to.y }; const dispatchPointer = (type, init) => canvas.evaluate((element, payload) => { const event = new PointerEvent(payload.type, { bubbles: true, cancelable: true, ...payload.init }); element.dispatchEvent(event); return event.pointerId; }, { type, init }); assert.equal(await dispatchPointer("pointerup", unrelated), unrelated.pointerId); assert.equal(await dispatchPointer("pointercancel", unrelated), unrelated.pointerId); assert.equal(await redraw.getAttribute("aria-pressed"), "true"); assert.equal(await selection.count(), 0); assert.equal(await dispatchPointer("pointercancel", { ...unrelated, pointerId, isPrimary: true }), pointerId); await page.mouse.move(to.x, to.y); await page.mouse.up(); assert.deepEqual(await readablePercentGeometry(committed), beforeCancel); assert.equal(await redraw.getAttribute("aria-pressed"), "true"); assert.equal(await selection.count(), 0); }
    await page.mouse.move(from.x, from.y); await page.mouse.down(); await page.mouse.move(to.x, to.y); await page.mouse.up(); await selection.waitFor(); const actual = await readablePercentGeometry(selection); const expected = { left: Math.min(start[0], end[0]) * 100, top: Math.min(start[1], end[1]) * 100, width: Math.abs(start[0] - end[0]) * 100, height: Math.abs(start[1] - end[1]) * 100 }; for (const key of Object.keys(expected)) assert.ok(Math.abs(actual[key] - expected[key]) < .15, `${region} ${key}: ${actual[key]} vs ${expected[key]}`); assert.equal(await selection.getByRole("button", { name: new RegExp(`Resize ${outer ? "Outer readable text focus" : "Inner colored highlight"} from`) }).count(), 4); return selection; };
  const placeAudioHotspot = async ({ label, marker, panelIndex = 0, activityX = .25, activityY = .3, focusStart = [.1, .1], focusEnd = [.85, .3] }) => {
    await audioEditor.getByRole("button", { name: "Add readable-text hotspot" }).click();
    await page.waitForFunction(() => document.querySelector(".native-audio-hotspot-list button:last-child")?.getAttribute("aria-selected") === "true");
    if (panelIndex) await audioEditor.getByLabel("Activity panel").selectOption({ index: panelIndex });
    await audioEditor.getByLabel("Hotspot accessible label").fill(label);
    const activityCanvas = audioEditor.getByRole("button", { name: "Place readable-text hotspot on activity" });
    const activityBox = await activityCanvas.boundingBox(); assert.ok(activityBox);
    await activityCanvas.click({ position: { x: activityBox.width * activityX, y: activityBox.height * activityY } });
    await exerciseIntegerHotspotCoordinates(audioEditor, panelIndex);
    await drawReadableRegion({ region: "focus", start: focusStart, end: focusEnd, exerciseTransactionGuards: label === "Listen to opening excerpt" });
    await audioEditor.locator('input[type="file"][accept*="audio/mpeg"]').setInputFiles({ name: `${label.toLowerCase().replaceAll(" ", "-")}.mp3`, mimeType: "audio/mpeg", buffer: hotspotMp3(marker) });
    await audioEditor.locator(":scope > .native-audio-hotspot-authoring > audio").waitFor();
  };
  await audioEditor.getByRole("button", { name: "Add readable-text hotspot" }).click(); await audioEditor.getByText("No MP3 attached (optional)", { exact: true }).waitFor(); await page.waitForFunction(() => [...document.querySelectorAll("button")].some((button) => button.textContent?.trim() === "Save Draft" && !button.disabled)); assert.equal(await page.getByRole("button", { name: "Save Draft" }).isDisabled(), false); await audioEditor.getByRole("button", { name: "Remove hotspot" }).click();
  await placeAudioHotspot({ label: "Listen to opening excerpt", marker: 1, activityX: .16, focusStart: [.08, .08], focusEnd: [.88, .27] });
  const outerFocusSelection = audioEditor.getByRole("group", { name: "Outer readable text focus selected", exact: true }); await outerFocusSelection.waitFor(); assert.equal(await outerFocusSelection.getByRole("button", { name: /Resize Outer readable text focus from/ }).count(), 4);
  for (const corner of ["top left", "top right", "bottom right", "bottom left"]) {
    const handle = audioEditor.getByRole("button", { name: `Resize Outer readable text focus from ${corner}` }); const box = await handle.boundingBox(); assert.ok(box);
    await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2); await page.mouse.down(); await page.mouse.move(box.x + box.width / 2 + (corner.includes("left") ? -4 : 4), box.y + box.height / 2 + (corner.includes("top") ? -3 : 3)); await page.mouse.up();
  }
  await drawReadableRegion({ region: "highlight", start: [.2, .12], end: [.7, .22] }); await audioEditor.getByRole("button", { name: "Delete inner highlight" }).click(); await audioEditor.getByRole("button", { name: "Add inner highlight" }).click(); const addedInnerSelection = audioEditor.getByRole("group", { name: "Inner colored highlight selected" }); await addedInnerSelection.waitFor(); assert.equal(await addedInnerSelection.getByRole("button", { name: /Resize Inner colored highlight from/ }).count(), 4);
  await audioEditor.getByLabel("Green").check(); assert.equal(await audioEditor.getByRole("group", { name: "Readable text focus and highlight regions" }).getAttribute("data-highlight-color"), "green");
  await exerciseOuterFocusAspectRatio(page, audioEditor);
  await placeAudioHotspot({ label: "Listen to second excerpt", marker: 2, activityX: .72, focusStart: [.08, .34], focusEnd: [.88, .54] });
  await placeAudioHotspot({ label: "Open panel two excerpt", marker: 3, panelIndex: 1, activityX: .38, activityY: .64, focusStart: [.08, .62], focusEnd: [.88, .82] }); const removedAudioAssetId = new URL(await audioEditor.locator(":scope > .native-audio-hotspot-authoring > audio").getAttribute("src"), origin).pathname.split("/").at(-1); await audioEditor.getByRole("button", { name: "Remove MP3" }).click(); await audioEditor.getByText("No MP3 attached (optional)", { exact: true }).waitFor(); assert.equal(await audioEditor.locator(":scope > .native-audio-hotspot-authoring > audio").count(), 0); await audioEditor.getByRole("button", { name: "Test hotspot" }).click(); const noAudioAuthoringPreview = audioEditor.locator(".native-audio-hotspot-authoring-preview .native-audio-text-focus"); await noAudioAuthoringPreview.waitFor(); assert.equal(await noAudioAuthoringPreview.locator("audio").count(), 0, "the optional-MP3 Builder preview renders readable focus without audio"); await audioEditor.getByRole("button", { name: "Close hotspot preview" }).click();
  await audioEditor.getByRole("tab", { name: "Hotspot 1" }).click();
  await drawReadableRegion({ region: "focus", start: [.12, .12], end: [.82, .32] }); await audioEditor.getByLabel("Keep aspect ratio", { exact: true }).check(); await audioEditor.getByLabel("Quick Width", { exact: true }).fill("512"); await audioEditor.getByRole("button", { name: "Delete inner highlight", exact: true }).click(); await audioEditor.getByRole("button", { name: "Add inner highlight", exact: true }).click();
  const originalOpeningAudioSource = await audioEditor.locator(":scope > .native-audio-hotspot-authoring > audio").getAttribute("src"); await audioEditor.locator('input[type="file"][accept*="audio/mpeg"]').setInputFiles({ name: "opening-replacement.mp3", mimeType: "audio/mpeg", buffer: hotspotMp3(4) }); await page.waitForFunction((source) => document.querySelector(".native-audio-hotspot-editor > .native-audio-hotspot-authoring > audio")?.getAttribute("src") !== source, originalOpeningAudioSource);
  await audioEditor.getByRole("tab", { name: "Hotspot 2" }).click(); await audioEditor.getByRole("button", { name: "Test hotspot" }).click(); await audioEditor.locator(".native-audio-hotspot-authoring-preview .native-audio-text-focus").waitFor(); await audioEditor.getByRole("button", { name: "Close hotspot preview" }).click();
  await uploadVideoCompanion(page); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const savedVisualChoice = nativeDocuments.get(singleChoiceId); assert.equal(savedVisualChoice.publicDocument.parts.length, 1); assert.equal(savedVisualChoice.publicDocument.parts[0].id, "part-1"); assert.equal(savedVisualChoice.publicDocument.parts[0].interaction.presentation.panels.length, 2); assert.deepEqual(savedVisualChoice.publicDocument.parts[0].interaction.presentation.panels.map(({ sourceWidth, sourceHeight }) => [sourceWidth, sourceHeight]), [[1024, 291], [700, 1200]]); const savedChoiceHotspots = savedVisualChoice.publicDocument.parts[0].interaction.presentation.panels.flatMap((panel) => panel.hotspots); assert.equal(savedChoiceHotspots.length, 4); assert.ok(savedChoiceHotspots.every((hotspot) => !Object.hasOwn(hotspot, "highlightArea")), "new hotspots persist one canonical area only"); assert.equal(savedVisualChoice.publicDocument.audioTextHotspots.hotspots[0].highlightColor, "green"); assert.doesNotMatch(JSON.stringify(savedVisualChoice.publicDocument), /correctOptionId|correctAnswers|isCorrect/);
  const persistedHotspotGeometry = structuredClone(savedChoiceHotspots[0].area);
  assert.equal(savedVisualChoice.publicDocument.audioTextHotspots.hotspots.length, 3); assert.equal(savedVisualChoice.publicDocument.audioTextHotspots.hotspots.find((hotspot) => hotspot.label === "Open panel two excerpt").audioAssetSlot, ""); assert.equal(savedVisualChoice.publicDocument.assets.some((asset) => asset.assetId === removedAudioAssetId), false, "removing a unique MP3 leaves no phantom managed reference"); assert.ok(savedVisualChoice.publicDocument.audioTextHotspots.hotspots.every((hotspot) => hotspot.readableHighlightArea && hotspot.readableHighlightArea.x >= hotspot.readableFocusArea.x && hotspot.readableHighlightArea.y >= hotspot.readableFocusArea.y && hotspot.readableHighlightArea.x + hotspot.readableHighlightArea.width <= hotspot.readableFocusArea.x + hotspot.readableFocusArea.width && hotspot.readableHighlightArea.y + hotspot.readableHighlightArea.height <= hotspot.readableFocusArea.y + hotspot.readableFocusArea.height)); const firstPanelAudioHotspots = savedVisualChoice.publicDocument.audioTextHotspots.hotspots.filter((hotspot) => hotspot.panelId === savedVisualChoice.publicDocument.parts[0].interaction.presentation.panels[0].id); assert.equal(firstPanelAudioHotspots.length, 2); assert.deepEqual(firstPanelAudioHotspots.map((hotspot) => hotspot.activityArea.y), [210, 210]); assert.notEqual(firstPanelAudioHotspots[0].activityArea.x, firstPanelAudioHotspots[1].activityArea.x); for (const panel of savedVisualChoice.publicDocument.parts[0].interaction.presentation.panels) { for (const [index, hotspot] of panel.hotspots.entries()) for (const other of panel.hotspots.slice(index + 1)) assert.equal(sourceRectanglesOverlap(hotspot.area, other.area), false, "Deterministic answer hotspots must not overlap one another"); for (const hotspot of panel.hotspots) for (const audioHotspot of savedVisualChoice.publicDocument.audioTextHotspots.hotspots.filter((candidate) => candidate.panelId === panel.id)) assert.equal(sourceRectanglesOverlap(hotspot.area, audioHotspot.activityArea), false, "Answer and audio hotspot fixtures must remain independently clickable"); } assert.doesNotMatch(JSON.stringify(savedVisualChoice.publicDocument.audioTextHotspots), /teacher|correct|previewUrl|https?:\/\//i);
  await page.getByRole("tab", { name: "Local Preview" }).click(); const front = page.getByRole("tabpanel", { name: "Local Preview" }).locator(".native-or-preview"); const pinnedHotspot = front.getByRole("button", { name: "Which release is graded?: The pinned release" }); await pinnedHotspot.focus(); await pinnedHotspot.press("Enter"); assert.equal(await pinnedHotspot.getAttribute("aria-pressed"), "true"); const latestHotspot = front.getByRole("button", { name: "Which release is graded?: The latest head" }); await latestHotspot.evaluate((button) => button.click()); assert.equal(await latestHotspot.getAttribute("aria-pressed"), "true"); assert.equal(await pinnedHotspot.getAttribute("aria-pressed"), "false"); await pinnedHotspot.evaluate((button) => button.click()); await front.getByRole("button", { name: "Next" }).click(); await front.getByText("Panel 2 of 2", { exact: true }).waitFor(); assert.equal(await front.getByRole("button", { name: "Next" }).isDisabled(), true); await front.getByRole("button", { name: "Show All" }).click(); assert.equal(await front.locator(".native-single-choice-visual-panel").count(), 2); await front.getByRole("button", { name: "Paged View" }).click(); await front.getByText("Panel 2 of 2", { exact: true }).waitFor(); await front.getByRole("button", { name: "Show All" }).click(); assert.equal(await pinnedHotspot.getAttribute("aria-pressed"), "true");
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(singleChoiceId) }).click(); assert.deepEqual(nativeDocuments.get(singleChoiceId).publicDocument.parts[0].interaction.presentation.panels[0].hotspots[0].area, persistedHotspotGeometry); await page.getByRole("tab", { name: "Visual" }).click(); const reloadedHotspotPercent = await page.locator(".native-single-choice-authoring-hotspot").first().evaluate((element) => ({ left: Number.parseFloat(element.style.left), top: Number.parseFloat(element.style.top), width: Number.parseFloat(element.style.width), height: Number.parseFloat(element.style.height) })); assert.ok(Math.abs(reloadedHotspotPercent.left - persistedHotspotGeometry.x / 1024 * 100) < .002); assert.ok(Math.abs(reloadedHotspotPercent.top - persistedHotspotGeometry.y / 291 * 100) < .002); await page.getByRole("tab", { name: "Local Preview" }).click(); assert.equal(await page.getByRole("tabpanel", { name: "Local Preview" }).locator(".native-single-choice-visual-panel").count(), 1);
  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /Multiple Choice/ }).check(); await page.getByLabel(/Initial title/).fill("One panel classroom choice"); await page.getByRole("button", { name: "Create activity" }).click();
  const onePanelChoiceId = "ultimate-b2-sb-u1-p1-o93"; await page.getByText(onePanelChoiceId, { exact: true }).first().waitFor(); await page.getByRole("button", { name: "Add Question" }).click(); await page.getByLabel("Prompt").fill("One panel question?"); optionInputs = page.locator('.native-single-choice-editor fieldset textarea[aria-label^="Option "]'); await optionInputs.nth(0).fill("Wrong one panel option"); await optionInputs.nth(1).fill("Correct one panel option"); await page.getByRole("tab", { name: "Answer Key" }).click(); await page.getByRole("checkbox", { name: "Option 2: Correct one panel option" }).click(); await page.getByRole("tab", { name: "Visual" }).click(); await page.getByRole("button", { name: "Enable visual mode" }).click(); await page.locator(".native-single-choice-visual-authoring input[type=file]").setInputFiles({ name: "choice-one-panel.png", mimeType: "image/png", buffer: smallFourThreeChoicePng }); await page.locator(".native-single-choice-hotspot-canvas img").waitFor(); await createVisualHotspot(1, { x: 32, y: 160, width: 80, height: 48 }); await page.getByRole("group", { name: "Hotspot selected" }).press("Escape"); await createVisualHotspot(2, { x: 176, y: 160, width: 80, height: 48 }); await page.getByRole("group", { name: "Hotspot selected" }).press("Escape"); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); assert.equal(nativeDocuments.get(onePanelChoiceId).publicDocument.parts[0].interaction.presentation.panels.length, 1); assert.deepEqual(nativeDocuments.get(onePanelChoiceId).publicDocument.parts[0].interaction.presentation.panels.map(({ sourceWidth, sourceHeight }) => [sourceWidth, sourceHeight]), [[320, 240]]);
  await exerciseBulkHotspotImport({ page, nativeDocuments, firstPanelPng: halfHeightChoicePng, secondPanelPng: portraitChoicePng });
  await page.locator('.hosted-builder-tool-tabs a[href$="/hotspots"]').click(); await page.getByRole("heading", { name: "Students Book hotspot builder" }).waitFor(); assert.equal(await page.getByRole("button", { name: "Review", exact: true }).count(), 1); assert.equal(await page.locator(".hosted-viewer-preview iframe").count(), 1);
  await createAndSavePageHotspot(page, legacyActivityId, "Explicit current native fixture");
  await page.locator(".editable-hotspot-box").first().click(); await page.getByLabel("Activity").selectOption(openResponseId); await page.getByLabel("Label").fill("Draft Open Response hotspot"); await page.waitForTimeout(150); await page.screenshot({ path: path.join(screenshotRoot, "hotspot-builder-1440.png"), fullPage: true }); const hotspotUrl = page.url(); for (const [viewportWidth, viewportHeight] of [[1280, 800], [1024, 768]]) { const responsive = await context.newPage(); await responsive.setViewportSize({ width: viewportWidth, height: viewportHeight }); await responsive.goto(hotspotUrl, { waitUntil: "domcontentloaded" }); await responsive.locator(".editable-hotspot-box").first().click(); assert.deepEqual(await responsive.evaluate(() => ({ body: document.body.scrollWidth, root: document.documentElement.scrollWidth, viewport: innerWidth })), { body: viewportWidth, root: viewportWidth, viewport: viewportWidth }); await responsive.screenshot({ path: path.join(screenshotRoot, `hotspot-builder-${viewportWidth}.png`), fullPage: true }); await responsive.close(); } await page.locator(".builder-save-state").getByRole("button", { name: "Save", exact: true }).click(); await page.locator(".builder-save-state").getByText("Saved", { exact: true }).waitFor();
  await page.getByRole("button", { name: "Review", exact: true }).click(); let viewer = page.frameLocator(".unified-builder-review-dialog iframe"); await viewer.getByRole("button", { name: "Draft Open Response hotspot" }).waitFor(); assert.equal(await viewer.getByRole("button", { name: "Show Text", exact: true }).count(), 0); assert.ok(authorizationIntents.length > firstReviewAuthorizationCount); assert.equal(await viewer.locator("title").evaluate((element) => element.ownerDocument.title), "Ultimate B2 Viewer");
  await viewer.getByRole("button", { name: "Draft Open Response hotspot" }).evaluate((button) => button.click()); await viewer.getByText("First prompt", { exact: true }).waitFor(); assert.equal(await viewer.getByText("Persisted browser response", { exact: true }).count(), 0); assert.equal(await viewer.getByText("Respond in complete sentences.", { exact: true }).count(), 0); assert.equal(await viewer.getByText("Students Book activity data could not be loaded.", { exact: true }).count(), 0); assert.equal(await viewer.getByText("First private model answer", { exact: true }).count(), 0);
  await exerciseExternalActivityNavigation(viewer);
  const viewerShowNext = viewer.getByRole("button", { name: "Show Next", exact: true });
  const viewerShowAll = viewer.getByRole("button", { name: "Show All", exact: true });
  const viewerReload = viewer.getByRole("button", { name: "Reload", exact: true });
  await viewerShowNext.click();
  await viewer.getByText("First private model answer", { exact: true }).waitFor();
  assert.equal(await viewerShowNext.isDisabled(), true);
  await viewerReload.click();
  assert.equal(await viewer.getByText("First private model answer", { exact: true }).count(), 0);
  await viewerShowAll.click();
  await viewer.getByText("First private model answer", { exact: true }).waitFor();
  const runtimePresentation = viewer.locator(".native-readable-text-presentation");
  await runtimePresentation.waitFor();
  assert.equal(await runtimePresentation.getAttribute("data-readable-text-available"), "true");
  const showText = viewer.locator(".teacher-book-navigation-context--show-text");
  await showText.waitFor();
  assert.equal(await showText.getAttribute("aria-pressed"), "false");
  const outerHeightBeforeText = await viewer.locator("body").evaluate((body) => body.scrollHeight);
  await showText.click();
  const readableScroll = viewer.locator(".native-readable-text-scroll");
  await readableScroll.waitFor();
  assert.equal(await showText.getAttribute("aria-pressed"), "true");
  assert.equal(await viewer.locator(".native-or-surface:visible").count(), 0);
  const readableGeometry = await readableScroll.evaluate((element) => {
    const rect = element.getBoundingClientRect();
    const view = element.closest(".native-readable-text-view");
    const presentation = element.closest(".native-readable-text-presentation");
    const content = element.closest(".teacher-offline-embedded-activity-content");
    const viewRect = view.getBoundingClientRect();
    const presentationRect = presentation.getBoundingClientRect();
    const contentRect = content.getBoundingClientRect();
    const viewStyle = getComputedStyle(view);
    const paddingY = Number.parseFloat(viewStyle.paddingTop) + Number.parseFloat(viewStyle.paddingBottom);
    const imageRect = element.querySelector("img").getBoundingClientRect();
    const renderedScale = view.offsetHeight ? viewRect.height / view.offsetHeight : 1;
    return { clientHeight: element.clientHeight, clientWidth: element.clientWidth, scrollHeight: element.scrollHeight, scrollBoxHeight: rect.height, viewHeight: viewRect.height, presentationHeight: presentationRect.height, contentHeight: contentRect.height, renderedPaddingY: paddingY * renderedScale, imageWidth: imageRect.width, imageHeight: imageRect.height };
  });
  assert.ok(readableGeometry.scrollHeight > readableGeometry.clientHeight);
  assert.ok(Math.abs((readableGeometry.imageWidth / readableGeometry.imageHeight) - (1000 / 1800)) < 0.02);
  assert.ok(Math.abs(readableGeometry.presentationHeight - readableGeometry.contentHeight) <= 2, JSON.stringify(readableGeometry));
  assert.ok(Math.abs(readableGeometry.viewHeight - readableGeometry.presentationHeight) <= 2, JSON.stringify(readableGeometry));
  assert.ok(Math.abs(readableGeometry.scrollBoxHeight - (readableGeometry.viewHeight - readableGeometry.renderedPaddingY)) <= 3, JSON.stringify(readableGeometry));
  const readableControl = viewer.getByRole("scrollbar", { name: "Readable text vertical scroll" });
  await readableControl.waitFor();
  assert.equal(await readableScroll.evaluate((element) => getComputedStyle(element).scrollbarWidth), "none");
  await readableScroll.hover(); await page.mouse.wheel(0, 600); await page.waitForTimeout(100);
  assert.ok(await readableScroll.evaluate((element) => element.scrollTop) > 0);
  await readableControl.focus(); await readableControl.press("End"); await page.waitForTimeout(50); assert.equal(await readableScroll.evaluate((element) => Math.round(element.scrollTop)), Math.round(await readableControl.getAttribute("aria-valuemax")));
  await readableControl.press("Home"); await page.waitForTimeout(50); assert.equal(await readableScroll.evaluate((element) => element.scrollTop), 0);
  assert.ok(await viewer.locator("body").evaluate((body) => body.scrollHeight) <= outerHeightBeforeText + 2);
  await showText.click(); await readableScroll.waitFor({ state: "detached" });
  assert.equal(await showText.getAttribute("aria-pressed"), "false");
  assert.equal(await viewer.getByText("First private model answer", { exact: true }).count(), 1);
  const showVideo = viewer.locator(".teacher-book-navigation-context--video"); await showVideo.waitFor(); assert.equal(await showVideo.getAttribute("aria-pressed"), "false");
  await showVideo.click(); const videoPlayer = viewer.locator(".native-video-player-shell"); await videoPlayer.waitFor(); assert.equal(await runtimePresentation.getAttribute("data-presentation-view"), "video"); assert.equal(await viewer.locator(".native-or-surface:visible").count(), 0); const videoFill = await videoPlayer.evaluate((shell) => { const view = shell.closest(".native-video-presentation-view").getBoundingClientRect(); const box = shell.getBoundingClientRect(); return { view: { width: view.width, height: view.height }, shell: { width: box.width, height: box.height } }; }); assert.ok(Math.abs(videoFill.view.width - videoFill.shell.width) <= 2 && Math.abs(videoFill.view.height - videoFill.shell.height) <= 2, JSON.stringify(videoFill));
  const runtimeVideo = videoPlayer.locator("video"); await runtimeVideo.evaluate((element) => { Object.defineProperty(element, "currentTime", { configurable: true, writable: true, value: 1 }); element.dispatchEvent(new Event("timeupdate")); }); await videoPlayer.getByText("Welcome to the companion video.", { exact: true }).waitFor();
  await videoPlayer.getByRole("button", { name: "Turn subtitles off" }).click(); assert.equal(await videoPlayer.getByText("Welcome to the companion video.", { exact: true }).count(), 0); await videoPlayer.getByRole("button", { name: "Turn subtitles on" }).click(); await videoPlayer.getByText("Welcome to the companion video.", { exact: true }).waitFor();
  const mediaBeforeVideo = await videoPlayer.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await videoPlayer.getByRole("button", { name: "Play video", exact: true }).first().click(); const mediaAfterVideo = await videoPlayer.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(mediaAfterVideo.playCount > mediaBeforeVideo.playCount);
  await videoPlayer.getByRole("button", { name: "Open fullscreen", exact: true }).click(); const exitVideoFullscreen = videoPlayer.getByRole("button", { name: "Exit fullscreen", exact: true }); await exitVideoFullscreen.waitFor(); assert.deepEqual(await videoPlayer.evaluate((shell) => ({ shellIsFullscreen: document.fullscreenElement === shell, videoIsFullscreen: document.fullscreenElement === shell.querySelector("video"), ...globalThis.__hhplmsFullscreenTest.snapshot() })), { shellIsFullscreen: true, videoIsFullscreen: false, requestCount: 1, exitCount: 0, requestedTagName: "SECTION" }); await exitVideoFullscreen.evaluate((button) => button.blur()); await videoPlayer.locator(".native-video-stage").hover({ position: { x: 20, y: 20 } }); await page.waitForTimeout(200); const exitIdleState = await exitVideoFullscreen.evaluate((button) => ({ opacity: getComputedStyle(button).opacity, hover: button.matches(":hover"), focusVisible: button.matches(":focus-visible"), active: button.matches(":active"), activeElement: button === document.activeElement, rect: button.getBoundingClientRect().toJSON() })); assert.equal(exitIdleState.opacity, "0.34", JSON.stringify(exitIdleState)); const fullscreenGeometry = await videoPlayer.evaluate((shell) => { const exit = shell.querySelector(".native-video-exit-fullscreen").getBoundingClientRect(); const controls = shell.querySelector(".native-video-controls").getBoundingClientRect(); const captions = shell.querySelector(".native-video-captions").getBoundingClientRect(); const intersects = (left, right) => left.left < right.right && left.right > right.left && left.top < right.bottom && left.bottom > right.top; return { exit: { left: exit.left, right: exit.right, top: exit.top, bottom: exit.bottom }, controls: { top: controls.top, bottom: controls.bottom }, overlapsControls: intersects(exit, controls), overlapsCaptions: intersects(exit, captions), shell: { left: shell.getBoundingClientRect().left, right: shell.getBoundingClientRect().right, top: shell.getBoundingClientRect().top, bottom: shell.getBoundingClientRect().bottom } }; }); assert.equal(fullscreenGeometry.overlapsControls, false, JSON.stringify(fullscreenGeometry)); assert.equal(fullscreenGeometry.overlapsCaptions, false, JSON.stringify(fullscreenGeometry)); assert.ok(fullscreenGeometry.exit.right <= fullscreenGeometry.shell.right && fullscreenGeometry.exit.left > (fullscreenGeometry.shell.left + fullscreenGeometry.shell.right) / 2 && fullscreenGeometry.exit.bottom < fullscreenGeometry.controls.top, JSON.stringify(fullscreenGeometry)); await exitVideoFullscreen.focus(); await page.waitForTimeout(200); assert.equal(await exitVideoFullscreen.evaluate((button) => getComputedStyle(button).opacity), "1");
  await videoPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.externalExit()); await videoPlayer.getByRole("button", { name: "Open fullscreen", exact: true }).waitFor(); await videoPlayer.evaluate(() => globalThis.__hhplmsFullscreenTest.rejectNextRequest()); await videoPlayer.getByRole("button", { name: "Open fullscreen", exact: true }).click(); await videoPlayer.getByText("Fullscreen could not be opened.", { exact: true }).waitFor();
  const beforeVideoClose = await videoPlayer.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await showVideo.click(); await videoPlayer.waitFor({ state: "detached" }); const afterVideoClose = await runtimePresentation.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(afterVideoClose.pauseCount > beforeVideoClose.pauseCount); assert.equal(await viewer.getByText("First private model answer", { exact: true }).count(), 1);
  await viewer.getByRole("button", { name: "Back" }).evaluate((button) => button.click());
  await page.getByRole("button", { name: "Close Review" }).click();
  await page.locator(".editable-hotspot-box").first().click(); await page.getByLabel("Activity").selectOption(imageId); await page.getByLabel("Label").fill("Draft Image hotspot"); await page.getByRole("button", { name: "Review", exact: true }).click(); await page.getByText("Unsaved changes are not included in Review. Save them first.", { exact: true }).waitFor(); await page.getByRole("button", { name: "Close Review" }).click(); await page.locator(".builder-save-state").getByRole("button", { name: "Save", exact: true }).click(); await page.locator(".builder-save-state").getByText("Saved", { exact: true }).waitFor();
  await page.getByRole("button", { name: "Review", exact: true }).click(); viewer = page.frameLocator(".unified-builder-review-dialog iframe"); await viewer.getByRole("button", { name: "Draft Image hotspot" }).waitFor(); const teacherRequestsBeforeImage = viewerRequests.filter((item) => /\/teacher$/.test(item.pathname)).length; await viewer.getByRole("button", { name: "Draft Image hotspot" }).click({ force: true }); const runtimeImage = viewer.locator(".native-image-surface img").first(); await runtimeImage.waitFor(); assert.equal(await viewer.getByText("Production image draft", { exact: true }).count(), 0); assert.equal(await viewer.getByText("Study this image.", { exact: true }).count(), 0); assert.equal(await viewer.getByText(imageContentText, { exact: true }).count(), 0); assert.equal(await viewer.getByLabel("Activity content").count(), 0); assert.equal(await viewer.locator(".native-image-learner-content").count(), 0); const imageFill = await viewer.locator(".native-image-presentation").evaluate((presentation) => { const available = presentation.closest(".native-readable-text-activity-view").getBoundingClientRect(); const outer = presentation.getBoundingClientRect(); const surface = presentation.querySelector(".native-image-surface").getBoundingClientRect(); return { available: { width: available.width, height: available.height }, presentation: { width: outer.width, height: outer.height }, surface: { width: surface.width, height: surface.height } }; }); assert.ok(Math.abs(imageFill.available.width - imageFill.presentation.width) <= 2 && Math.abs(imageFill.available.height - imageFill.presentation.height) <= 2, JSON.stringify(imageFill)); assert.ok(Math.abs(imageFill.surface.width / imageFill.surface.height - 1024 / 582) < .02 && imageFill.surface.width <= imageFill.presentation.width + 2 && imageFill.surface.height <= imageFill.presentation.height + 2, JSON.stringify(imageFill)); const runtimeImageWrapper = runtimeImage.locator(".."); const imagePresentationBeforeHover = await runtimeImageWrapper.evaluate((wrapper) => ({ tagName: wrapper.tagName, disabled: wrapper.hasAttribute("disabled"), hasButtonAncestor: Boolean(wrapper.closest("button")), pointerEvents: getComputedStyle(wrapper).pointerEvents, opacity: getComputedStyle(wrapper).opacity, cursor: getComputedStyle(wrapper).cursor })); assert.deepEqual({ ...imagePresentationBeforeHover, cursor: undefined }, { tagName: "DIV", disabled: false, hasButtonAncestor: false, pointerEvents: "none", opacity: "1", cursor: undefined }); assert.ok(["auto", "default"].includes(imagePresentationBeforeHover.cursor)); const runtimeImageBox = await runtimeImage.boundingBox(); assert.ok(runtimeImageBox); await page.mouse.move(runtimeImageBox.x + runtimeImageBox.width / 2, runtimeImageBox.y + runtimeImageBox.height / 2); await page.waitForTimeout(50); const imagePresentationAfterHover = await runtimeImageWrapper.evaluate((wrapper) => ({ opacity: getComputedStyle(wrapper).opacity, cursor: getComputedStyle(wrapper).cursor })); assert.deepEqual(imagePresentationAfterHover, { opacity: "1", cursor: imagePresentationBeforeHover.cursor }); assert.notEqual(imagePresentationAfterHover.cursor, "not-allowed"); assert.equal(viewerRequests.filter((item) => /\/teacher$/.test(item.pathname)).length, teacherRequestsBeforeImage + 1); assert.ok(viewerRequests.some((item) => item.pathname.includes(`/activities/${imageId}/assets/`)));
  const protectedDraftRequests = viewerRequests.filter((item) => item.pathname.includes("/preview/native-activities/")); assert.ok(protectedDraftRequests.length >= 4); assert.ok(protectedDraftRequests.every((item) => new URLSearchParams(item.search).getAll("previewAuthorization").length === 1 && !item.headers.cookie));
  const installedComponentSlugs = new Set(["ultimate-b2-students-book", "ultimate-b2-workbook", "ultimate-b2-grammar-book"]); assert.ok(viewerExchangeIntents.length > 0); assert.ok(viewerExchangeIntents.every((exchange) => exchange.source.bookSlug === "ultimate-b2" && exchange.intent.bookSlug === "ultimate-b2" && installedComponentSlugs.has(exchange.source.componentSlug) && installedComponentSlugs.has(exchange.intent.componentSlug)), JSON.stringify(viewerExchangeIntents));

  await page.getByRole("button", { name: "Close Review" }).click(); await page.locator(".editable-hotspot-box").first().click(); await page.getByLabel("Activity").selectOption(singleChoiceId); await page.getByLabel("Label").fill("Draft Multiple Choice hotspot"); await page.locator(".builder-save-state").getByRole("button", { name: "Save", exact: true }).click(); await page.locator(".builder-save-state").getByText("Saved", { exact: true }).waitFor(); const teacherRequestsBeforeChoice = viewerRequests.filter((item) => /\/teacher$/.test(item.pathname)).length;
  await page.getByRole("button", { name: "Review", exact: true }).click();
  viewer = page.frameLocator(".unified-builder-review-dialog iframe");
  await viewer.getByRole("button", { name: "Draft Multiple Choice hotspot" }).click({ force: true });
  const teacherChoice = viewer.locator('.native-single-choice-teacher[data-native-single-choice-presentation="visual"]');
  await teacherChoice.waitFor();
  const previousInternal = viewer.getByRole("button", { name: "Previous activity part", exact: true });
  const nextInternal = viewer.getByRole("button", { name: "Next activity part", exact: true });
  const choiceReload = viewer.getByRole("button", { name: "Reload", exact: true });
  const choiceShowAll = viewer.getByRole("button", { name: "Show All", exact: true });
  const choiceShowNext = viewer.getByRole("button", { name: "Show Next", exact: true });
  const savedChoiceDocument = savedVisualChoice.publicDocument;
  const choiceQuestions = savedChoiceDocument.parts[0].interaction.questions;
  const correctChoiceByQuestion = new Map(savedVisualChoice.teacherDocument.parts[0].solution.correctAnswers.map((answer) => [answer.questionId, answer.correctOptionIds?.[0] || answer.correctOptionId]));
  const firstChoiceQuestion = choiceQuestions[0];
  const firstCorrectOption = firstChoiceQuestion.options.find((option) => option.id === correctChoiceByQuestion.get(firstChoiceQuestion.id));
  const firstWrongOption = firstChoiceQuestion.options.find((option) => option.id !== firstCorrectOption.id);
  const firstPanel = savedChoiceDocument.parts[0].interaction.presentation.panels[0];
  const firstWrongHotspotDocument = firstPanel.hotspots.find((hotspot) => hotspot.optionId === firstWrongOption.id);
  const firstWrongHotspot = teacherChoice.getByRole("button", { name: `${firstChoiceQuestion.prompt}: ${firstWrongOption.text}` });
  const firstCorrectHotspot = teacherChoice.getByRole("button", { name: `${firstChoiceQuestion.prompt}: ${firstCorrectOption.text}` });
  assert.equal(await teacherChoice.locator(".native-single-choice-visual-stage").count(), 1);
  assert.equal(await teacherChoice.locator(".native-single-choice-visual-navigation").count(), 0);
  assert.equal(await teacherChoice.getByRole("button", { name: "Next", exact: true }).count(), 0);
  assert.equal(await teacherChoice.getByRole("button", { name: "Show All", exact: true }).count(), 0);
  assert.equal(await teacherChoice.getByText("Panel 1 of 2", { exact: true }).count(), 0);
  assert.equal(await teacherChoice.locator('.native-single-choice-visual[aria-label="Panel 1 of 2"]').count(), 1);
  assert.equal(await previousInternal.isDisabled(), true);
  assert.equal(await nextInternal.isDisabled(), false);
  const firstBackground = teacherChoice.locator(".native-single-choice-visual-stage img").first();
  await firstBackground.waitFor();
  assert.ok(new URL(await firstBackground.getAttribute("src"), origin).pathname.includes(`/activities/${singleChoiceId}/assets/`));
  const hotspotGeometry = await firstWrongHotspot.evaluate((element) => ({ left: Number.parseFloat(element.style.left), top: Number.parseFloat(element.style.top), width: Number.parseFloat(element.style.width), height: Number.parseFloat(element.style.height) }));
  assert.ok(Math.abs(hotspotGeometry.left - firstWrongHotspotDocument.area.x / firstPanel.sourceWidth * 100) < .001);
  assert.ok(Math.abs(hotspotGeometry.top - firstWrongHotspotDocument.area.y / firstPanel.sourceHeight * 100) < .001);
  assert.ok(Math.abs(hotspotGeometry.width - firstWrongHotspotDocument.area.width / firstPanel.sourceWidth * 100) < .001);
  assert.ok(Math.abs(hotspotGeometry.height - firstWrongHotspotDocument.area.height / firstPanel.sourceHeight * 100) < .001);
  const openingAudioCue = teacherChoice.getByRole("button", { name: "Listen to opening excerpt", exact: true });
  const secondAudioCue = teacherChoice.getByRole("button", { name: "Listen to second excerpt", exact: true });
  const renderedHotspotOverlap = await teacherChoice.evaluate((surface) => { const overlaps = (left, right) => left.left < right.right && left.right > right.left && left.top < right.bottom && left.bottom > right.top; const answers = [...surface.querySelectorAll(".native-single-choice-hotspot")].map((element) => element.getBoundingClientRect()); const audio = [...surface.querySelectorAll(".native-audio-text-hotspot")].map((element) => element.getBoundingClientRect()); return answers.flatMap((answer, answerIndex) => audio.flatMap((cue, audioIndex) => overlaps(answer, cue) ? [{ answerIndex, audioIndex }] : [])); }); assert.deepEqual(renderedHotspotOverlap, [], "Rendered answer and audio hotspot fixtures must remain independently clickable");
  assert.match(await openingAudioCue.locator("img").getAttribute("src"), /audio-text-hotspot-active/);
  assert.equal(await teacherChoice.getByRole("button", { name: "Open panel two excerpt", exact: true }).count(), 0);
  const openingAudioDocument = savedChoiceDocument.audioTextHotspots.hotspots.find((hotspot) => hotspot.label === "Listen to opening excerpt");
  let inactiveRegistration = null;
  for (const viewport of [{ width: 1440, height: 900 }, { width: 1180, height: 760 }]) {
    await page.setViewportSize(viewport);
    const centered = await teacherChoice.evaluate((surface) => { const bounds = (element) => { const rect = element.getBoundingClientRect(); return { left: rect.left, right: rect.right, top: rect.top, bottom: rect.bottom, width: rect.width, height: rect.height, centerX: rect.left + rect.width / 2, centerY: rect.top + rect.height / 2 }; }; const availableElement = surface.closest(".native-readable-text-activity-view"); const stageElement = surface.querySelector(".native-single-choice-visual-stage"); const stageStyle = getComputedStyle(stageElement); return { available: bounds(availableElement), panel: bounds(surface.querySelector(".native-single-choice-visual-panel")), stage: bounds(stageElement), image: bounds(stageElement.querySelector(":scope > img")), answer: bounds(surface.querySelector(".native-single-choice-hotspot")), audio: bounds(surface.querySelector(".native-audio-text-hotspot")), chrome: { background: stageStyle.backgroundColor, border: stageStyle.borderStyle, radius: stageStyle.borderRadius, shadow: stageStyle.boxShadow }, overflow: { activity: getComputedStyle(availableElement).overflow, bodyX: document.body.scrollWidth - innerWidth, bodyY: document.body.scrollHeight - innerHeight } }; });
    if (viewport.width === 1440) inactiveRegistration = centered;
    assert.ok(Math.abs(centered.stage.centerX - centered.panel.centerX) <= 2, JSON.stringify({ viewport, centered }));
    assert.ok(Math.abs(centered.stage.centerY - centered.panel.centerY) <= 2, JSON.stringify({ viewport, centered }));
    assert.ok(centered.stage.left >= centered.panel.left - 2 && centered.stage.top >= centered.panel.top - 2 && centered.stage.right <= centered.panel.right + 2 && centered.stage.bottom <= centered.panel.bottom + 2, JSON.stringify({ viewport, centered }));
    assert.ok(Math.abs(centered.stage.width / centered.stage.height - firstPanel.sourceWidth / firstPanel.sourceHeight) < .02, JSON.stringify({ viewport, centered }));
    assert.ok(Math.abs(centered.stage.width - centered.available.width) <= 2, JSON.stringify({ viewport, centered }));
    assert.ok(Math.abs(centered.stage.height - centered.stage.width * 291 / 1024) <= 2, JSON.stringify({ viewport, centered }));
    assert.ok(Math.abs(centered.stage.centerY - centered.available.centerY) <= 2, JSON.stringify({ viewport, centered }));
    assert.ok(Math.abs(centered.image.width / centered.image.height - firstPanel.sourceWidth / firstPanel.sourceHeight) < .02, JSON.stringify({ viewport, centered }));
    assert.deepEqual({ left: Math.round(centered.image.left), top: Math.round(centered.image.top), right: Math.round(centered.image.right), bottom: Math.round(centered.image.bottom) }, { left: Math.round(centered.stage.left), top: Math.round(centered.stage.top), right: Math.round(centered.stage.right), bottom: Math.round(centered.stage.bottom) });
    assert.ok(Math.abs((centered.answer.left - centered.stage.left) / centered.stage.width - firstWrongHotspotDocument.area.x / firstPanel.sourceWidth) < .002);
    assert.ok(Math.abs((centered.answer.top - centered.stage.top) / centered.stage.height - firstWrongHotspotDocument.area.y / firstPanel.sourceHeight) < .002);
    assert.ok(Math.abs((centered.audio.left - centered.stage.left) / centered.stage.width - openingAudioDocument.activityArea.x / firstPanel.sourceWidth) < .002, JSON.stringify({ viewport, centered, openingAudioDocument }));
    assert.ok(Math.abs((centered.audio.top - centered.stage.top) / centered.stage.height - openingAudioDocument.activityArea.y / firstPanel.sourceHeight) < .002, JSON.stringify({ viewport, centered, openingAudioDocument }));
    assert.deepEqual(centered.chrome, { background: "rgba(0, 0, 0, 0)", border: "none", radius: "0px", shadow: "none" });
    assert.equal(centered.overflow.activity, "hidden"); assert.ok(centered.overflow.bodyX <= 1 && centered.overflow.bodyY <= 1, JSON.stringify({ viewport, centered }));
  }
  await page.screenshot({ path: path.join(screenshotRoot, "single-choice-viewer-width-1440.png"), fullPage: true });
  await page.setViewportSize({ width: 1440, height: 900 });
  await firstWrongHotspot.click(); assert.equal(await firstWrongHotspot.getAttribute("data-answer-state"), "incorrect");
  await openingAudioCue.focus(); await openingAudioCue.press("Enter"); const focusView = viewer.locator(".native-audio-text-focus"); await focusView.waitFor(); assert.equal(await firstWrongHotspot.getAttribute("data-answer-state"), "incorrect");
  const expectedOpeningFocus = savedChoiceDocument.audioTextHotspots.hotspots.find((hotspot) => hotspot.label === "Listen to opening excerpt").readableFocusArea;
  assert.ok(Math.abs(expectedOpeningFocus.width / expectedOpeningFocus.height - 1024 / 291) < .03, JSON.stringify(expectedOpeningFocus));
  assert.equal(await focusView.locator("svg").getAttribute("viewBox"), `${expectedOpeningFocus.x} ${expectedOpeningFocus.y} ${expectedOpeningFocus.width} ${expectedOpeningFocus.height}`);
  assert.equal(await focusView.locator(".native-audio-text-focus-crop").getAttribute("data-highlight-color"), "green");
  const runtimeHighlightStyle = await focusView.locator(".native-audio-text-focus-highlight").evaluate((element) => ({ left: Number.parseFloat(element.style.left), top: Number.parseFloat(element.style.top), width: Number.parseFloat(element.style.width), height: Number.parseFloat(element.style.height) })); const expectedRuntimeHighlight = savedChoiceDocument.audioTextHotspots.hotspots.find((hotspot) => hotspot.label === "Listen to opening excerpt").readableHighlightArea; assert.ok(Math.abs(runtimeHighlightStyle.left - (expectedRuntimeHighlight.x - expectedOpeningFocus.x) / expectedOpeningFocus.width * 100) < .002); assert.ok(Math.abs(runtimeHighlightStyle.top - (expectedRuntimeHighlight.y - expectedOpeningFocus.y) / expectedOpeningFocus.height * 100) < .002);
  assert.equal(await focusView.locator("image").getAttribute("preserveAspectRatio"), "xMidYMid meet");
  assert.equal(await focusView.getByText("Listen to opening excerpt", { exact: true }).count(), 0);
  assert.equal(await focusView.getByRole("button", { name: /Close/i }).count(), 0);
  assert.equal(await focusView.locator("audio[controls]").count(), 0);
  assert.equal(await focusView.locator("audio[hidden]").count(), 1);
  for (const viewport of [{ width: 1440, height: 900 }, { width: 1180, height: 760 }]) {
    await page.setViewportSize(viewport);
    const focusGeometry = await viewer.locator(".native-readable-text-presentation").evaluate((presentation) => { const bounds = (element) => { const rect = element.getBoundingClientRect(); return { left: rect.left, right: rect.right, top: rect.top, bottom: rect.bottom, width: rect.width, height: rect.height }; }; const stageElement = presentation.querySelector(".native-single-choice-visual-stage"); const answerElement = presentation.querySelector(".native-single-choice-hotspot"); const audioElement = presentation.querySelector(".native-audio-text-hotspot"); const answer = bounds(answerElement); const hit = document.elementFromPoint(answer.left + answer.width / 2, answer.top + answer.height / 2); const divider = getComputedStyle(presentation, "::after"); return { outer: bounds(presentation), top: bounds(presentation.querySelector(".native-audio-text-focus-slot")), focus: bounds(presentation.querySelector(".native-audio-text-focus")), crop: bounds(presentation.querySelector(".native-audio-text-focus-crop")), svg: bounds(presentation.querySelector(".native-audio-text-focus-crop svg")), highlight: bounds(presentation.querySelector(".native-audio-text-focus-highlight")), bottom: bounds(presentation.querySelector(".native-readable-text-activity-view")), stage: bounds(stageElement), image: bounds(stageElement.querySelector(":scope > img")), answer, audio: bounds(audioElement), answerHit: hit === answerElement || answerElement.contains(hit), divider: { height: divider.height, background: divider.backgroundColor, pointerEvents: divider.pointerEvents, top: divider.top, transform: divider.transform }, overflow: getComputedStyle(presentation).overflow }; });
    const tolerance = 2;
    assert.ok(Math.abs(focusGeometry.top.left - focusGeometry.outer.left) <= tolerance && Math.abs(focusGeometry.top.right - focusGeometry.outer.right) <= tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(Math.abs(focusGeometry.crop.left - focusGeometry.outer.left) <= tolerance && Math.abs(focusGeometry.crop.right - focusGeometry.outer.right) <= tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(Math.abs(focusGeometry.crop.width / focusGeometry.crop.height - 1024 / 291) < .02, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(Math.abs(focusGeometry.top.height - focusGeometry.outer.height / 2) <= tolerance && Math.abs(focusGeometry.bottom.height - focusGeometry.outer.height / 2) <= tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(focusGeometry.top.top >= focusGeometry.outer.top - tolerance && focusGeometry.top.bottom <= focusGeometry.outer.top + focusGeometry.outer.height / 2 + tolerance && focusGeometry.bottom.top >= focusGeometry.outer.top + focusGeometry.outer.height / 2 - tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(focusGeometry.crop.left >= focusGeometry.outer.left - tolerance && focusGeometry.crop.right <= focusGeometry.outer.right + tolerance && focusGeometry.crop.top >= focusGeometry.outer.top - tolerance && focusGeometry.crop.bottom <= focusGeometry.top.bottom + tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(Math.abs(focusGeometry.svg.left - focusGeometry.crop.left) <= tolerance && Math.abs(focusGeometry.svg.right - focusGeometry.crop.right) <= tolerance && Math.abs(focusGeometry.svg.top - focusGeometry.crop.top) <= tolerance && Math.abs(focusGeometry.svg.bottom - focusGeometry.crop.bottom) <= tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(focusGeometry.highlight.left >= focusGeometry.crop.left - tolerance && focusGeometry.highlight.right <= focusGeometry.crop.right + tolerance && focusGeometry.highlight.top >= focusGeometry.crop.top - tolerance && focusGeometry.highlight.bottom <= focusGeometry.crop.bottom + tolerance && focusGeometry.highlight.width < focusGeometry.crop.width && focusGeometry.highlight.height < focusGeometry.crop.height, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(focusGeometry.stage.width > 0 && focusGeometry.stage.height > 0 && focusGeometry.stage.left >= focusGeometry.bottom.left - tolerance && focusGeometry.stage.right <= focusGeometry.bottom.right + tolerance && focusGeometry.stage.top >= focusGeometry.bottom.top - tolerance && focusGeometry.stage.bottom <= focusGeometry.bottom.bottom + tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.ok(Math.abs(focusGeometry.stage.width - focusGeometry.bottom.width) <= tolerance && Math.abs(focusGeometry.stage.height - focusGeometry.bottom.height) <= tolerance, JSON.stringify({ viewport, focusGeometry }));
    assert.deepEqual({ left: Math.round(focusGeometry.image.left), top: Math.round(focusGeometry.image.top), right: Math.round(focusGeometry.image.right), bottom: Math.round(focusGeometry.image.bottom) }, { left: Math.round(focusGeometry.stage.left), top: Math.round(focusGeometry.stage.top), right: Math.round(focusGeometry.stage.right), bottom: Math.round(focusGeometry.stage.bottom) });
    assert.equal(focusGeometry.answerHit, true); assert.equal(focusGeometry.divider.height, "7px"); assert.equal(focusGeometry.divider.background, "rgb(0, 0, 0)"); assert.equal(focusGeometry.divider.pointerEvents, "none");
    if (viewport.width === 1440) { for (const item of ["answer", "audio"]) { const inactiveRelative = { left: (inactiveRegistration[item].left - inactiveRegistration.stage.left) / inactiveRegistration.stage.width, top: (inactiveRegistration[item].top - inactiveRegistration.stage.top) / inactiveRegistration.stage.height, width: inactiveRegistration[item].width / inactiveRegistration.stage.width, height: inactiveRegistration[item].height / inactiveRegistration.stage.height }; const focusRelative = { left: (focusGeometry[item].left - focusGeometry.stage.left) / focusGeometry.stage.width, top: (focusGeometry[item].top - focusGeometry.stage.top) / focusGeometry.stage.height, width: focusGeometry[item].width / focusGeometry.stage.width, height: focusGeometry[item].height / focusGeometry.stage.height }; for (const key of ["left", "top", "width", "height"]) assert.ok(Math.abs(focusRelative[key] - inactiveRelative[key]) <= .003, JSON.stringify({ item, inactiveRelative, focusRelative, inactiveRegistration, focusGeometry })); } assert.ok(Math.abs(focusGeometry.stage.top - inactiveRegistration.stage.top) > 20, JSON.stringify({ inactiveRegistration, focusGeometry })); }
    assert.equal(focusGeometry.overflow, "hidden");
  }
  await page.setViewportSize({ width: 1440, height: 900 });
  await page.screenshot({ path: path.join(screenshotRoot, "readable-focus-green-1440.png"), fullPage: true });
  const splitGeometry = await viewer.locator(".native-readable-text-presentation").evaluate((presentation) => { const top = presentation.querySelector(".native-audio-text-focus-slot").getBoundingClientRect(); const bottom = presentation.querySelector(".native-readable-text-activity-view").getBoundingClientRect(); const outer = presentation.getBoundingClientRect(); return { top: { y: top.y, bottom: top.bottom, height: top.height }, bottom: { y: bottom.y, bottom: bottom.bottom, height: bottom.height }, outer: { y: outer.y, bottom: outer.bottom, height: outer.height, overflow: getComputedStyle(presentation).overflow } }; });
  assert.ok(splitGeometry.top.height / splitGeometry.outer.height > .4 && splitGeometry.top.height / splitGeometry.outer.height < .6, JSON.stringify(splitGeometry)); assert.ok(splitGeometry.bottom.height / splitGeometry.outer.height > .4 && splitGeometry.bottom.height / splitGeometry.outer.height < .6, JSON.stringify(splitGeometry)); assert.ok(splitGeometry.top.y < splitGeometry.bottom.y); assert.equal(splitGeometry.outer.overflow, "hidden"); assert.ok(splitGeometry.top.y >= splitGeometry.outer.y - 2 && splitGeometry.bottom.bottom <= splitGeometry.outer.bottom + 2, JSON.stringify(splitGeometry));
  const firstMediaSnapshot = await focusView.locator("audio").evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(firstMediaSnapshot.playCount >= 1); assert.match(firstMediaSnapshot.lastSource, new RegExp(`/activities/${singleChoiceId}/assets/`));
  const firstAudioSource = await focusView.locator("audio").getAttribute("src"); const splitCueGeometry = await secondAudioCue.evaluate((cue) => { const rect = cue.getBoundingClientRect(); const stage = cue.closest(".native-single-choice-visual-stage").getBoundingClientRect(); const panel = cue.closest(".native-single-choice-visual-panel").getBoundingClientRect(); const hit = document.elementFromPoint(rect.left + rect.width / 2, rect.top + rect.height / 2); return { cue: { left: rect.left, top: rect.top, right: rect.right, bottom: rect.bottom }, stage: { left: stage.left, top: stage.top, right: stage.right, bottom: stage.bottom, width: stage.width, height: stage.height, centerX: stage.left + stage.width / 2, centerY: stage.top + stage.height / 2 }, panel: { centerX: panel.left + panel.width / 2, centerY: panel.top + panel.height / 2 }, viewport: { width: innerWidth, height: innerHeight }, hitTarget: hit === cue || cue.contains(hit) }; }); assert.ok(splitCueGeometry.cue.left >= 0 && splitCueGeometry.cue.top >= 0 && splitCueGeometry.cue.right <= splitCueGeometry.viewport.width && splitCueGeometry.cue.bottom <= splitCueGeometry.viewport.height && splitCueGeometry.hitTarget, JSON.stringify(splitCueGeometry)); assert.ok(Math.abs(splitCueGeometry.stage.centerX - splitCueGeometry.panel.centerX) <= 2 && Math.abs(splitCueGeometry.stage.centerY - splitCueGeometry.panel.centerY) <= 2, JSON.stringify(splitCueGeometry)); assert.ok(Math.abs(splitCueGeometry.stage.width / splitCueGeometry.stage.height - firstPanel.sourceWidth / firstPanel.sourceHeight) < .02); await secondAudioCue.evaluate((button) => button.click()); await viewer.getByLabel("Focused readable text: Listen to second excerpt").waitFor(); const secondAudioSource = await focusView.locator("audio").getAttribute("src"); assert.notEqual(secondAudioSource, firstAudioSource); const switchedMediaSnapshot = await focusView.locator("audio").evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(switchedMediaSnapshot.playCount > firstMediaSnapshot.playCount); assert.ok(switchedMediaSnapshot.pauseCount >= firstMediaSnapshot.pauseCount);
  await secondAudioCue.evaluate((button) => button.click()); await focusView.waitFor({ state: "detached" }); assert.equal(await firstWrongHotspot.getAttribute("data-answer-state"), "incorrect"); const restoredRegistration = await teacherChoice.evaluate((surface) => { const stage = surface.querySelector(".native-single-choice-visual-stage").getBoundingClientRect(); const answer = surface.querySelector(".native-single-choice-hotspot").getBoundingClientRect(); return { stage: { left: stage.left, top: stage.top, width: stage.width, height: stage.height }, answer: { left: answer.left, top: answer.top, width: answer.width, height: answer.height } }; }); for (const target of ["stage", "answer"]) for (const key of ["left", "top", "width", "height"]) assert.ok(Math.abs(restoredRegistration[target][key] - inactiveRegistration[target][key]) <= 2, JSON.stringify({ restoredRegistration, inactiveRegistration }));
  await openingAudioCue.click(); await focusView.waitFor(); const beforeEscapeMedia = await focusView.locator("audio").evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await focusView.press("Escape"); await focusView.waitFor({ state: "detached" }); const afterEscapeMedia = await teacherChoice.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(afterEscapeMedia.pauseCount > beforeEscapeMedia.pauseCount);
  await openingAudioCue.focus(); await openingAudioCue.press("Enter"); await focusView.waitFor(); await nextInternal.click(); await focusView.waitFor({ state: "detached" }); const panelTwoReadableCue = teacherChoice.getByRole("button", { name: "Open panel two excerpt", exact: true }); await panelTwoReadableCue.waitFor(); const readableCueArtwork = await panelTwoReadableCue.locator("img").getAttribute("src"); assert.match(readableCueArtwork, /^data:image\/svg\+xml/); assert.doesNotMatch(readableCueArtwork, /audio-text-hotspot/); await panelTwoReadableCue.click(); await viewer.getByLabel("Focused readable text: Open panel two excerpt").waitFor(); assert.equal(await focusView.locator("audio").count(), 0, "the no-MP3 runtime focus creates no audio element"); await panelTwoReadableCue.click(); await focusView.waitFor({ state: "detached" }); assert.equal(await teacherChoice.getByRole("button", { name: "Listen to opening excerpt", exact: true }).count(), 0); await previousInternal.click(); await firstWrongHotspot.waitFor(); assert.equal(await firstWrongHotspot.getAttribute("data-answer-state"), "incorrect");
  await firstCorrectHotspot.click(); assert.equal(await firstCorrectHotspot.getAttribute("data-answer-state"), "correct"); assert.equal(await firstWrongHotspot.getAttribute("data-answer-state"), "incorrect"); assert.equal(await firstCorrectHotspot.isDisabled(), true);
  await nextInternal.click(); await teacherChoice.getByRole("button", { name: /Who calculates the score\?:/ }).first().waitFor();
  assert.equal(await previousInternal.isDisabled(), false);
  assert.equal(await nextInternal.isDisabled(), true);
  const secondChoiceQuestion = choiceQuestions[1];
  const secondCorrectOption = secondChoiceQuestion.options.find((option) => option.id === correctChoiceByQuestion.get(secondChoiceQuestion.id));
  const secondWrongOption = secondChoiceQuestion.options.find((option) => option.id !== secondCorrectOption.id);
  const secondWrongHotspot = teacherChoice.getByRole("button", { name: `${secondChoiceQuestion.prompt}: ${secondWrongOption.text}` });
  const secondCorrectHotspot = teacherChoice.getByRole("button", { name: `${secondChoiceQuestion.prompt}: ${secondCorrectOption.text}` });
  await secondWrongHotspot.click(); await secondCorrectHotspot.click(); assert.equal(await secondWrongHotspot.getAttribute("data-answer-state"), "incorrect"); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct");
  const choiceShowText = viewer.locator(".teacher-book-navigation-context--show-text"); await choiceShowText.waitFor(); await panelTwoReadableCue.click(); await focusView.waitFor(); await choiceShowText.click(); await focusView.waitFor({ state: "detached" }); await viewer.locator(".native-readable-text-scroll").waitFor(); assert.equal(await viewer.locator(".native-single-choice-visual-stage:visible").count(), 0); assert.equal(await viewer.locator(".native-readable-text-scroll img:visible").count(), 1); assert.equal(await choiceShowText.getAttribute("aria-pressed"), "true"); await choiceShowText.click(); await secondWrongHotspot.waitFor(); assert.equal(await secondWrongHotspot.getAttribute("data-answer-state"), "incorrect"); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct");
  const choiceVideo = viewer.locator(".teacher-book-navigation-context--video"); await choiceVideo.waitFor(); await choiceVideo.click(); const choiceVideoPlayer = viewer.locator(".native-video-player-shell"); await choiceVideoPlayer.waitFor(); await choiceVideoPlayer.getByRole("button", { name: "Play video", exact: true }).first().click(); await choiceVideo.click(); await choiceVideoPlayer.waitFor({ state: "detached" }); await secondWrongHotspot.waitFor(); assert.equal(await secondWrongHotspot.getAttribute("data-answer-state"), "incorrect"); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct");
  await choiceShowText.click(); await viewer.locator(".native-readable-text-scroll").waitFor(); assert.equal(await previousInternal.isDisabled(), true); assert.equal(await nextInternal.isDisabled(), true);
  await choiceShowText.click(); await viewer.locator(".native-readable-text-scroll").waitFor({ state: "detached" }); await previousInternal.click(); await firstBackground.waitFor(); await openingAudioCue.focus(); await openingAudioCue.press("Enter"); await focusView.waitFor(); assert.equal(await choiceReload.isDisabled(), false); await choiceReload.click(); await focusView.waitFor({ state: "detached" }); await firstBackground.waitFor(); assert.equal(await previousInternal.isDisabled(), true); assert.equal(await nextInternal.isDisabled(), false); assert.equal(await teacherChoice.locator("[data-answer-state]").count(), 0);
  await choiceShowNext.click(); await page.waitForTimeout(25); assert.equal(await firstCorrectHotspot.getAttribute("data-answer-state"), "correct"); assert.equal(await firstWrongHotspot.getAttribute("data-answer-state"), null); assert.equal(await choiceShowNext.isDisabled(), false);
  await choiceShowNext.click(); await secondCorrectHotspot.waitFor(); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct"); assert.equal(await choiceShowNext.isDisabled(), true); assert.equal(await nextInternal.isDisabled(), true);
  await previousInternal.click(); await firstCorrectHotspot.waitFor(); assert.equal(await firstCorrectHotspot.getAttribute("data-answer-state"), "correct"); await nextInternal.click(); await secondCorrectHotspot.waitFor(); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct");
  await choiceReload.click(); await firstBackground.waitFor(); assert.equal(await teacherChoice.locator("[data-answer-state]").count(), 0); await nextInternal.click(); await secondWrongHotspot.waitFor(); assert.equal(await teacherChoice.locator("[data-answer-state]").count(), 0); await previousInternal.click(); await firstBackground.waitFor();
  await choiceShowAll.click(); await teacherChoice.locator('button[data-answer-state="correct"]').first().waitFor(); assert.equal(await firstCorrectHotspot.getAttribute("data-answer-state"), "correct"); assert.equal(await choiceShowAll.isDisabled(), true); await nextInternal.click(); await secondCorrectHotspot.waitFor(); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct");
  await choiceShowText.click(); await viewer.locator(".native-readable-text-scroll").waitFor(); await choiceShowText.click(); await secondCorrectHotspot.waitFor(); assert.equal(await secondCorrectHotspot.getAttribute("data-answer-state"), "correct");
  const visualBackgroundAssetIds = [...new Set(savedChoiceDocument.parts[0].interaction.presentation.panels.map((panel) => savedChoiceDocument.assets.find((asset) => asset.slot === panel.backgroundAssetSlot)?.assetId).filter(Boolean))];
  assert.ok(visualBackgroundAssetIds.every((assetId) => viewerRequests.some((item) => item.pathname.includes(`/activities/${singleChoiceId}/assets/${assetId}`))));
  assert.equal(await viewer.getByText("The pinned release — Correct answer", { exact: true }).count(), 0); assert.equal(await viewer.getByText("Teacher answers are unavailable.", { exact: true }).count(), 0); assert.equal(await viewer.getByText("Persisted browser multiple choice", { exact: true }).count(), 0); assert.equal(await viewer.getByText("Choose one answer for each question.", { exact: true }).count(), 0); assert.equal(viewerRequests.filter((item) => /\/teacher$/.test(item.pathname)).length, teacherRequestsBeforeChoice + 3, "initial Teacher document plus two generic Reload remounts");
  await page.getByRole("button", { name: "Close Review" }).click(); await page.locator(".editable-hotspot-box").first().click(); await page.getByLabel("Activity").selectOption(onePanelChoiceId); await page.getByLabel("Label").fill("Draft One Panel Choice hotspot"); await page.locator(".builder-save-state").getByRole("button", { name: "Save", exact: true }).click(); await page.locator(".builder-save-state").getByText("Saved", { exact: true }).waitFor(); await page.getByRole("button", { name: "Review", exact: true }).click(); viewer = page.frameLocator(".unified-builder-review-dialog iframe"); await viewer.getByRole("button", { name: "Draft One Panel Choice hotspot" }).click({ force: true }); await viewer.locator('.native-single-choice-teacher[data-native-single-choice-presentation="visual"]').waitFor(); assert.equal(await viewer.getByRole("button", { name: "Previous activity part", exact: true }).count(), 0); assert.equal(await viewer.getByRole("button", { name: "Next activity part", exact: true }).count(), 0); assert.equal(await viewer.locator(".native-single-choice-visual-navigation").count(), 0); await page.getByRole("button", { name: "Close Review" }).click(); await page.locator(".editable-hotspot-box").first().click(); await page.getByLabel("Activity").selectOption(imageId); await page.getByLabel("Label").fill("Draft Image hotspot"); await page.locator(".builder-save-state").getByRole("button", { name: "Save", exact: true }).click(); await page.locator(".builder-save-state").getByText("Saved", { exact: true }).waitFor();
  await page.goto(`${origin}/#/books/ultimate-b2/ui`, { waitUntil: "domcontentloaded" }); await page.getByRole("heading", { name: "Page UI Controller" }).waitFor(); assert.equal(await page.getByRole("button", { name: "Review", exact: true }).count(), 1); assert.equal(await page.locator(".hosted-viewer-preview iframe").count(), 1); await page.getByRole("button", { name: "Review", exact: true }).click(); await page.getByRole("heading", { name: "Review · Saved Draft", exact: true }).waitFor(); const uiReviewIframe = page.locator(".unified-builder-review-dialog iframe"); const uiFrameUrlValue = await page.waitForFunction((iframe) => { const url = new URL(iframe.src); return url.searchParams.get("view") === "library" ? url.href : false; }, await uiReviewIframe.elementHandle()).then((handle) => handle.jsonValue()); const uiFrameUrl = new URL(uiFrameUrlValue); assert.equal(uiFrameUrl.searchParams.get("view"), "library"); assert.equal(uiFrameUrl.searchParams.has("pageId"), false); assert.equal(authorizationIntents.at(-1).intent.view, "library"); assert.equal(authorizationIntents.at(-1).intent.pageId, null); viewer = page.frameLocator(".unified-builder-review-dialog iframe"); await viewer.getByRole("button", { name: "Draft Image hotspot" }).waitFor(); await viewer.getByRole("button", { name: "Draft Image hotspot" }).click({ force: true }); await viewer.locator(".native-image-surface img").first().waitFor(); assert.equal(await viewer.getByText("Production image draft", { exact: true }).count(), 0); assert.equal(await viewer.getByText(imageContentText, { exact: true }).count(), 0); assert.equal(await viewer.locator(".native-image-learner-content").count(), 0); await page.getByRole("button", { name: "Close Review" }).click();
  await page.getByRole("button", { name: "Review", exact: true }).click(); await page.getByRole("heading", { name: "Review · Saved Draft", exact: true }).waitFor();
  const pageAuthorizationCount = authorizationIntents.length;
  const [pagePlayer] = await Promise.all([
    context.waitForEvent("page"),
    page.getByRole("link", { name: "Open Player", exact: true }).click(),
  ]);
  await pagePlayer.getByRole("heading", { name: "Player Review", exact: true }).waitFor();
  assert.equal(new URL(pagePlayer.url()).origin, origin);
  assert.equal(new URL(pagePlayer.url()).hash, "#/books/ultimate-b2/components/ultimate-b2-students-book/review?view=library");
  assert.doesNotMatch(pagePlayer.url(), /previewAuthorization|token|secret/i);
  await waitForHostedViewerFrame(pagePlayer.locator(".hosted-builder-review-page iframe"));
  const pagePlayerFrameUrl = new URL(await pagePlayer.locator(".hosted-builder-review-page iframe").getAttribute("src"));
  assert.equal(pagePlayerFrameUrl.origin, "https://hhplms-viewer.netlify.app");
  assert.equal(pagePlayerFrameUrl.searchParams.get("view"), "library");
  assert.equal(pagePlayerFrameUrl.searchParams.has("pageId"), false);
  assert.ok(authorizationIntents.length > pageAuthorizationCount);
  const pagePlayerViewer = pagePlayer.frameLocator(".hosted-builder-review-page iframe");
  await openStudentsUnitOnePage(pagePlayerViewer);
  await pagePlayerViewer.getByRole("button", { name: "Draft Image hotspot" }).waitFor();
  await pagePlayer.close(); await page.getByRole("button", { name: "Close Review" }).click();
  await page.goto(`${origin}/#/books/ultimate-b2/components/ultimate-b2-students-book/publication`, { waitUntil: "domcontentloaded" }); await page.getByRole("heading", { name: "Publication", exact: true }).waitFor(); assert.equal(await page.getByRole("button", { name: "Review", exact: true }).count(), 1); assert.equal(await page.locator(".hosted-viewer-preview iframe").count(), 1); await page.getByRole("button", { name: "Review", exact: true }).click(); await page.getByRole("heading", { name: "Review · Saved Draft", exact: true }).waitFor(); assert.equal(await page.getByRole("button", { name: "No release prepared" }).isDisabled(), true); await page.locator(".unified-builder-review-dialog iframe").waitFor(); assert.equal(await page.locator(".unified-builder-review-dialog iframe").count(), 1); await page.getByRole("button", { name: "Close Review" }).click(); assert.equal(await page.locator(".unified-builder-review-dialog iframe").count(), 1);

  hotspotManifest = structuredClone(hotspotManifest);
  hotspotManifest.pages["ub2-sb-unit-1-part-1"] ||= [];
  hotspotManifest.pages["ub2-sb-unit-1-part-2"] ||= [];
  hotspotManifest.pages["ub2-sb-unit-1-part-1"].push({ id: "browser-delete-one", unitNumber: 1, pageId: "ub2-sb-unit-1-part-1", pageNumber: 5, left: 2, top: 2, width: 8, height: 8, label: "Retired launch one", actionType: "normalized_activity", activityKey: singleChoiceId });
  hotspotManifest.pages["ub2-sb-unit-1-part-2"].push({ id: "browser-delete-two", unitNumber: 1, pageId: "ub2-sb-unit-1-part-2", pageNumber: 6, left: 2, top: 2, width: 8, height: 8, label: "Retired launch two", actionType: "normalized_activity", activityKey: singleChoiceId });
  hotspotRevision += 1;
  await page.locator('.hosted-builder-tool-tabs a[href$="/activities"]').click(); await page.getByRole("heading", { name: "Activity authoring" }).waitFor();
  await exerciseDisposableCanonicalPageNativeDeletion(page, () => { ({ indexRevision, hotspotRevision } = addDisposableCanonicalPageNative({ nativeDocuments, nativeIndex, hotspotManifest, indexRevision, hotspotRevision })); }, () => ({ nativeIndex, nativeDocuments, hotspotManifest }));
  await page.getByRole("button", { name: new RegExp(singleChoiceId) }).click(); await page.getByRole("button", { name: "Delete Activity", exact: true }).click();
  const deleteDialog = page.getByRole("dialog", { name: "Delete activity?" }); await deleteDialog.waitFor(); await deleteDialog.getByText("Persisted browser multiple choice", { exact: true }).waitFor(); await deleteDialog.getByText(singleChoiceId, { exact: true }).waitFor(); await deleteDialog.getByText(/every Students Book page hotspot/).waitFor(); await deleteDialog.getByText(/revision history.*immutable historical releases/).waitFor(); await deleteDialog.getByRole("button", { name: "Cancel" }).click(); await deleteDialog.waitFor({ state: "detached" });
  await page.getByLabel("Activity title").fill("Unsaved title before delete"); await page.locator(".native-activity-identity-actions").getByRole("button", { name: "Delete Activity", exact: true }).click(); await deleteDialog.getByText(/Unsaved changes in this editor will be discarded/).waitFor(); await deleteDialog.getByRole("button", { name: "Delete Activity", exact: true }).click(); await deleteDialog.waitFor({ state: "detached" });
  assert.equal(nativeIndex.activities.some((entry) => entry.activityId === singleChoiceId), false); assert.ok(nativeDocuments.has(singleChoiceId)); assert.equal(Object.values(hotspotManifest.pages).flat().some((hotspot) => hotspot.activityKey === singleChoiceId), false); assert.equal(await page.getByRole("button", { name: new RegExp(singleChoiceId) }).count(), 0);
  const retiredPreview = await context.newPage(); const retiredPreviewResponse = await retiredPreview.goto(`https://hhplms-viewer.netlify.app/preview/native-activities/books/ultimate-b2/components/ultimate-b2-students-book/activities/${singleChoiceId}/public?previewAuthorization=${encodeURIComponent(previewToken)}`); assert.equal(retiredPreviewResponse.status(), 404); await retiredPreview.close();
  await page.locator('.hosted-builder-tool-tabs a[href$="/hotspots"]').click(); await page.getByRole("heading", { name: "Students Book hotspot builder" }).waitFor(); assert.equal(await page.getByText("Retired launch one", { exact: true }).count(), 0); await page.locator(".editable-hotspot-box").first().click(); assert.equal(await page.getByLabel("Activity").locator(`option[value="${singleChoiceId}"]`).count(), 0);
  await page.locator('.hosted-builder-tool-tabs a[href$="/activities"]').click(); await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /Multiple Choice/ }).check(); await page.getByLabel(/Initial title/).fill("Post-delete identity"); await page.getByRole("button", { name: "Create activity" }).click(); await page.getByRole("heading", { name: "Post-delete identity" }).first().waitFor(); const replacementId = [...nativeDocuments].find(([, pair]) => pair.publicDocument.metadata.title === "Post-delete identity")[0]; await page.getByText(replacementId, { exact: true }).first().waitFor(); assert.ok(Number(replacementId.match(/-o(\d+)$/)[1]) > Number(singleChoiceId.match(/-o(\d+)$/)[1]));

  const completeSentencesId = await exerciseCompleteSentencesAuthoring({
    page, nativeDocuments, nativeFonts, getNativeIndex: () => nativeIndex, screenshotRoot, uploadReadableText, assertContentTabActive,
  }); await exerciseCompleteSentencesBulkHotspotImport({ page, nativeDocuments, firstPanelPng: halfHeightChoicePng, secondPanelPng: portraitChoicePng });
  const openResponseFontSearch = page.getByPlaceholder("Search title, type, or ID"); await openResponseFontSearch.fill(openResponseId); for (let depth = 0; depth < 3; depth += 1) { await page.locator('.activity-tree-toggle[aria-expanded="false"]').evaluateAll((buttons) => buttons.forEach((button) => button.click())); await page.waitForTimeout(20); } await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: "Response for question 1" }).click();
  const fontResponseControls = page.getByRole("group", { name: "Response region quick controls" }); const sharedAnswerFont = fontResponseControls.getByLabel("Answer font"); await sharedAnswerFont.selectOption({ label: "Ahem" }); assert.equal(nativeFonts.size, 1, "Open Response must reuse the font uploaded through Complete the Sentences");
  await fontResponseControls.getByLabel("Quick Line count").fill("1"); await fontResponseControls.getByLabel("Quick Line spacing").fill("40"); await fontResponseControls.getByLabel("Requested answer size").fill("16"); await fontResponseControls.getByLabel("Requested answer size").press("Enter"); await fontResponseControls.getByLabel("Answer text color").fill("#2468ac");
  const typographySample = fontResponseControls.getByLabel("Answer typography sample"); await typographySample.waitFor(); assert.equal(await typographySample.textContent(), "Answer sample"); assert.equal(await typographySample.getAttribute("data-requested-font-size"), "16"); assert.doesNotMatch(await typographySample.textContent(), /private model answer/i); await assertAhemRendering(typographySample, "Open Response inspector sample");
  await page.getByRole("tab", { name: "Local Preview" }).click(); const openResponseFontPreview = page.locator(".native-or-preview"); await openResponseFontPreview.getByRole("button", { name: "Student Preview" }).click(); let studentAnswer = openResponseFontPreview.locator(".native-or-student-response").first(); await studentAnswer.fill("OK"); await studentAnswer.waitFor({ state: "visible" }); assert.equal(await studentAnswer.getAttribute("data-requested-font-size"), "16"); assert.equal(await studentAnswer.getAttribute("data-effective-font-size"), "16"); assert.ok(Math.abs(await logicalFontSize(studentAnswer) - 16) < .2); await assertAhemRendering(studentAnswer, "Open Response Student Preview at 16px");
  await page.getByRole("tab", { name: "Layout" }).click(); await fontResponseControls.waitFor(); await fontResponseControls.getByLabel("Requested answer size").fill("32"); await fontResponseControls.getByLabel("Requested answer size").press("Enter"); assert.equal(await typographySample.getAttribute("data-requested-font-size"), "32");
  await page.getByRole("tab", { name: "Local Preview" }).click(); await openResponseFontPreview.getByRole("button", { name: "Student Preview" }).click(); studentAnswer = openResponseFontPreview.locator(".native-or-student-response").first(); await studentAnswer.fill("OK"); assert.equal(await studentAnswer.getAttribute("data-requested-font-size"), "32"); assert.equal(await studentAnswer.getAttribute("data-effective-font-size"), "32"); assert.ok(Math.abs(await logicalFontSize(studentAnswer) - 32) < .2); await studentAnswer.fill("Ahem answer text that is deliberately much too long to remain at the requested size in one response region."); assert.ok(Number(await studentAnswer.getAttribute("data-effective-font-size")) < 32); assert.equal(await studentAnswer.getAttribute("data-requested-font-size"), "32");
  await openResponseFontPreview.getByRole("button", { name: "Teacher Preview" }).click(); const fontTeacherAnswer = openResponseFontPreview.locator(".native-or-answer-layer").first(); await fontTeacherAnswer.click(); const teacherLine = fontTeacherAnswer.locator(".native-or-answer-line").first(); await assertAhemRendering(teacherLine, "Open Response Teacher Preview"); await openResponseFontPreview.locator('.native-or-answer-layer[data-font-status="loaded"]').first().waitFor(); assert.equal(await fontTeacherAnswer.getAttribute("data-requested-font-size"), "32"); const teacherEffectiveFontSize = await fontTeacherAnswer.getAttribute("data-effective-font-size"); assert.ok(Number(teacherEffectiveFontSize) <= 32);
  await page.getByRole("tab", { name: "Layout" }).click(); await fontResponseControls.waitFor(); await fontResponseControls.getByText(/requested 32px · rendered|Requested and rendered at 32px/).waitFor();
  await page.getByRole("button", { name: "Save Draft", exact: true }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const savedOpenResponseFontDocument = nativeDocuments.get(openResponseId).publicDocument; const savedOpenResponseAnswerPresentation = savedOpenResponseFontDocument.parts[0].interaction.questions[0].responseRegion.presentation; assert.equal(savedOpenResponseAnswerPresentation.answerFontAssetSlot, [...nativeFonts.values()][0].reference.slot); assert.equal(savedOpenResponseAnswerPresentation.answerFontSizeMax, 32); assert.equal(savedOpenResponseAnswerPresentation.color, "#2468ac"); assert.equal(savedOpenResponseFontDocument.assets.filter((asset) => asset.role === "activity_font").length, 1); assert.doesNotMatch(JSON.stringify(savedOpenResponseFontDocument), /private model answer/i);

  const brokenAssetId = "20000000-0000-4000-8000-999999999999"; const brokenSlot = `font-${brokenAssetId.replaceAll("-", "")}`; nativeFonts.set(brokenAssetId, { bytes: Buffer.from("not-a-font"), type: "font/ttf", reference: { assetId: brokenAssetId, checksumSha256: "0".repeat(64), role: "activity_font", slot: brokenSlot, displayLabel: "Broken fixture", familyAlias: `hh-native-font-${brokenAssetId.replaceAll("-", "")}`, byteSize: 10, previewUrl: `${nativeRoot}/fonts/${brokenAssetId}/preview` } });
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: new RegExp(openResponseId) }).click(); await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: "Response for question 1" }).click(); const failureControls = page.getByRole("group", { name: "Response region quick controls" }); await failureControls.getByLabel("Answer font").selectOption({ label: "Broken fixture" }); await failureControls.getByText("Selected font could not be loaded; using the default font.", { exact: true }).waitFor(); assert.equal(await failureControls.getByLabel("Answer typography sample").getAttribute("data-font-status"), "error"); await failureControls.getByLabel("Answer font").selectOption({ label: "Ahem" }); await failureControls.getByLabel("Answer typography sample").waitFor(); await page.getByRole("button", { name: "Save Draft", exact: true }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); normalizeNativeRuntimePublicDocument(nativeDocuments.get(openResponseId).publicDocument, { activityId: openResponseId, kind: "open-response" }); await openResponseFontSearch.fill("");
  await page.locator('.hosted-builder-tool-tabs a[href$="/hotspots"]').click(); await drawPageHotspot(page); await page.getByLabel("Activity").selectOption(openResponseId); await page.getByLabel("Label").fill("Draft Open Response hotspot"); await page.locator(".builder-save-state").getByRole("button", { name: "Save", exact: true }).click(); await page.locator(".builder-save-state").getByText("Saved", { exact: true }).waitFor(); const hostedFontFrame = page.locator(".unified-builder-review-dialog iframe"); const previousHostedFontFrameUrl = await hostedFontFrame.evaluate((iframe) => iframe.src); await page.getByRole("button", { name: "Review", exact: true }).click(); await page.waitForFunction(({ previous }) => { const iframe = document.querySelector(".unified-builder-review-dialog iframe"); return iframe?.src && iframe.src !== previous; }, { previous: previousHostedFontFrameUrl }); viewer = page.frameLocator(".unified-builder-review-dialog iframe"); const hostedFontHotspot = viewer.getByRole("button", { name: "Draft Open Response hotspot" }); await hostedFontHotspot.waitFor(); await hostedFontHotspot.evaluate((button) => button.click()); const hostedFontAnswer = viewer.locator(".native-or-answer-layer").first(); await hostedFontAnswer.waitFor(); await viewer.getByRole("button", { name: "Show All", exact: true }).click(); const hostedFontLine = hostedFontAnswer.locator(".native-or-answer-line").first(); await assertAhemRendering(hostedFontLine, "hosted Viewer Open Response"); await viewer.locator('.native-or-answer-layer[data-font-status="loaded"]').first().waitFor(); assert.equal(await hostedFontAnswer.getAttribute("data-requested-font-size"), "32"); assert.equal(await hostedFontAnswer.getAttribute("data-effective-font-size"), teacherEffectiveFontSize); await page.getByRole("button", { name: "Close Review" }).click(); await page.locator('.hosted-builder-tool-tabs a[href$="/activities"]').click();
  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /^Listening / }).check(); await page.getByLabel(/Initial title/).fill("Browser native listening"); await page.getByRole("button", { name: "Create activity" }).click(); await assertContentTabActive(page, ".native-listening-editor");
  await page.getByRole("heading", { name: "Browser native listening" }).first().waitFor(); const listeningId = [...nativeDocuments].find(([, pair]) => pair.publicDocument.metadata.title === "Browser native listening")[0]; assert.equal(await page.getByLabel("Visible instruction").count(), 0); await page.getByLabel("Activity title").fill("Persisted browser listening");
  await page.getByRole("tab", { name: "Audio & Transcript" }).click();
  await page.locator('.native-listening-editor input[type=file][accept*="audio/mpeg"]').setInputFiles({ name: "listening.mp3", mimeType: "audio/mpeg", buffer: listeningMp3 }); await page.getByText("Listening MP3 uploaded.", { exact: true }).waitFor();
  await page.locator('.native-listening-editor input[type=file][accept*="image/png"]').setInputFiles({ name: "listening-background.png", mimeType: "image/png", buffer: tallReadablePng }); await page.getByText(/Background uploaded/).waitFor();
  await page.locator('.native-listening-editor input[type=file][accept*=".srt"]').setInputFiles({ name: "listening.srt", mimeType: "application/x-subrip", buffer: Buffer.from(listeningSrt) }); await page.getByText("10 SRT cues imported.", { exact: true }).waitFor(); await page.screenshot({ path: path.join(screenshotRoot, "native-listening-audio-transcript-1440.png"), fullPage: true });
  await page.getByRole("button", { name: /Cue 2/ }).click(); await page.getByLabel("Transcript text").fill("Edited transcript cue two."); await page.getByLabel("Timing playhead").fill("1000"); await page.getByRole("button", { name: "Set Start" }).click(); await page.getByLabel("Start (MM:SS)").fill("00:01"); await page.getByLabel("Start (MM:SS)").press("Tab"); await page.getByRole("tab", { name: "Content" }).click(); await page.getByRole("button", { name: "Add Question" }).click(); await page.getByRole("textbox", { name: "Prompt", exact: true }).fill("What is the speaker's main point?"); await page.getByRole("tab", { name: "Answer Key" }).click(); await page.getByLabel("Private model answer").fill("Private listening model answer"); await page.getByRole("tab", { name: "Visual" }).click(); await page.locator(".native-listening-question-authoring .native-or-upload input").setInputFiles({ name: "listening-question.png", mimeType: "image/png", buffer: onePixelPng }); await page.getByLabel("Alt text").fill("Listening question illustration"); await page.getByRole("button", { name: "Add Show Text Hotspot" }).click(); await page.getByLabel("Accessible label").fill("Show the opening transcript excerpt"); await page.getByLabel(/Cue 2: Edited transcript cue two\./).check(); await page.locator('.native-listening-question-properties input[type="file"][accept*="audio/mpeg"]').setInputFiles({ name: "listening-hotspot.mp3", mimeType: "audio/mpeg", buffer: hotspotMp3(7) }); await page.getByText("Hotspot MP3 assigned.", { exact: true }).waitFor(); await page.screenshot({ path: path.join(screenshotRoot, "native-listening-visual-1440.png"), fullPage: true });
  await uploadVideoCompanion(page); await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); const listeningDraftPreview = page.locator(".native-or-preview .native-listening"); await listeningDraftPreview.waitFor(); const listeningDraftGeometry = await listeningDraftPreview.evaluate((root) => { const measure = (element) => { const rect = element?.getBoundingClientRect(); const style = element ? getComputedStyle(element) : null; return element ? { tag: element.tagName, className: element.className, text: element.textContent?.trim(), width: rect.width, height: rect.height, display: style.display, visibility: style.visibility, opacity: style.opacity } : null; }; return { root: measure(root), stage: measure(root.querySelector(".native-listening-local-stage")), session: measure(root.querySelector(".native-or-panel-session")), surface: measure(root.querySelector(".native-or-surface")), prompt: measure(root.querySelector(".native-or-prompt")) }; }); console.log(`LISTENING_DRAFT_PREVIEW_GEOMETRY ${JSON.stringify(listeningDraftGeometry)}`); assert.equal(listeningDraftGeometry.prompt.text, "What is the speaker's main point?"); assert.ok(listeningDraftGeometry.root.height > 0 && listeningDraftGeometry.prompt.height > 0, JSON.stringify(listeningDraftGeometry)); await page.getByText("Unsaved changes", { exact: true }).waitFor(); const listeningReadinessIssues = await page.locator(".native-listening-editor .studio-readiness li").allTextContents(); assert.deepEqual(listeningReadinessIssues, [], JSON.stringify(listeningReadinessIssues)); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const savedListening = nativeDocuments.get(listeningId); const savedListeningInteraction = savedListening.publicDocument.parts[0].interaction; assert.equal(savedListeningInteraction.cues[1].startMs, 1_000); assert.equal(savedListeningInteraction.artwork.length, 1); assert.equal(savedListeningInteraction.artwork[0].altText, "Listening question illustration"); assert.equal(savedListeningInteraction.snippetHotspots[0].cueIds.length, 2); const savedSnippetAudioSlot = savedListeningInteraction.snippetHotspots[0].audioAssetSlot; assert.ok(savedSnippetAudioSlot); assert.notEqual(savedSnippetAudioSlot, savedListeningInteraction.audioAssetSlot); const savedSnippetAudio = savedListening.publicDocument.assets.find((asset) => asset.slot === savedSnippetAudioSlot); const savedMainAudio = savedListening.publicDocument.assets.find((asset) => asset.slot === savedListeningInteraction.audioAssetSlot); assert.ok(savedSnippetAudio); assert.notEqual(savedSnippetAudio.assetId, savedMainAudio.assetId); assert.doesNotMatch(JSON.stringify(savedListening.publicDocument), /Private listening model answer/); assert.match(JSON.stringify(savedListening.teacherDocument), /Private listening model answer/);
  await page.reload({ waitUntil: "domcontentloaded" }); await page.getByLabel("Access").selectOption("all"); await page.getByLabel("Type").selectOption("all"); await page.getByPlaceholder("Search title, type, or ID").fill(listeningId); for (let depth = 0; depth < 3; depth += 1) { await page.locator('.activity-tree-toggle[aria-expanded="false"]').evaluateAll((buttons) => buttons.forEach((button) => button.click())); await page.waitForTimeout(30); } await page.getByRole("button", { name: new RegExp(listeningId) }).click(); await page.getByRole("heading", { name: "Persisted browser listening" }).waitFor(); await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); const listeningPreview = page.locator(".native-or-preview .native-listening"); await listeningPreview.waitFor(); assert.equal(await listeningPreview.getAttribute("data-view"), "questions"); assert.equal(await listeningPreview.getByRole("tab").count(), 0);
  const mediaBeforeListening = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await listeningPreview.getByRole("button", { name: "Play Listening audio" }).click(); await page.waitForFunction((element) => element.getAttribute("data-view") === "transcript", await listeningPreview.elementHandle()); const mediaAfterListening = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(mediaAfterListening.playCount > mediaBeforeListening.playCount); assert.match(mediaAfterListening.lastSource, new RegExp(`/activities/${listeningId}/assets/`)); const muteButton = listeningPreview.getByRole("button", { name: "Mute Listening audio" }); await muteButton.click(); assert.equal(await listeningPreview.locator("audio").evaluate((audio) => audio.muted), true); await listeningPreview.getByRole("button", { name: "Unmute Listening audio" }).click(); assert.equal(await listeningPreview.locator("audio").evaluate((audio) => audio.muted), false); const beforePause = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await listeningPreview.getByRole("button", { name: "Pause Listening audio" }).click(); const afterPause = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(afterPause.pauseCount > beforePause.pauseCount); await listeningPreview.locator("audio").evaluate((audio) => Object.defineProperty(audio, "currentTime", { configurable: true, writable: true, value: 0 })); const listeningSeek = listeningPreview.getByRole("slider", { name: "Listening audio position" }); await listeningSeek.fill("1200"); assert.equal(Number(await listeningSeek.inputValue()), 1200); assert.equal(await listeningPreview.locator("audio").evaluate((audio) => audio.currentTime), 1.2); const activeListeningCue = listeningPreview.locator('.native-listening-transcript p.is-active').filter({ hasText: "Edited transcript cue two." }); await activeListeningCue.waitFor(); await page.waitForFunction((element) => getComputedStyle(element).backgroundColor === "rgba(250, 204, 21, 0.28)", await activeListeningCue.elementHandle()); const activeCueStyle = await activeListeningCue.evaluate((element) => { const style = getComputedStyle(element); return { backgroundColor: style.backgroundColor, boxShadow: style.boxShadow, outlineStyle: style.outlineStyle, borderWidths: [style.borderTopWidth, style.borderRightWidth, style.borderBottomWidth, style.borderLeftWidth] }; }); assert.equal(activeCueStyle.backgroundColor, "rgba(250, 204, 21, 0.28)"); assert.equal(activeCueStyle.boxShadow, "none"); assert.equal(activeCueStyle.outlineStyle, "none"); assert.deepEqual(activeCueStyle.borderWidths, ["0px", "0px", "0px", "0px"]);
  await listeningPreview.getByRole("button", { name: "Play Listening audio" }).click(); const afterResume = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(afterResume.playCount > beforePause.playCount); await listeningPreview.getByRole("button", { name: "Stop Listening audio" }).click(); assert.equal(await listeningPreview.getAttribute("data-view"), "questions"); assert.match(await listeningPreview.getByLabel("Listening audio time").innerText(), /^00:00 \/ /); await listeningPreview.getByRole("button", { name: "Play Listening audio" }).click();
  await listeningPreview.locator("audio").evaluate((audio) => { Object.defineProperty(audio, "currentTime", { configurable: true, writable: true, value: 7.4 }); audio.dispatchEvent(new Event("timeupdate")); }); const ninthActiveCue = listeningPreview.locator('.native-listening-transcript p.is-active').filter({ hasText: /cue 9/i }); await ninthActiveCue.waitFor(); assert.match(await listeningPreview.getByLabel("Listening audio time").innerText(), /^00:07 \/ \d{2,}:\d{2}$/); await page.waitForTimeout(200); assert.ok(await listeningPreview.locator(".native-listening-transcript").evaluate((element) => element.scrollTop) > 0);
  await listeningPreview.locator("audio").evaluate((audio) => { audio.currentTime = 1.2; audio.dispatchEvent(new Event("timeupdate")); }); await listeningPreview.locator('.native-listening-transcript p.is-active').filter({ hasText: "Edited transcript cue two." }).waitFor();
  await listeningPreview.locator("audio").evaluate((audio) => { audio.currentTime = Number(audio.closest(".native-listening").querySelector('input[type="range"]').max) / 1_000 + 1; audio.pause(); audio.dispatchEvent(new Event("ended")); }); const endedTimer = listeningPreview.getByLabel("Listening audio time"); await page.waitForFunction((element) => { const [current, total] = element.textContent.split("/").map((value) => value.trim()); return current === total; }, await endedTimer.elementHandle()); const beforeReplay = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await listeningPreview.getByRole("button", { name: "Play Listening audio" }).click(); const afterReplay = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.equal(afterReplay.playCount, beforeReplay.playCount + 1);
  await listeningPreview.getByRole("button", { name: "Stop Listening audio" }).click(); const beforeHotspotAudio = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); await listeningPreview.getByRole("button", { name: "Show the opening transcript excerpt" }).evaluate((button) => button.click()); const firstReferenceCue = listeningPreview.locator('.native-listening-transcript p.is-reference').filter({ hasText: /Transcript cue 1 has enough semantic classroom text/ }); const secondReferenceCue = listeningPreview.locator('.native-listening-transcript p.is-reference').filter({ hasText: "Edited transcript cue two." }); await firstReferenceCue.waitFor(); await secondReferenceCue.waitFor(); assert.equal(await listeningPreview.locator('.native-listening-transcript p.is-reference').count(), 2); assert.equal(await firstReferenceCue.getAttribute("aria-current"), "true"); const hotspotAudioSource = await listeningPreview.locator("audio").getAttribute("src"); assert.match(hotspotAudioSource, new RegExp(savedSnippetAudio.assetId)); const afterHotspotAudio = await listeningPreview.evaluate(() => globalThis.__hhplmsMediaTest.snapshot()); assert.ok(afterHotspotAudio.playCount > beforeHotspotAudio.playCount); assert.equal(await listeningPreview.getByText("Private listening model answer", { exact: true }).count(), 0);
  await listeningPreview.getByRole("button", { name: "Stop Listening audio" }).click(); assert.equal(await listeningPreview.getAttribute("data-view"), "questions"); await page.locator(".native-or-preview").getByRole("button", { name: "Teacher Preview" }).click(); const listeningTeacher = page.locator(".native-or-preview .native-listening"); const modelAnswerButton = listeningTeacher.locator(".native-or-answer-layer"); await modelAnswerButton.evaluate((button) => button.click()); assert.equal(await listeningTeacher.getByText("Private listening model answer", { exact: true }).count(), 1);

  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /Oldschool Listening/ }).check(); await page.getByLabel(/Initial title/).fill("Browser oldschool listening"); await page.getByRole("button", { name: "Create activity" }).click(); await assertContentTabActive(page, ".native-oldschool-listening-editor"); assert.equal(await page.getByRole("tab", { name: "Supplemental MP3" }).count(), 0);
  const oldschoolId = [...nativeDocuments].find(([, pair]) => pair.publicDocument.metadata.title === "Browser oldschool listening")[0]; await exerciseOldschoolQuestionVisibility(page);
  await page.getByRole("tab", { name: "Audio & Timeline" }).click(); await page.locator('.native-oldschool-listening-editor input[type=file][accept*="audio/mpeg"]').setInputFiles({ name: "oldschool.mp3", mimeType: "audio/mpeg", buffer: listeningMp3 }); await page.getByText("Oldschool Listening MP3 uploaded.", { exact: true }).waitFor(); await page.locator('.native-oldschool-listening-editor input[type=file][accept*="image/png"]').setInputFiles({ name: "readable-oldschool-page.png", mimeType: "image/png", buffer: tallReadablePng }); await page.getByText("Page image uploaded at its intrinsic dimensions.", { exact: true }).waitFor(); await page.getByLabel("Page image alt text").fill("A full printed listening page with three highlighted passages"); await page.locator('.native-oldschool-listening-editor input[type=file][accept*=".srt"]').setInputFiles({ name: "oldschool.srt", mimeType: "application/x-subrip", buffer: Buffer.from(oldschoolSrt) }); await page.getByText("3 SRT cues imported; page mappings are intentionally empty.", { exact: true }).waitFor();
  await page.getByRole("tab", { name: "Page Mapping" }).click(); const mappingViewport = page.locator(".native-oldschool-mapping-viewport"); const mappingCanvas = page.locator(".native-oldschool-mapping-canvas"); const drawOldschoolRegion = async (x, y, width, height) => { await page.getByRole("button", { name: "Draw highlight region" }).click(); await mappingViewport.scrollIntoViewIfNeeded(); const box = await mappingCanvas.boundingBox(); assert.ok(box); await page.mouse.move(box.x + box.width * x, box.y + box.height * y); await page.mouse.down(); await page.mouse.move(box.x + box.width * (x + width), box.y + box.height * (y + height)); await page.mouse.up(); await page.getByRole("group", { name: "Oldschool Listening highlight region selected" }).waitFor(); };
  const clearOldschoolRegionSelection = async () => { const frame = page.getByRole("group", { name: "Oldschool Listening highlight region selected" }); await frame.press("Escape"); await frame.waitFor({ state: "hidden" }); }; await drawOldschoolRegion(.12, .10, .32, .022); await clearOldschoolRegionSelection(); await drawOldschoolRegion(.55, .10, .22, .022); await clearOldschoolRegionSelection(); await drawOldschoolRegion(.12, .18, .35, .022); await clearOldschoolRegionSelection(); await drawOldschoolRegion(.55, .18, .22, .022); await page.getByRole("button", { name: /Cue 2/ }).click(); await page.getByLabel("Authored scroll target Y (source pixels)").fill(""); await page.locator(".native-oldschool-mapping-viewport").evaluate((element) => { element.scrollTop = element.scrollHeight / 4; }); await drawOldschoolRegion(.10, .48, .72, .022); await page.getByRole("button", { name: /Cue 3/ }).click(); await page.locator(".native-oldschool-mapping-viewport").evaluate((element) => { element.scrollTop = element.scrollHeight; }); await drawOldschoolRegion(.10, .76, .72, .022); await page.getByLabel("Authored scroll target Y (source pixels)").fill("1450"); assert.equal(await page.locator('.native-oldschool-mapping-cues button[data-mapped="true"]').count(), 3); await importOldschoolExactTranscriptMapping(page);
  await page.getByRole("tab", { name: "Local Preview" }).click(); await page.getByRole("button", { name: "Student Preview" }).click(); assert.deepEqual(await page.locator(".native-oldschool-listening-editor .studio-readiness li").allTextContents(), []); await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor(); const savedOldschool = nativeDocuments.get(oldschoolId); const oldschoolInteraction = savedOldschool.publicDocument.parts[0].interaction; assert.deepEqual(oldschoolInteraction.questionSurface, { promptQuestionIds: [], responseQuestionIds: oldschoolInteraction.questions.map((question) => question.id) }); assert.equal(oldschoolInteraction.questions[0].responseRegion.presentation.answerFontSizeMax, 32); assert.equal(oldschoolInteraction.questions[0].responseRegion.presentation.answerSizeMode, "authored"); assert.equal(oldschoolInteraction.questions[0].responseRegion.presentation.color, "#224466"); assert.deepEqual(oldschoolInteraction.cues.map((cue) => cue.highlightRegions.length), [4, 1, 1]); assert.equal(oldschoolInteraction.cues[1].scrollY, null); assert.equal(oldschoolInteraction.cues[2].scrollY, 1450); assert.equal(oldschoolInteraction.cues.slice(0, 2).flatMap((cue) => cue.highlightRegions).every((region) => typeof region.text === "string" && region.text.length > 0), true); assert.equal(Object.hasOwn(oldschoolInteraction.cues[2].highlightRegions[0], "text"), false); assert.doesNotMatch(JSON.stringify(savedOldschool.publicDocument), /speaker emphasizes the later printed detail/i); assert.match(JSON.stringify(savedOldschool.teacherDocument), /speaker emphasizes the later printed detail/i);
  await exerciseOldschoolPersistentTranscript({ page, oldschoolId, nativeDocuments, screenshotRoot, publishViewerHotspot: () => { hotspotManifest = structuredClone(hotspotManifest); hotspotManifest.pages["ub2-sb-unit-1-part-1"] ||= []; hotspotManifest.pages["ub2-sb-unit-1-part-1"].push({ id: "browser-oldschool-listening-launch", unitNumber: 1, pageId: "ub2-sb-unit-1-part-1", pageNumber: 5, left: 30, top: 12, width: 12, height: 12, label: "Draft Oldschool Listening hotspot", actionType: "normalized_activity", activityKey: oldschoolId }); hotspotRevision += 1; } });
  await exerciseOldschoolMultipleChoicePanel({ page, oldschoolId, nativeDocuments, screenshotRoot });
  const tablet = await context.newPage(); await tablet.setViewportSize({ width: 900, height: 700 }); await tablet.goto(`${origin}/#/books/ultimate-b2/components/ultimate-b2-students-book/activities`, { waitUntil: "domcontentloaded" }); await tablet.getByRole("button", { name: new RegExp(openResponseId) }).click(); await tablet.getByRole("tab", { name: "Local Preview" }).click(); await tablet.getByRole("button", { name: "Teacher Preview" }).click(); await tablet.getByRole("button", { name: /Reveal model answer/ }).click();
  const geometry = await tablet.locator(".native-or-surface:visible").evaluate((surface) => { const rect = surface.getBoundingClientRect(); const line = surface.querySelector(".native-or-line"); const answer = surface.querySelector(".native-or-answer-line"); return { ratio: rect.width / rect.height, lineTop: line?.style.top, answerTop: answer?.style.top }; });
  assert.ok(Math.abs(geometry.ratio - (1024 / 582)) < 0.02); assert.equal(geometry.answerTop, geometry.lineTop); await tablet.close();
  const mobile = await context.newPage(); await mobile.setViewportSize({ width: 768, height: 900 }); await mobile.goto(`${origin}/#/books/ultimate-b2/components/ultimate-b2-students-book/activities`, { waitUntil: "domcontentloaded" }); await mobile.getByRole("button", { name: "Add Activity" }).waitFor(); assert.deepEqual(await mobile.evaluate(() => ({ viewport: innerWidth, body: document.body.scrollWidth, root: document.documentElement.scrollWidth })), await mobile.evaluate(() => ({ viewport: innerWidth, body: innerWidth, root: innerWidth }))); await mobile.waitForTimeout(250); await mobile.screenshot({ path: path.join(screenshotRoot, "activity-builder-768.png"), fullPage: true }); await mobile.close();
  await page.getByRole("button", { name: "Add Activity" }).click(); await page.getByRole("radio", { name: /Drag & Drop/ }).check(); await page.getByLabel(/Initial title/).fill("Browser native drag and drop"); await page.getByRole("button", { name: "Create activity" }).click(); await page.getByRole("heading", { name: "Browser native drag and drop" }).first().waitFor(); await assertContentTabActive(page, ".native-drag-drop-editor");
  const dragDropId = [...nativeDocuments].find(([, pair]) => pair.publicDocument.metadata.title === "Browser native drag and drop")[0]; await page.getByText(dragDropId, { exact: true }).first().waitFor(); await page.getByRole("tab", { name: "Content" }).click();
  for (const value of ["First", "repeated", "repeated", "distractor", ...Array.from({ length: 27 }, (_, index) => `extra-${index + 1}`), "A deliberately longer draggable answer that must retain its exact chip geometry."]) { await page.getByRole("button", { name: "Add word" }).click(); await page.getByRole("textbox", { name: `Word ${await page.locator(".native-drag-drop-word-row").count()}`, exact: true }).fill(value); }
  await page.getByLabel("Bank words family").selectOption("Georgia"); await page.getByLabel("Quick Bank words size").fill("17"); await page.getByLabel("Bank words color").fill("#17406d"); await page.getByLabel("Bank words managed font").selectOption({ label: "Ahem" });
  await page.getByLabel("Placed answers family").selectOption("Verdana"); await page.getByLabel("Quick Placed answers size").fill("23"); await page.getByLabel("Placed answers color").fill("#6d1740"); await page.getByLabel("Placed answers managed font").selectOption({ label: "Ahem" });
  await page.getByRole("tab", { name: "Layout" }).click(); await page.getByRole("button", { name: "Add panel" }).click();
  await page.getByLabel("Add Background").setInputFiles({ name: "drag-panel-1-background.png", mimeType: "image/png", buffer: landscapeChoicePng }); await page.locator(".native-drag-drop-authoring-stage img").waitFor(); await page.getByLabel("Alt text").fill("First panel background");
  await page.getByLabel("Add Image").setInputFiles({ name: "drag-panel-1-overlay.png", mimeType: "image/png", buffer: smallFourThreeChoicePng }); await page.locator(".native-drag-drop-authoring-stage img").nth(1).waitFor(); await page.getByLabel("Alt text").fill("First panel overlay"); const overlayFrame = page.getByRole("group", { name: "First panel overlay selected" }); await overlayFrame.press("ArrowRight"); const overlayResize = page.getByRole("button", { name: "Resize First panel overlay from bottom right" }); const overlayResizeBox = await overlayResize.boundingBox(); assert.ok(overlayResizeBox); await page.mouse.move(overlayResizeBox.x + overlayResizeBox.width / 2, overlayResizeBox.y + overlayResizeBox.height / 2); await page.mouse.down(); await page.mouse.move(overlayResizeBox.x + overlayResizeBox.width / 2 + 24, overlayResizeBox.y + overlayResizeBox.height / 2 + 16); await page.mouse.up(); await page.getByRole("button", { name: "Send backward" }).click(); await page.getByRole("button", { name: "Bring forward" }).click();
  const drawDragTarget = async (startX, startY, { keepSelected = false } = {}) => { await page.getByRole("button", { name: "Draw Drop Target" }).click(); await page.getByRole("button", { name: "Cancel drawing" }).waitFor(); const canvas = page.locator(".native-drag-drop-authoring-stage"); await canvas.scrollIntoViewIfNeeded(); const box = await canvas.boundingBox(); assert.ok(box); await page.mouse.move(box.x + box.width * startX, box.y + box.height * startY); await page.mouse.down(); await page.mouse.move(box.x + box.width * (startX + .22), box.y + box.height * (startY + .12)); await page.mouse.up(); const selected = page.getByRole("group", { name: /Drop target \d+ selected/ }); await selected.waitFor(); if (!keepSelected) await selected.press("Escape"); return selected; };
  const firstSelectedDragTarget = await drawDragTarget(.12, .62, { keepSelected: true });
  const inspectorMapping = page.getByLabel("Correct word mapping"); assert.match(await inspectorMapping.locator("option:checked").textContent(), /^First .* word 1$/, "new targets receive the next unused word automatically");
  const quickX = page.getByLabel("Quick X"); const originalTargetX = Number(await quickX.inputValue()); const requestedTargetX = originalTargetX + 11.25; const authoredTargetX = roundStageValue(requestedTargetX); const selectedBoxBeforeNumericEdit = await firstSelectedDragTarget.boundingBox(); await quickX.fill(String(requestedTargetX)); await expect(quickX).toHaveValue(String(authoredTargetX)); const selectedBoxAfterNumericEdit = await firstSelectedDragTarget.boundingBox(); assert.ok(selectedBoxBeforeNumericEdit && selectedBoxAfterNumericEdit && selectedBoxAfterNumericEdit.x > selectedBoxBeforeNumericEdit.x, "numeric geometry updates the selected canvas target");
  for (const label of ["Quick Y", "Quick Width", "Quick Height"]) assert.equal(await page.getByLabel(label).count(), 1, `${label} is available in the right inspector`);
  await inspectorMapping.selectOption({ index: 2 }); assert.match(await inspectorMapping.locator("option:checked").textContent(), /^repeated .* word 2$/); await firstSelectedDragTarget.press("Escape");
  await drawDragTarget(.56, .62);
  await page.getByRole("button", { name: "Add panel" }).click(); await page.getByLabel("Add Background").setInputFiles({ name: "drag-panel-2-background.png", mimeType: "image/png", buffer: portraitChoicePng }); await page.getByLabel("Alt text").fill("Second panel background"); await drawDragTarget(.35, .48); await page.getByRole("button", { name: "Move panel 2 up" }).click(); await page.getByRole("button", { name: "Move panel 1 down" }).click(); await page.screenshot({ path: path.join(screenshotRoot, "native-drag-drop-layout-1440.png"), fullPage: true });
  await page.getByRole("tab", { name: "Answer Key" }).click(); const mappingSelects = page.locator(".native-drag-drop-mapping select"); assert.equal(await mappingSelects.count(), 3); assert.match(await mappingSelects.nth(0).locator("option:checked").textContent(), /^repeated .* word 2$/); assert.match(await mappingSelects.nth(1).locator("option:checked").textContent(), /^First .* word 1$/); await mappingSelects.nth(0).selectOption({ label: "First · word 1" }); await page.getByText("That item is already correct for another target. Turn on Reusable item first.", { exact: true }).waitFor(); assert.match(await mappingSelects.nth(0).locator("option:checked").textContent(), /^repeated .* word 2$/, "a conflicting non-reusable mapping preserves the original target"); assert.match(await mappingSelects.nth(1).locator("option:checked").textContent(), /^First .* word 1$/, "a conflicting non-reusable mapping preserves the other target"); await mappingSelects.nth(0).selectOption({ label: "repeated · word 2" }); assert.match(await mappingSelects.nth(1).locator("option:checked").textContent(), /^First .* word 1$/); await page.getByRole("tab", { name: "Local Preview" }).click(); await page.locator(".studio-preview-panel").getByText("First", { exact: true }).first().waitFor(); await page.getByText("Unsaved changes", { exact: true }).waitFor();
  await page.getByRole("button", { name: "Save Draft" }).click(); await page.getByText("Draft saved.", { exact: true }).waitFor();
  const savedDragDrop = nativeDocuments.get(dragDropId); assert.equal(savedDragDrop.publicDocument.parts[0].interaction.panels.length, 2); assert.deepEqual(savedDragDrop.publicDocument.parts[0].interaction.panels.map((panel) => [panel.images.length, panel.dropTargets.length]), [[2, 2], [1, 1]]); assert.ok(savedDragDrop.publicDocument.parts[0].interaction.panels[0].images[1].area.width > 360, "overlay resize persists as canonical logical geometry"); assert.equal(savedDragDrop.publicDocument.parts[0].interaction.panels[0].dropTargets[0].area.x, authoredTargetX, "right-inspector geometry persists canonically"); assert.deepEqual(savedDragDrop.publicDocument.parts[0].interaction.presentation, { bankWordStyle: { fontFamily: "Georgia", fontSize: 17, color: "#17406d", fontAssetSlot: [...nativeFonts.values()][0].reference.slot }, placedAnswerStyle: { fontFamily: "Verdana", fontSize: 23, color: "#6d1740", fontAssetSlot: [...nativeFonts.values()][0].reference.slot } }); assert.equal(savedDragDrop.publicDocument.assets.filter((asset) => asset.role === "activity_font").length, 1, "shared managed font is attached once"); assert.equal(savedDragDrop.teacherDocument.parts[0].solution.mappings.length, 3); assert.doesNotMatch(JSON.stringify(savedDragDrop.publicDocument), /mappings|correctWordId|answerKey|solution|isCorrect/i); assert.match(JSON.stringify(savedDragDrop.teacherDocument), /mappings/);
  const savedDragShape = structuredClone(savedDragDrop.publicDocument.parts[0].interaction); await page.reload({ waitUntil: "domcontentloaded" }); await page.getByPlaceholder("Search title, type, or ID").fill(dragDropId); for (let depth = 0; depth < 3; depth += 1) { await page.locator('.activity-tree-toggle[aria-expanded="false"]').evaluateAll((buttons) => buttons.forEach((button) => button.click())); await page.waitForTimeout(20); } await page.getByRole("button", { name: new RegExp(dragDropId) }).click(); assert.deepEqual(nativeDocuments.get(dragDropId).publicDocument.parts[0].interaction, savedDragShape);
  await page.getByRole("tab", { name: "Local Preview" }).click(); const dragPreview = page.locator(".studio-preview-panel .native-drag-drop"); await dragPreview.waitFor(); for (const viewport of [{ width: 1440, height: 900 }, { width: 1024, height: 768 }, { width: 768, height: 900 }]) {
    await page.setViewportSize(viewport); await dragPreview.waitFor(); const dragLayout = await measureDragDrop(dragPreview, { context: "authored-local-student-preview", viewport });
    assert.ok(dragLayout.usableVisualRatio > .98 && dragLayout.usableVisualRatio < 1.02, JSON.stringify(dragLayout)); assert.ok(dragLayout.usableBankRatio > .18 && dragLayout.usableBankRatio < .22, JSON.stringify(dragLayout)); assert.ok(dragLayout.bankInsideStage && dragLayout.bankOverlapsStage && dragLayout.bankTopRatio > .78 && dragLayout.bankTopRatio < .82, JSON.stringify(dragLayout));
    assert.ok(dragLayout.bankItemRows > 1, JSON.stringify(dragLayout)); if (viewport.width === 768) assert.equal(dragLayout.bankItemsScrollable, true, JSON.stringify(dragLayout)); assert.ok(Math.abs(dragLayout.stageAspectRatio - dragLayout.sourceAspectRatio) < .02 && dragLayout.stageInsideVisual && dragLayout.stageInsideSlot, JSON.stringify(dragLayout));
    assert.ok(dragLayout.horizontalOverflow <= 1, JSON.stringify(dragLayout)); assert.ok(dragLayout.activityScrollOverflow.x <= 1 && dragLayout.activityScrollOverflow.y <= 1, JSON.stringify(dragLayout));
  }
  await page.setViewportSize({ width: 1440, height: 900 });
  assert.equal(await dragPreview.getByText("Choose a word, then choose a target—or drag it into place.", { exact: true }).count(), 0); const styledBankWord = dragPreview.locator("[data-drag-drop-word-id]").first(); await assertAhemRendering(styledBankWord, "Drag & Drop bank word"); const bankTypography = await styledBankWord.evaluate((element) => { const stage = element.closest(".native-drag-drop-stage"); const style = getComputedStyle(element); return { logicalSize: Number.parseFloat(style.fontSize) * Number(stage.dataset.surfaceWidth) / stage.getBoundingClientRect().width, color: style.color }; }); assert.ok(Math.abs(bankTypography.logicalSize - 17) < .2, JSON.stringify(bankTypography)); assert.equal(bankTypography.color, "rgb(23, 64, 109)");
  const restingTarget = dragPreview.locator("[data-drag-drop-target-id]").first(); const restingTargetMetrics = await restingTarget.evaluate((target) => { const targetRect = target.getBoundingClientRect(); const stageRect = target.closest(".native-drag-drop-stage").getBoundingClientRect(); const style = getComputedStyle(target); return { text: target.textContent.trim(), borderTopWidth: style.borderTopWidth, backgroundColor: style.backgroundColor, x: (targetRect.left - stageRect.left) / stageRect.width, y: (targetRect.top - stageRect.top) / stageRect.height, width: targetRect.width / stageRect.width, height: targetRect.height / stageRect.height }; }); const canonicalRestingTarget = savedDragDrop.publicDocument.parts[0].interaction.panels[0].dropTargets[0]; const canonicalSurface = savedDragDrop.publicDocument.parts[0].interaction.panels[0].surface; assert.deepEqual({ text: restingTargetMetrics.text, borderTopWidth: restingTargetMetrics.borderTopWidth, backgroundColor: restingTargetMetrics.backgroundColor }, { text: "", borderTopWidth: "0px", backgroundColor: "rgba(0, 0, 0, 0)" }, "empty targets are visually transparent and contain no placeholder text"); assert.ok(Math.abs(restingTargetMetrics.x - canonicalRestingTarget.area.x / canonicalSurface.width) < .01 && Math.abs(restingTargetMetrics.y - canonicalRestingTarget.area.y / canonicalSurface.height) < .01 && Math.abs(restingTargetMetrics.width - canonicalRestingTarget.area.width / canonicalSurface.width) < .01 && Math.abs(restingTargetMetrics.height - canonicalRestingTarget.area.height / canonicalSurface.height) < .01, "target geometry remains aligned to the contained logical stage");
  const stableBankOrder = await dragPreview.locator("[data-drag-drop-word-id]").evaluateAll((nodes) => nodes.map((node) => node.dataset.dragDropWordId)); await dragPreview.locator("[data-drag-drop-word-id]").first().click(); await dragPreview.locator('[data-drag-drop-word-id][aria-pressed="true"]').click(); assert.deepEqual(await dragPreview.locator("[data-drag-drop-word-id]").evaluateAll((nodes) => nodes.map((node) => node.dataset.dragDropWordId)), stableBankOrder, "ordinary rerenders do not reshuffle the bank");
  await page.evaluate(() => { globalThis.__nativeDragDropOriginalRandom = Math.random; Math.random = () => 0; }); await page.getByRole("tab", { name: "Content" }).click(); await page.getByRole("tab", { name: "Local Preview" }).click(); await dragPreview.waitFor(); const serializedWordIds = savedDragDrop.publicDocument.parts[0].interaction.words.map((word) => word.id); assert.deepEqual(await dragPreview.locator("[data-drag-drop-word-id]").evaluateAll((nodes) => nodes.map((node) => node.dataset.dragDropWordId)), [...serializedWordIds.slice(1), serializedWordIds[0]], "a fresh activity mount creates a fresh randomized presentation order"); await page.evaluate(() => { Math.random = globalThis.__nativeDragDropOriginalRandom; delete globalThis.__nativeDragDropOriginalRandom; });
  await exerciseDragDropPanelTransitionGuard(dragPreview);
  await exerciseDragDropProxy(page, dragPreview, savedDragDrop);
  const autoFillTarget = dragPreview.locator("[data-drag-drop-target-id]").first(); const autoFillTargetId = await autoFillTarget.getAttribute("data-drag-drop-target-id"); const autoFillWordId = savedDragDrop.teacherDocument.parts[0].solution.mappings.find((mapping) => mapping.targetId === autoFillTargetId).wordIds[0]; await autoFillTarget.click(); await expect(autoFillTarget).toHaveAttribute("data-occupied", "true"); assert.equal(await dragPreview.locator(`[data-drag-drop-word-id="${autoFillWordId}"]`).count(), 0, "empty-target click fills through the solution-aware preview boundary and consumes the word"); const placedText = autoFillTarget.locator("[data-drag-drop-target-text]"); await assertAhemRendering(placedText, "Drag & Drop placed answer"); const placedMetrics = await autoFillTarget.evaluate((target) => { const text = target.querySelector("[data-drag-drop-target-text]"); const stage = target.closest(".native-drag-drop-stage"); const targetRect = target.getBoundingClientRect(); const textRect = text.getBoundingClientRect(); const targetStyle = getComputedStyle(target); const textStyle = getComputedStyle(text); return { logicalSize: Number.parseFloat(textStyle.fontSize) * Number(stage.dataset.surfaceWidth) / stage.getBoundingClientRect().width, color: textStyle.color, borderWidth: targetStyle.borderTopWidth, background: targetStyle.backgroundColor, inside: textRect.left >= targetRect.left - 1 && textRect.right <= targetRect.right + 1 && textRect.top >= targetRect.top - 1 && textRect.bottom <= targetRect.bottom + 1, centerDelta: { x: Math.abs((textRect.left + textRect.right - targetRect.left - targetRect.right) / 2), y: Math.abs((textRect.top + textRect.bottom - targetRect.top - targetRect.bottom) / 2) } }; }); assert.ok(Math.abs(placedMetrics.logicalSize - 23) < .2 && placedMetrics.inside && placedMetrics.centerDelta.x < 2 && placedMetrics.centerDelta.y < 2, JSON.stringify(placedMetrics)); assert.deepEqual({ color: placedMetrics.color, borderWidth: placedMetrics.borderWidth, background: placedMetrics.background }, { color: "rgb(109, 23, 64)", borderWidth: "0px", background: "rgba(0, 0, 0, 0)" }); await autoFillTarget.click(); await expect(autoFillTarget).not.toHaveAttribute("data-occupied", "true"); assert.equal(await dragPreview.locator(`[data-drag-drop-word-id="${autoFillWordId}"]`).count(), 1, "removing an occupied response restores its bank word");
  await dragPreview.getByRole("button", { name: "First", exact: true }).click(); await dragPreview.locator("[data-drag-drop-target-id]").nth(1).click(); await dragPreview.getByText("First", { exact: true }).first().waitFor(); await dragPreview.getByRole("button", { name: "Next", exact: true }).click(); await dragPreview.getByText("Panel 2 of 2", { exact: true }).waitFor(); const dragTarget = dragPreview.locator("[data-drag-drop-target-id]").first(); const dragTargetId = await dragTarget.getAttribute("data-drag-drop-target-id"); const dragWordId = savedDragDrop.teacherDocument.parts[0].solution.mappings.find((mapping) => mapping.targetId === dragTargetId).wordIds[0]; const dragWord = dragPreview.locator(`[data-drag-drop-word-id="${dragWordId}"]`); await dragWord.scrollIntoViewIfNeeded(); const [dragWordBox, dragTargetBox] = await Promise.all([dragWord.boundingBox(), dragTarget.boundingBox()]); assert.ok(dragWordBox && dragTargetBox); await page.mouse.move(dragWordBox.x + dragWordBox.width / 2, dragWordBox.y + dragWordBox.height / 2); await page.mouse.down(); await page.mouse.move(dragTargetBox.x + dragTargetBox.width / 2, dragTargetBox.y + dragTargetBox.height / 2); await page.mouse.up(); assert.equal(await dragTarget.getAttribute("data-occupied"), "true"); await dragPreview.getByRole("button", { name: "Previous", exact: true }).click(); assert.equal(await dragPreview.locator("[data-drag-drop-target-id][data-occupied]").count(), 1, "responses from the first panel persist after navigation");
  await page.getByRole("button", { name: "Teacher Preview" }).click(); const dragTeacher = page.locator(".studio-preview-panel .native-drag-drop-teacher"); await dragTeacher.waitFor(); const teacherTarget = await exerciseValidatedDragDrop(page, dragTeacher, savedDragDrop); assert.equal(await teacherTarget.getAttribute("data-revealed"), null); await teacherTarget.click(); assert.equal(await teacherTarget.getAttribute("data-occupied"), null); await teacherTarget.click(); assert.equal(await teacherTarget.getAttribute("data-revealed"), "true"); assert.ok((await teacherTarget.textContent()).trim().length > 0);
  hotspotManifest = structuredClone(hotspotManifest); hotspotManifest.pages["ub2-sb-unit-1-part-1"] ||= []; hotspotManifest.pages["ub2-sb-unit-1-part-1"].push({ id: "browser-drag-drop-launch", unitNumber: 1, pageId: "ub2-sb-unit-1-part-1", pageNumber: 5, left: 12, top: 12, width: 12, height: 12, label: "Draft Drag and Drop hotspot", actionType: "normalized_activity", activityKey: dragDropId }); hotspotRevision += 1;
  await page.locator('.hosted-builder-tool-tabs a[href$="/hotspots"]').click(); await page.reload({ waitUntil: "domcontentloaded" }); await page.getByRole("button", { name: "Review", exact: true }).click(); viewer = page.frameLocator(".unified-builder-review-dialog iframe"); await viewer.getByRole("button", { name: "Draft Drag and Drop hotspot" }).click({ force: true }); const viewerDragTeacher = viewer.locator(".native-drag-drop-teacher"); await viewerDragTeacher.waitFor(); assert.equal(await viewer.getByText("Student mode", { exact: true }).count(), 0); assert.equal(await viewer.locator(".native-drag-drop-student").count(), 0); for (const viewport of [{ width: 1440, height: 900 }, { width: 1024, height: 768 }, { width: 768, height: 900 }]) {
    await page.setViewportSize(viewport); await viewerDragTeacher.waitFor(); const geometry = await measureDragDrop(viewerDragTeacher, { context: "final-builder-review-hosted-draft-teacher", viewport });
    assert.ok(geometry.activityHostFillRatio > .95 && geometry.activityHostFillRatio < 1.05, JSON.stringify(geometry)); assert.ok(geometry.usableVisualRatio > .98 && geometry.usableVisualRatio < 1.02, JSON.stringify(geometry)); assert.ok(geometry.usableBankRatio > .18 && geometry.usableBankRatio < .22, JSON.stringify(geometry));
    assert.ok(geometry.bankInsideStage && geometry.bankOverlapsStage && geometry.bankTopRatio > .78 && geometry.bankTopRatio < .82, JSON.stringify(geometry)); assert.ok(geometry.stageInsideVisual && geometry.stageInsideSlot, JSON.stringify(geometry));
    assert.ok(geometry.horizontalOverflow <= 1, JSON.stringify(geometry)); assert.ok(geometry.activityScrollOverflow.x <= 1 && geometry.activityScrollOverflow.y <= 1, JSON.stringify(geometry));
  }
  await page.setViewportSize({ width: 1440, height: 900 });
  const validatedViewerTarget = await exerciseDragDropResetGuard(page, viewerDragTeacher, viewer.getByRole("button", { name: "Reload", exact: true }), savedDragDrop); await validatedViewerTarget.click(); assert.equal(await validatedViewerTarget.getAttribute("data-occupied"), null);
  const revealedViewerDragTargets = viewer.locator(".native-drag-drop-teacher-target[data-revealed]"); const viewerDragTargets = viewer.locator(".native-drag-drop-teacher-target"); assert.equal(await revealedViewerDragTargets.count(), 0); assert.equal(await viewer.getByText("First", { exact: true }).count(), 1, "the public reference bank is visible but no mapped target answer is pre-revealed"); await viewerDragTargets.first().click(); await revealedViewerDragTargets.first().waitFor(); assert.equal(await revealedViewerDragTargets.count(), 1); const viewerDragShowNext = viewer.getByRole("button", { name: "Show Next", exact: true }); await viewerDragShowNext.click(); await revealedViewerDragTargets.nth(1).waitFor(); assert.equal(await revealedViewerDragTargets.count(), 2); await viewerDragShowNext.click(); await viewer.getByAltText("Second panel background").waitFor(); assert.equal(await revealedViewerDragTargets.count(), 1); assert.equal(await viewerDragShowNext.isDisabled(), true); const viewerDragPrevious = viewer.getByRole("button", { name: "Previous activity part", exact: true }); await viewerDragPrevious.click(); await viewer.getByAltText("First panel background").waitFor(); assert.equal(await revealedViewerDragTargets.count(), 2); const viewerDragReload = viewer.getByRole("button", { name: "Reload", exact: true }); await viewerDragReload.click(); await viewerDragTeacher.waitFor(); await revealedViewerDragTargets.first().waitFor({ state: "detached" }); assert.equal(await revealedViewerDragTargets.count(), 0); const viewerDragShowAll = viewer.getByRole("button", { name: "Show All", exact: true }); await viewerDragShowAll.click(); await revealedViewerDragTargets.nth(1).waitFor(); assert.equal(await revealedViewerDragTargets.count(), 2); assert.equal(await viewerDragShowAll.isDisabled(), true); const viewerDragNext = viewer.getByRole("button", { name: "Next activity part", exact: true }); await viewerDragNext.click(); await viewer.getByAltText("Second panel background").waitFor(); assert.equal(await revealedViewerDragTargets.count(), 1); await viewerDragReload.click(); await viewerDragTeacher.waitFor(); await revealedViewerDragTargets.first().waitFor({ state: "detached" }); assert.equal(await revealedViewerDragTargets.count(), 0); await page.getByRole("button", { name: "Close Review" }).click();
  await exercisePersistedVisualRefinements(page, { completeSentencesId, listeningId });
  await exerciseDragDropExtensions(page, { dragDropId, savedDragDrop });
  await page.reload({ waitUntil: "domcontentloaded" });
  const markWordsId = await exerciseMarkWordsAuthoring(page, { screenshotRoot, savedPair: (title) => [...nativeDocuments.values()].find((pair) => pair.publicDocument.metadata.title === title) });
  await exerciseMarkWordsHostedViewer(page, { activityId: markWordsId, screenshotRoot }); await exerciseHostedComposition(page, { nativeDocuments, imageId, answerBytes: tallReadablePng });
  assert.equal(requestedPaths.some((value) => /xml|iwb|import\/prepare/i.test(value)), false); process.stdout.write(`Hosted Unit Extra management plus native draft authoring and real Viewer acceptance passed for ${openResponseId}, ${imageId}, ${singleChoiceId}, ${onePanelChoiceId}, ${completeSentencesId}, ${listeningId}, ${oldschoolId}, and ${dragDropId}.\n`);
} catch (error) { for (const context of browser?.contexts() || []) for (const tab of context.pages()) console.error("Synthetic browser failure:", await tab.locator("body").innerText().catch(() => "unavailable")); throw error; } finally { await browser?.close(); await new Promise((resolve) => server.close(resolve)); }
